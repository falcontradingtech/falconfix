// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: c12e762c869d58c7

#pragma once

#include <array>
#include <cstdint>
#include <string_view>
#include <utils/fast_buffer.h>
#include <utils/error_codes.h>
#include <FIX4.4/core/types.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/field_id.h>
#include <FIX4.4/engine/protocol.h>
#include <FIX4.4/messages/Header.h>
#include <FIX4.4/messages/Trailer.h>

namespace fix44::messages {

class UserResponse {
	static constexpr std::size_t P_UserRequestID                = 0;
	static constexpr std::size_t P_Username                     = 1;
	static constexpr std::size_t P_UserStatus                   = 2;
	static constexpr std::size_t P_UserStatusText               = 3;

	static constexpr uint64_t REQUIRED =
		(1ULL << 0) |
		(1ULL << 1);
	static constexpr std::size_t MAX_ENCODE_SIZE = 136;

	Header m_Header{};
	Trailer m_Trailer{};

	types::STRING<32> m_UserRequestID{fields::UserRequestID_NULL};
	types::STRING<32> m_Username{fields::Username_NULL};
	types::INT m_UserStatus{fields::NULL_INT};
	types::STRING<32> m_UserStatusText{fields::UserStatusText_NULL};

	uint64_t m_presence{0};

public:
	using Protocol = fix44::Protocol;
	static constexpr falconfix::ProtocolVersion PROTOCOL = Protocol::VERSION;
	static constexpr std::string_view MSGTYPE = "BF";
	static constexpr bool IS_ADMIN = false;

	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	FFStatus setString(const char *&p, const char *end) noexcept;
	FFStatus setString(std::string_view sv) noexcept {
		const char *p = sv.data();
		const char *end = p + sv.size();
		return setString(p, end);
	}

	// encode
	FFStatus toString(falconfix::FastBuffer &out, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE
			+ m_Header.compute_buffer_size()
			+ m_Trailer.compute_buffer_size();
		return total;
	}

	// accessors
	Header &getRefHeader() noexcept { return m_Header; }
	const Header &getRefHeader() const noexcept { return m_Header; }
	Trailer &getRefTrailer() noexcept { return m_Trailer; }
	const Trailer &getRefTrailer() const noexcept { return m_Trailer; }

	const types::STRING<32> &getUserRequestID() const noexcept { return m_UserRequestID; }
	const types::STRING<32> &getUsername() const noexcept { return m_Username; }
	const types::INT &getUserStatus() const noexcept { return m_UserStatus; }
	const types::STRING<32> &getUserStatusText() const noexcept { return m_UserStatusText; }

	// mutators
	void setUserRequestID(const types::STRING<32> &v) noexcept { m_UserRequestID = v; m_presence |= (1ULL << P_UserRequestID); }
	void setUsername(const types::STRING<32> &v) noexcept { m_Username = v; m_presence |= (1ULL << P_Username); }
	void setUserStatus(types::INT v) noexcept { m_UserStatus = v; m_presence |= (1ULL << P_UserStatus); }
	void setUserStatusText(const types::STRING<32> &v) noexcept { m_UserStatusText = v; m_presence |= (1ULL << P_UserStatusText); }

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		return false;
	}
	bool hasUserRequestID() const noexcept { return (m_presence & (1ULL << P_UserRequestID)) != 0; }
	bool hasUsername() const noexcept { return (m_presence & (1ULL << P_Username)) != 0; }
	bool hasUserStatus() const noexcept { return (m_presence & (1ULL << P_UserStatus)) != 0; }
	bool hasUserStatusText() const noexcept { return (m_presence & (1ULL << P_UserStatusText)) != 0; }
};

} // namespace fix44::messages
