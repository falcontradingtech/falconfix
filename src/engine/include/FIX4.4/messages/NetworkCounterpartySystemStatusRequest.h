// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: ff5bc844b5b296fd

#pragma once

#include <array>
#include <cstdint>
#include <string_view>
#include <utils/fast_buffer.h>
#include <utils/error_codes.h>
#include <FIX4.4/core/types.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/field_id.h>
#include <FIX4.4/engine/protocol.h>
#include <FIX4.4/messages/Header.h>
#include <FIX4.4/messages/Trailer.h>

#include <FIX4.4/components/CompIDReqGrp.h>

namespace fix44::messages {

class NetworkCounterpartySystemStatusRequest {
	static constexpr std::size_t P_NetworkRequestType           = 0;
	static constexpr std::size_t P_NetworkRequestID             = 1;

	static constexpr uint64_t REQUIRED =
		(1ULL << 0) |
		(1ULL << 1);
	static constexpr std::size_t MAX_ENCODE_SIZE = 62;

	Header m_Header{};
	Trailer m_Trailer{};

	types::INT m_NetworkRequestType{fields::NULL_INT};
	types::STRING<32> m_NetworkRequestID{fields::NetworkRequestID_NULL};
	components::CompIDReqGrp m_CompIDReqGrp{};

	uint64_t m_presence{0};

public:
	using Protocol = fix44::Protocol;
	static constexpr falconfix::ProtocolVersion PROTOCOL = Protocol::VERSION;
	static constexpr std::string_view MSGTYPE = "BC";
	static constexpr bool IS_ADMIN = false;

	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	FFStatus setString(const char *&p, const char *end) noexcept;
	FFStatus setString(std::string_view sv) noexcept {
		const char *p = sv.data();
		const char *end = p + sv.size();
		return setString(p, end);
	}

	// encode
	FFStatus toString(falconfix::FastBuffer &out, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE
			+ m_Header.compute_buffer_size()
			+ m_Trailer.compute_buffer_size();
		total += m_CompIDReqGrp.compute_buffer_size();
		return total;
	}

	// accessors
	Header &getRefHeader() noexcept { return m_Header; }
	const Header &getRefHeader() const noexcept { return m_Header; }
	Trailer &getRefTrailer() noexcept { return m_Trailer; }
	const Trailer &getRefTrailer() const noexcept { return m_Trailer; }

	const types::INT &getNetworkRequestType() const noexcept { return m_NetworkRequestType; }
	const types::STRING<32> &getNetworkRequestID() const noexcept { return m_NetworkRequestID; }
	components::CompIDReqGrp &getRefCompIDReqGrp() noexcept { return m_CompIDReqGrp; }
	const components::CompIDReqGrp &getRefCompIDReqGrp() const noexcept { return m_CompIDReqGrp; }

	// mutators
	void setNetworkRequestType(types::INT v) noexcept { m_NetworkRequestType = v; m_presence |= (1ULL << P_NetworkRequestType); }
	void setNetworkRequestID(const types::STRING<32> &v) noexcept { m_NetworkRequestID = v; m_presence |= (1ULL << P_NetworkRequestID); }

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (m_CompIDReqGrp.hasAnySet()) { return true; }
		return false;
	}
	bool hasNetworkRequestType() const noexcept { return (m_presence & (1ULL << P_NetworkRequestType)) != 0; }
	bool hasNetworkRequestID() const noexcept { return (m_presence & (1ULL << P_NetworkRequestID)) != 0; }
};

} // namespace fix44::messages
