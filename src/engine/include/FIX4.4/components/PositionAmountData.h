// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: cefa96f33829bf00

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class PositionAmountData {
public:
	struct NoPosAmt {
		static constexpr std::size_t P_PosAmtType                   = 0;
		static constexpr std::size_t P_PosAmt                       = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 66;

		types::STRING<32> m_PosAmtType{fields::PosAmtType_NULL};
		types::FLOAT m_PosAmt{fields::NULL_AMT};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getPosAmtType() const noexcept { return m_PosAmtType; }
		const types::FLOAT &getPosAmt() const noexcept { return m_PosAmt; }

		// mutators
		void setPosAmtType(const types::STRING<32> &v) noexcept { m_PosAmtType = v; m_presence |= (1ULL << P_PosAmtType); }
		void setPosAmt(types::FLOAT v) noexcept { m_PosAmt = v; m_presence |= (1ULL << P_PosAmt); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasPosAmtType() const noexcept { return (m_presence & (1ULL << P_PosAmtType)) != 0; }
		bool hasPosAmt() const noexcept { return (m_presence & (1ULL << P_PosAmt)) != 0; }
	};

private:
	static constexpr std::size_t P_NoPosAmt                     = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoPosAmt> m_NoPosAmtGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoPosAmtGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoPosAmt> &getRefNoPosAmtGroup() noexcept { return m_NoPosAmtGroup; }
	const std::vector<NoPosAmt> &getRefNoPosAmtGroup() const noexcept { return m_NoPosAmtGroup; }
	std::size_t getNoPosAmt() const noexcept { return m_NoPosAmtGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoPosAmtGroup.empty()) { return true; }
		return false;
	}
	bool hasNoPosAmt() const noexcept { return !m_NoPosAmtGroup.empty(); }
};

} // namespace fix44::components
