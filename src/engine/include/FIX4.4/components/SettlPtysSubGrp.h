// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 57beaea06a68e72c

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class SettlPtysSubGrp {
public:
	struct NoSettlPartySubIDs {
		static constexpr std::size_t P_SettlPartySubID              = 0;
		static constexpr std::size_t P_SettlPartySubIDType          = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 62;

		types::STRING<32> m_SettlPartySubID{fields::SettlPartySubID_NULL};
		types::INT m_SettlPartySubIDType{fields::NULL_INT};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getSettlPartySubID() const noexcept { return m_SettlPartySubID; }
		const types::INT &getSettlPartySubIDType() const noexcept { return m_SettlPartySubIDType; }

		// mutators
		void setSettlPartySubID(const types::STRING<32> &v) noexcept { m_SettlPartySubID = v; m_presence |= (1ULL << P_SettlPartySubID); }
		void setSettlPartySubIDType(types::INT v) noexcept { m_SettlPartySubIDType = v; m_presence |= (1ULL << P_SettlPartySubIDType); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasSettlPartySubID() const noexcept { return (m_presence & (1ULL << P_SettlPartySubID)) != 0; }
		bool hasSettlPartySubIDType() const noexcept { return (m_presence & (1ULL << P_SettlPartySubIDType)) != 0; }
	};

private:
	static constexpr std::size_t P_NoSettlPartySubIDs           = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoSettlPartySubIDs> m_NoSettlPartySubIDsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoSettlPartySubIDsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoSettlPartySubIDs> &getRefNoSettlPartySubIDsGroup() noexcept { return m_NoSettlPartySubIDsGroup; }
	const std::vector<NoSettlPartySubIDs> &getRefNoSettlPartySubIDsGroup() const noexcept { return m_NoSettlPartySubIDsGroup; }
	std::size_t getNoSettlPartySubIDs() const noexcept { return m_NoSettlPartySubIDsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoSettlPartySubIDsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoSettlPartySubIDs() const noexcept { return !m_NoSettlPartySubIDsGroup.empty(); }
};

} // namespace fix44::components
