// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 48352527f9fea2ef

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

#include <FIX4.4/components/NestedParties2.h>

namespace fix44::components {

class OrdAllocGrp {
public:
	struct NoOrders {
		static constexpr std::size_t P_ClOrdID                      = 0;
		static constexpr std::size_t P_OrderID                      = 1;
		static constexpr std::size_t P_SecondaryOrderID             = 2;
		static constexpr std::size_t P_SecondaryClOrdID             = 3;
		static constexpr std::size_t P_ListID                       = 4;
		static constexpr std::size_t P_OrderQty                     = 5;
		static constexpr std::size_t P_OrderAvgPx                   = 6;
		static constexpr std::size_t P_OrderBookingQty              = 7;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 268;

		types::STRING<32> m_ClOrdID{fields::ClOrdID_NULL};
		types::STRING<32> m_OrderID{fields::OrderID_NULL};
		types::STRING<32> m_SecondaryOrderID{fields::SecondaryOrderID_NULL};
		types::STRING<32> m_SecondaryClOrdID{fields::SecondaryClOrdID_NULL};
		types::STRING<32> m_ListID{fields::ListID_NULL};
		NestedParties2 m_NestedParties2{};
		types::FLOAT m_OrderQty{fields::NULL_QTY};
		types::FLOAT m_OrderAvgPx{fields::NULL_PRICE};
		types::FLOAT m_OrderBookingQty{fields::NULL_QTY};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		std::size_t compute_buffer_size() const noexcept {
			return MAX_ENCODE_SIZE
				+ m_NestedParties2.compute_buffer_size();
		}

		// accessors
		NestedParties2 &getRefNestedParties2() noexcept { return m_NestedParties2; }
		const NestedParties2 &getRefNestedParties2() const noexcept { return m_NestedParties2; }
		const types::STRING<32> &getClOrdID() const noexcept { return m_ClOrdID; }
		const types::STRING<32> &getOrderID() const noexcept { return m_OrderID; }
		const types::STRING<32> &getSecondaryOrderID() const noexcept { return m_SecondaryOrderID; }
		const types::STRING<32> &getSecondaryClOrdID() const noexcept { return m_SecondaryClOrdID; }
		const types::STRING<32> &getListID() const noexcept { return m_ListID; }
		const types::FLOAT &getOrderQty() const noexcept { return m_OrderQty; }
		const types::FLOAT &getOrderAvgPx() const noexcept { return m_OrderAvgPx; }
		const types::FLOAT &getOrderBookingQty() const noexcept { return m_OrderBookingQty; }

		// mutators
		void setClOrdID(const types::STRING<32> &v) noexcept { m_ClOrdID = v; m_presence |= (1ULL << P_ClOrdID); }
		void setOrderID(const types::STRING<32> &v) noexcept { m_OrderID = v; m_presence |= (1ULL << P_OrderID); }
		void setSecondaryOrderID(const types::STRING<32> &v) noexcept { m_SecondaryOrderID = v; m_presence |= (1ULL << P_SecondaryOrderID); }
		void setSecondaryClOrdID(const types::STRING<32> &v) noexcept { m_SecondaryClOrdID = v; m_presence |= (1ULL << P_SecondaryClOrdID); }
		void setListID(const types::STRING<32> &v) noexcept { m_ListID = v; m_presence |= (1ULL << P_ListID); }
		void setOrderQty(types::FLOAT v) noexcept { m_OrderQty = v; m_presence |= (1ULL << P_OrderQty); }
		void setOrderAvgPx(types::FLOAT v) noexcept { m_OrderAvgPx = v; m_presence |= (1ULL << P_OrderAvgPx); }
		void setOrderBookingQty(types::FLOAT v) noexcept { m_OrderBookingQty = v; m_presence |= (1ULL << P_OrderBookingQty); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			if (m_NestedParties2.hasAnySet()) { return true; }
			return false;
		}
		bool hasClOrdID() const noexcept { return (m_presence & (1ULL << P_ClOrdID)) != 0; }
		bool hasOrderID() const noexcept { return (m_presence & (1ULL << P_OrderID)) != 0; }
		bool hasSecondaryOrderID() const noexcept { return (m_presence & (1ULL << P_SecondaryOrderID)) != 0; }
		bool hasSecondaryClOrdID() const noexcept { return (m_presence & (1ULL << P_SecondaryClOrdID)) != 0; }
		bool hasListID() const noexcept { return (m_presence & (1ULL << P_ListID)) != 0; }
		bool hasOrderQty() const noexcept { return (m_presence & (1ULL << P_OrderQty)) != 0; }
		bool hasOrderAvgPx() const noexcept { return (m_presence & (1ULL << P_OrderAvgPx)) != 0; }
		bool hasOrderBookingQty() const noexcept { return (m_presence & (1ULL << P_OrderBookingQty)) != 0; }
	};

private:
	static constexpr std::size_t P_NoOrders                     = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 24;

	std::vector<NoOrders> m_NoOrdersGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoOrdersGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoOrders> &getRefNoOrdersGroup() noexcept { return m_NoOrdersGroup; }
	const std::vector<NoOrders> &getRefNoOrdersGroup() const noexcept { return m_NoOrdersGroup; }
	std::size_t getNoOrders() const noexcept { return m_NoOrdersGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoOrdersGroup.empty()) { return true; }
		return false;
	}
	bool hasNoOrders() const noexcept { return !m_NoOrdersGroup.empty(); }
};

} // namespace fix44::components
