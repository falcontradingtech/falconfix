// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 6c3ba4b9f58de549

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class NstdPtys3SubGrp {
public:
	struct NoNested3PartySubIDs {
		static constexpr std::size_t P_Nested3PartySubID            = 0;
		static constexpr std::size_t P_Nested3PartySubIDType        = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 62;

		types::STRING<32> m_Nested3PartySubID{fields::Nested3PartySubID_NULL};
		types::INT m_Nested3PartySubIDType{fields::NULL_INT};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getNested3PartySubID() const noexcept { return m_Nested3PartySubID; }
		const types::INT &getNested3PartySubIDType() const noexcept { return m_Nested3PartySubIDType; }

		// mutators
		void setNested3PartySubID(const types::STRING<32> &v) noexcept { m_Nested3PartySubID = v; m_presence |= (1ULL << P_Nested3PartySubID); }
		void setNested3PartySubIDType(types::INT v) noexcept { m_Nested3PartySubIDType = v; m_presence |= (1ULL << P_Nested3PartySubIDType); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasNested3PartySubID() const noexcept { return (m_presence & (1ULL << P_Nested3PartySubID)) != 0; }
		bool hasNested3PartySubIDType() const noexcept { return (m_presence & (1ULL << P_Nested3PartySubIDType)) != 0; }
	};

private:
	static constexpr std::size_t P_NoNested3PartySubIDs         = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoNested3PartySubIDs> m_NoNested3PartySubIDsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoNested3PartySubIDsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoNested3PartySubIDs> &getRefNoNested3PartySubIDsGroup() noexcept { return m_NoNested3PartySubIDsGroup; }
	const std::vector<NoNested3PartySubIDs> &getRefNoNested3PartySubIDsGroup() const noexcept { return m_NoNested3PartySubIDsGroup; }
	std::size_t getNoNested3PartySubIDs() const noexcept { return m_NoNested3PartySubIDsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoNested3PartySubIDsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoNested3PartySubIDs() const noexcept { return !m_NoNested3PartySubIDsGroup.empty(); }
};

} // namespace fix44::components
