// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 519343d57446481b

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class CollInqQualGrp {
public:
	struct NoCollInquiryQualifier {
		static constexpr std::size_t P_CollInquiryQualifier         = 0;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 25;

		types::INT m_CollInquiryQualifier{fields::NULL_INT};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::INT &getCollInquiryQualifier() const noexcept { return m_CollInquiryQualifier; }

		// mutators
		void setCollInquiryQualifier(types::INT v) noexcept { m_CollInquiryQualifier = v; m_presence |= (1ULL << P_CollInquiryQualifier); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasCollInquiryQualifier() const noexcept { return (m_presence & (1ULL << P_CollInquiryQualifier)) != 0; }
	};

private:
	static constexpr std::size_t P_NoCollInquiryQualifier       = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoCollInquiryQualifier> m_NoCollInquiryQualifierGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoCollInquiryQualifierGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoCollInquiryQualifier> &getRefNoCollInquiryQualifierGroup() noexcept { return m_NoCollInquiryQualifierGroup; }
	const std::vector<NoCollInquiryQualifier> &getRefNoCollInquiryQualifierGroup() const noexcept { return m_NoCollInquiryQualifierGroup; }
	std::size_t getNoCollInquiryQualifier() const noexcept { return m_NoCollInquiryQualifierGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoCollInquiryQualifierGroup.empty()) { return true; }
		return false;
	}
	bool hasNoCollInquiryQualifier() const noexcept { return !m_NoCollInquiryQualifierGroup.empty(); }
};

} // namespace fix44::components
