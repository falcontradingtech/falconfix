// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 49b89dfe0d427545

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

#include <FIX4.4/components/InstrumentLeg.h>
#include <FIX4.4/components/LegBenchmarkCurveData.h>
#include <FIX4.4/components/LegStipulations.h>

namespace fix44::components {

class InstrmtLegSecListGrp {
public:
	struct NoLegs {
		static constexpr std::size_t P_LegSwapType                  = 0;
		static constexpr std::size_t P_LegSettlType                 = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 31;

		InstrumentLeg m_InstrumentLeg{};
		types::INT m_LegSwapType{fields::NULL_INT};
		types::CHAR m_LegSettlType{fields::NULL_CHAR};
		LegStipulations m_LegStipulations{};
		LegBenchmarkCurveData m_LegBenchmarkCurveData{};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		std::size_t compute_buffer_size() const noexcept {
			return MAX_ENCODE_SIZE
				+ m_InstrumentLeg.compute_buffer_size()
				+ m_LegStipulations.compute_buffer_size()
				+ m_LegBenchmarkCurveData.compute_buffer_size();
		}

		// accessors
		InstrumentLeg &getRefInstrumentLeg() noexcept { return m_InstrumentLeg; }
		const InstrumentLeg &getRefInstrumentLeg() const noexcept { return m_InstrumentLeg; }
		LegStipulations &getRefLegStipulations() noexcept { return m_LegStipulations; }
		const LegStipulations &getRefLegStipulations() const noexcept { return m_LegStipulations; }
		LegBenchmarkCurveData &getRefLegBenchmarkCurveData() noexcept { return m_LegBenchmarkCurveData; }
		const LegBenchmarkCurveData &getRefLegBenchmarkCurveData() const noexcept { return m_LegBenchmarkCurveData; }
		const types::INT &getLegSwapType() const noexcept { return m_LegSwapType; }
		const types::CHAR &getLegSettlType() const noexcept { return m_LegSettlType; }

		// mutators
		void setLegSwapType(types::INT v) noexcept { m_LegSwapType = v; m_presence |= (1ULL << P_LegSwapType); }
		void setLegSettlType(types::CHAR v) noexcept { m_LegSettlType = v; m_presence |= (1ULL << P_LegSettlType); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			if (m_InstrumentLeg.hasAnySet()) { return true; }
			if (m_LegStipulations.hasAnySet()) { return true; }
			if (m_LegBenchmarkCurveData.hasAnySet()) { return true; }
			return false;
		}
		bool hasLegSwapType() const noexcept { return (m_presence & (1ULL << P_LegSwapType)) != 0; }
		bool hasLegSettlType() const noexcept { return (m_presence & (1ULL << P_LegSettlType)) != 0; }
	};

private:
	static constexpr std::size_t P_NoLegs                       = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoLegs> m_NoLegsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoLegsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoLegs> &getRefNoLegsGroup() noexcept { return m_NoLegsGroup; }
	const std::vector<NoLegs> &getRefNoLegsGroup() const noexcept { return m_NoLegsGroup; }
	std::size_t getNoLegs() const noexcept { return m_NoLegsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoLegsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoLegs() const noexcept { return !m_NoLegsGroup.empty(); }
};

} // namespace fix44::components
