// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 2d026b09bd8c192c

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class ContraGrp {
public:
	struct NoContraBrokers {
		static constexpr std::size_t P_ContraBroker                 = 0;
		static constexpr std::size_t P_ContraTrader                 = 1;
		static constexpr std::size_t P_ContraTradeQty               = 2;
		static constexpr std::size_t P_ContraTradeTime              = 3;
		static constexpr std::size_t P_ContraLegRefID               = 4;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 162;

		types::STRING<32> m_ContraBroker{fields::ContraBroker_NULL};
		types::STRING<32> m_ContraTrader{fields::ContraTrader_NULL};
		types::FLOAT m_ContraTradeQty{fields::NULL_QTY};
		types::UTCTIMESTAMP m_ContraTradeTime{fields::NULL_UTCTIMESTAMP};
		types::STRING<32> m_ContraLegRefID{fields::ContraLegRefID_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getContraBroker() const noexcept { return m_ContraBroker; }
		const types::STRING<32> &getContraTrader() const noexcept { return m_ContraTrader; }
		const types::FLOAT &getContraTradeQty() const noexcept { return m_ContraTradeQty; }
		const types::UTCTIMESTAMP &getContraTradeTime() const noexcept { return m_ContraTradeTime; }
		const types::STRING<32> &getContraLegRefID() const noexcept { return m_ContraLegRefID; }

		// mutators
		void setContraBroker(const types::STRING<32> &v) noexcept { m_ContraBroker = v; m_presence |= (1ULL << P_ContraBroker); }
		void setContraTrader(const types::STRING<32> &v) noexcept { m_ContraTrader = v; m_presence |= (1ULL << P_ContraTrader); }
		void setContraTradeQty(types::FLOAT v) noexcept { m_ContraTradeQty = v; m_presence |= (1ULL << P_ContraTradeQty); }
		void setContraTradeTime(types::UTCTIMESTAMP v) noexcept { m_ContraTradeTime = v; m_presence |= (1ULL << P_ContraTradeTime); }
		void setContraLegRefID(const types::STRING<32> &v) noexcept { m_ContraLegRefID = v; m_presence |= (1ULL << P_ContraLegRefID); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasContraBroker() const noexcept { return (m_presence & (1ULL << P_ContraBroker)) != 0; }
		bool hasContraTrader() const noexcept { return (m_presence & (1ULL << P_ContraTrader)) != 0; }
		bool hasContraTradeQty() const noexcept { return (m_presence & (1ULL << P_ContraTradeQty)) != 0; }
		bool hasContraTradeTime() const noexcept { return (m_presence & (1ULL << P_ContraTradeTime)) != 0; }
		bool hasContraLegRefID() const noexcept { return (m_presence & (1ULL << P_ContraLegRefID)) != 0; }
	};

private:
	static constexpr std::size_t P_NoContraBrokers              = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoContraBrokers> m_NoContraBrokersGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoContraBrokersGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoContraBrokers> &getRefNoContraBrokersGroup() noexcept { return m_NoContraBrokersGroup; }
	const std::vector<NoContraBrokers> &getRefNoContraBrokersGroup() const noexcept { return m_NoContraBrokersGroup; }
	std::size_t getNoContraBrokers() const noexcept { return m_NoContraBrokersGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoContraBrokersGroup.empty()) { return true; }
		return false;
	}
	bool hasNoContraBrokers() const noexcept { return !m_NoContraBrokersGroup.empty(); }
};

} // namespace fix44::components
