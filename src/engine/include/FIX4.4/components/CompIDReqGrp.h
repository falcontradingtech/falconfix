// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 4feb56845a759a65

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class CompIDReqGrp {
public:
	struct NoCompIDs {
		static constexpr std::size_t P_RefCompID                    = 0;
		static constexpr std::size_t P_RefSubID                     = 1;
		static constexpr std::size_t P_LocationID                   = 2;
		static constexpr std::size_t P_DeskID                       = 3;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 148;

		types::STRING<32> m_RefCompID{fields::RefCompID_NULL};
		types::STRING<32> m_RefSubID{fields::RefSubID_NULL};
		types::STRING<32> m_LocationID{fields::LocationID_NULL};
		types::STRING<32> m_DeskID{fields::DeskID_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getRefCompID() const noexcept { return m_RefCompID; }
		const types::STRING<32> &getRefSubID() const noexcept { return m_RefSubID; }
		const types::STRING<32> &getLocationID() const noexcept { return m_LocationID; }
		const types::STRING<32> &getDeskID() const noexcept { return m_DeskID; }

		// mutators
		void setRefCompID(const types::STRING<32> &v) noexcept { m_RefCompID = v; m_presence |= (1ULL << P_RefCompID); }
		void setRefSubID(const types::STRING<32> &v) noexcept { m_RefSubID = v; m_presence |= (1ULL << P_RefSubID); }
		void setLocationID(const types::STRING<32> &v) noexcept { m_LocationID = v; m_presence |= (1ULL << P_LocationID); }
		void setDeskID(const types::STRING<32> &v) noexcept { m_DeskID = v; m_presence |= (1ULL << P_DeskID); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasRefCompID() const noexcept { return (m_presence & (1ULL << P_RefCompID)) != 0; }
		bool hasRefSubID() const noexcept { return (m_presence & (1ULL << P_RefSubID)) != 0; }
		bool hasLocationID() const noexcept { return (m_presence & (1ULL << P_LocationID)) != 0; }
		bool hasDeskID() const noexcept { return (m_presence & (1ULL << P_DeskID)) != 0; }
	};

private:
	static constexpr std::size_t P_NoCompIDs                    = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoCompIDs> m_NoCompIDsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoCompIDsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoCompIDs> &getRefNoCompIDsGroup() noexcept { return m_NoCompIDsGroup; }
	const std::vector<NoCompIDs> &getRefNoCompIDsGroup() const noexcept { return m_NoCompIDsGroup; }
	std::size_t getNoCompIDs() const noexcept { return m_NoCompIDsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoCompIDsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoCompIDs() const noexcept { return !m_NoCompIDsGroup.empty(); }
};

} // namespace fix44::components
