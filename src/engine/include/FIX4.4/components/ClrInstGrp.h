// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 82d8d77071e46adc

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class ClrInstGrp {
public:
	struct NoClearingInstructions {
		static constexpr std::size_t P_ClearingInstruction          = 0;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 25;

		types::INT m_ClearingInstruction{fields::NULL_INT};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::INT &getClearingInstruction() const noexcept { return m_ClearingInstruction; }

		// mutators
		void setClearingInstruction(types::INT v) noexcept { m_ClearingInstruction = v; m_presence |= (1ULL << P_ClearingInstruction); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasClearingInstruction() const noexcept { return (m_presence & (1ULL << P_ClearingInstruction)) != 0; }
	};

private:
	static constexpr std::size_t P_NoClearingInstructions       = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoClearingInstructions> m_NoClearingInstructionsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoClearingInstructionsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoClearingInstructions> &getRefNoClearingInstructionsGroup() noexcept { return m_NoClearingInstructionsGroup; }
	const std::vector<NoClearingInstructions> &getRefNoClearingInstructionsGroup() const noexcept { return m_NoClearingInstructionsGroup; }
	std::size_t getNoClearingInstructions() const noexcept { return m_NoClearingInstructionsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoClearingInstructionsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoClearingInstructions() const noexcept { return !m_NoClearingInstructionsGroup.empty(); }
};

} // namespace fix44::components
