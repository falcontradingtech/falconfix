// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 682ac28ad969487a

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class TrdCollGrp {
public:
	struct NoTrades {
		static constexpr std::size_t P_TradeReportID                = 0;
		static constexpr std::size_t P_SecondaryTradeReportID       = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 74;

		types::STRING<32> m_TradeReportID{fields::TradeReportID_NULL};
		types::STRING<32> m_SecondaryTradeReportID{fields::SecondaryTradeReportID_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getTradeReportID() const noexcept { return m_TradeReportID; }
		const types::STRING<32> &getSecondaryTradeReportID() const noexcept { return m_SecondaryTradeReportID; }

		// mutators
		void setTradeReportID(const types::STRING<32> &v) noexcept { m_TradeReportID = v; m_presence |= (1ULL << P_TradeReportID); }
		void setSecondaryTradeReportID(const types::STRING<32> &v) noexcept { m_SecondaryTradeReportID = v; m_presence |= (1ULL << P_SecondaryTradeReportID); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasTradeReportID() const noexcept { return (m_presence & (1ULL << P_TradeReportID)) != 0; }
		bool hasSecondaryTradeReportID() const noexcept { return (m_presence & (1ULL << P_SecondaryTradeReportID)) != 0; }
	};

private:
	static constexpr std::size_t P_NoTrades                     = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoTrades> m_NoTradesGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoTradesGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoTrades> &getRefNoTradesGroup() noexcept { return m_NoTradesGroup; }
	const std::vector<NoTrades> &getRefNoTradesGroup() const noexcept { return m_NoTradesGroup; }
	std::size_t getNoTrades() const noexcept { return m_NoTradesGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoTradesGroup.empty()) { return true; }
		return false;
	}
	bool hasNoTrades() const noexcept { return !m_NoTradesGroup.empty(); }
};

} // namespace fix44::components
