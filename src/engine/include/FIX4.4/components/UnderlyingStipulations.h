// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 78c4075ac47158f4

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class UnderlyingStipulations {
public:
	struct NoUnderlyingStips {
		static constexpr std::size_t P_UnderlyingStipType           = 0;
		static constexpr std::size_t P_UnderlyingStipValue          = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 74;

		types::STRING<32> m_UnderlyingStipType{fields::UnderlyingStipType_NULL};
		types::STRING<32> m_UnderlyingStipValue{fields::UnderlyingStipValue_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getUnderlyingStipType() const noexcept { return m_UnderlyingStipType; }
		const types::STRING<32> &getUnderlyingStipValue() const noexcept { return m_UnderlyingStipValue; }

		// mutators
		void setUnderlyingStipType(const types::STRING<32> &v) noexcept { m_UnderlyingStipType = v; m_presence |= (1ULL << P_UnderlyingStipType); }
		void setUnderlyingStipValue(const types::STRING<32> &v) noexcept { m_UnderlyingStipValue = v; m_presence |= (1ULL << P_UnderlyingStipValue); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasUnderlyingStipType() const noexcept { return (m_presence & (1ULL << P_UnderlyingStipType)) != 0; }
		bool hasUnderlyingStipValue() const noexcept { return (m_presence & (1ULL << P_UnderlyingStipValue)) != 0; }
	};

private:
	static constexpr std::size_t P_NoUnderlyingStips            = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoUnderlyingStips> m_NoUnderlyingStipsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoUnderlyingStipsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoUnderlyingStips> &getRefNoUnderlyingStipsGroup() noexcept { return m_NoUnderlyingStipsGroup; }
	const std::vector<NoUnderlyingStips> &getRefNoUnderlyingStipsGroup() const noexcept { return m_NoUnderlyingStipsGroup; }
	std::size_t getNoUnderlyingStips() const noexcept { return m_NoUnderlyingStipsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoUnderlyingStipsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoUnderlyingStips() const noexcept { return !m_NoUnderlyingStipsGroup.empty(); }
};

} // namespace fix44::components
