// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 9477a7775ad025d8

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class ExecAllocGrp {
public:
	struct NoExecs {
		static constexpr std::size_t P_LastQty                      = 0;
		static constexpr std::size_t P_ExecID                       = 1;
		static constexpr std::size_t P_SecondaryExecID              = 2;
		static constexpr std::size_t P_LastPx                       = 3;
		static constexpr std::size_t P_LastParPx                    = 4;
		static constexpr std::size_t P_LastCapacity                 = 5;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 163;

		types::FLOAT m_LastQty{fields::NULL_QTY};
		types::STRING<32> m_ExecID{fields::ExecID_NULL};
		types::STRING<32> m_SecondaryExecID{fields::SecondaryExecID_NULL};
		types::FLOAT m_LastPx{fields::NULL_PRICE};
		types::FLOAT m_LastParPx{fields::NULL_PRICE};
		types::CHAR m_LastCapacity{fields::NULL_CHAR};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::FLOAT &getLastQty() const noexcept { return m_LastQty; }
		const types::STRING<32> &getExecID() const noexcept { return m_ExecID; }
		const types::STRING<32> &getSecondaryExecID() const noexcept { return m_SecondaryExecID; }
		const types::FLOAT &getLastPx() const noexcept { return m_LastPx; }
		const types::FLOAT &getLastParPx() const noexcept { return m_LastParPx; }
		const types::CHAR &getLastCapacity() const noexcept { return m_LastCapacity; }

		// mutators
		void setLastQty(types::FLOAT v) noexcept { m_LastQty = v; m_presence |= (1ULL << P_LastQty); }
		void setExecID(const types::STRING<32> &v) noexcept { m_ExecID = v; m_presence |= (1ULL << P_ExecID); }
		void setSecondaryExecID(const types::STRING<32> &v) noexcept { m_SecondaryExecID = v; m_presence |= (1ULL << P_SecondaryExecID); }
		void setLastPx(types::FLOAT v) noexcept { m_LastPx = v; m_presence |= (1ULL << P_LastPx); }
		void setLastParPx(types::FLOAT v) noexcept { m_LastParPx = v; m_presence |= (1ULL << P_LastParPx); }
		void setLastCapacity(types::CHAR v) noexcept { m_LastCapacity = v; m_presence |= (1ULL << P_LastCapacity); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasLastQty() const noexcept { return (m_presence & (1ULL << P_LastQty)) != 0; }
		bool hasExecID() const noexcept { return (m_presence & (1ULL << P_ExecID)) != 0; }
		bool hasSecondaryExecID() const noexcept { return (m_presence & (1ULL << P_SecondaryExecID)) != 0; }
		bool hasLastPx() const noexcept { return (m_presence & (1ULL << P_LastPx)) != 0; }
		bool hasLastParPx() const noexcept { return (m_presence & (1ULL << P_LastParPx)) != 0; }
		bool hasLastCapacity() const noexcept { return (m_presence & (1ULL << P_LastCapacity)) != 0; }
	};

private:
	static constexpr std::size_t P_NoExecs                      = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoExecs> m_NoExecsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoExecsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoExecs> &getRefNoExecsGroup() noexcept { return m_NoExecsGroup; }
	const std::vector<NoExecs> &getRefNoExecsGroup() const noexcept { return m_NoExecsGroup; }
	std::size_t getNoExecs() const noexcept { return m_NoExecsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoExecsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoExecs() const noexcept { return !m_NoExecsGroup.empty(); }
};

} // namespace fix44::components
