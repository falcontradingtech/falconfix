// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 7bfc836f669b6ab5

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

#include <FIX4.4/components/QuotEntryGrp.h>
#include <FIX4.4/components/UnderlyingInstrument.h>

namespace fix44::components {

class QuotSetGrp {
public:
	struct NoQuoteSets {
		static constexpr std::size_t P_QuoteSetID                   = 0;
		static constexpr std::size_t P_QuoteSetValidUntilTime       = 1;
		static constexpr std::size_t P_TotNoQuoteEntries            = 2;
		static constexpr std::size_t P_LastFragment                 = 3;

		static constexpr uint64_t REQUIRED =
			(1ULL << 0) |
			(1ULL << 2);
		static constexpr std::size_t MAX_ENCODE_SIZE = 90;

		types::STRING<32> m_QuoteSetID{fields::QuoteSetID_NULL};
		UnderlyingInstrument m_UnderlyingInstrument{};
		types::UTCTIMESTAMP m_QuoteSetValidUntilTime{fields::NULL_UTCTIMESTAMP};
		types::INT m_TotNoQuoteEntries{fields::NULL_INT};
		types::BOOLEAN m_LastFragment{fields::NULL_BOOLEAN};
		QuotEntryGrp m_QuotEntryGrp{};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		std::size_t compute_buffer_size() const noexcept {
			return MAX_ENCODE_SIZE
				+ m_UnderlyingInstrument.compute_buffer_size()
				+ m_QuotEntryGrp.compute_buffer_size();
		}

		// accessors
		UnderlyingInstrument &getRefUnderlyingInstrument() noexcept { return m_UnderlyingInstrument; }
		const UnderlyingInstrument &getRefUnderlyingInstrument() const noexcept { return m_UnderlyingInstrument; }
		QuotEntryGrp &getRefQuotEntryGrp() noexcept { return m_QuotEntryGrp; }
		const QuotEntryGrp &getRefQuotEntryGrp() const noexcept { return m_QuotEntryGrp; }
		const types::STRING<32> &getQuoteSetID() const noexcept { return m_QuoteSetID; }
		const types::UTCTIMESTAMP &getQuoteSetValidUntilTime() const noexcept { return m_QuoteSetValidUntilTime; }
		const types::INT &getTotNoQuoteEntries() const noexcept { return m_TotNoQuoteEntries; }
		const types::BOOLEAN &getLastFragment() const noexcept { return m_LastFragment; }

		// mutators
		void setQuoteSetID(const types::STRING<32> &v) noexcept { m_QuoteSetID = v; m_presence |= (1ULL << P_QuoteSetID); }
		void setQuoteSetValidUntilTime(types::UTCTIMESTAMP v) noexcept { m_QuoteSetValidUntilTime = v; m_presence |= (1ULL << P_QuoteSetValidUntilTime); }
		void setTotNoQuoteEntries(types::INT v) noexcept { m_TotNoQuoteEntries = v; m_presence |= (1ULL << P_TotNoQuoteEntries); }
		void setLastFragment(types::BOOLEAN v) noexcept { m_LastFragment = v; m_presence |= (1ULL << P_LastFragment); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			if (m_UnderlyingInstrument.hasAnySet()) { return true; }
			if (m_QuotEntryGrp.hasAnySet()) { return true; }
			return false;
		}
		bool hasQuoteSetID() const noexcept { return (m_presence & (1ULL << P_QuoteSetID)) != 0; }
		bool hasQuoteSetValidUntilTime() const noexcept { return (m_presence & (1ULL << P_QuoteSetValidUntilTime)) != 0; }
		bool hasTotNoQuoteEntries() const noexcept { return (m_presence & (1ULL << P_TotNoQuoteEntries)) != 0; }
		bool hasLastFragment() const noexcept { return (m_presence & (1ULL << P_LastFragment)) != 0; }
	};

private:
	static constexpr std::size_t P_NoQuoteSets                  = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoQuoteSets> m_NoQuoteSetsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoQuoteSetsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoQuoteSets> &getRefNoQuoteSetsGroup() noexcept { return m_NoQuoteSetsGroup; }
	const std::vector<NoQuoteSets> &getRefNoQuoteSetsGroup() const noexcept { return m_NoQuoteSetsGroup; }
	std::size_t getNoQuoteSets() const noexcept { return m_NoQuoteSetsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoQuoteSetsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoQuoteSets() const noexcept { return !m_NoQuoteSetsGroup.empty(); }
};

} // namespace fix44::components
