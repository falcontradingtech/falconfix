// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 908b8d96d5711be6

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class IOIQualGrp {
public:
	struct NoIOIQualifiers {
		static constexpr std::size_t P_IOIQualifier                 = 0;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 6;

		types::CHAR m_IOIQualifier{fields::NULL_CHAR};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::CHAR &getIOIQualifier() const noexcept { return m_IOIQualifier; }

		// mutators
		void setIOIQualifier(types::CHAR v) noexcept { m_IOIQualifier = v; m_presence |= (1ULL << P_IOIQualifier); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasIOIQualifier() const noexcept { return (m_presence & (1ULL << P_IOIQualifier)) != 0; }
	};

private:
	static constexpr std::size_t P_NoIOIQualifiers              = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoIOIQualifiers> m_NoIOIQualifiersGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoIOIQualifiersGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoIOIQualifiers> &getRefNoIOIQualifiersGroup() noexcept { return m_NoIOIQualifiersGroup; }
	const std::vector<NoIOIQualifiers> &getRefNoIOIQualifiersGroup() const noexcept { return m_NoIOIQualifiersGroup; }
	std::size_t getNoIOIQualifiers() const noexcept { return m_NoIOIQualifiersGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoIOIQualifiersGroup.empty()) { return true; }
		return false;
	}
	bool hasNoIOIQualifiers() const noexcept { return !m_NoIOIQualifiersGroup.empty(); }
};

} // namespace fix44::components
