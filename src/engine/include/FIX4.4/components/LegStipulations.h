// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 856f16bed59ba919

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class LegStipulations {
public:
	struct NoLegStipulations {
		static constexpr std::size_t P_LegStipulationType           = 0;
		static constexpr std::size_t P_LegStipulationValue          = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 74;

		types::STRING<32> m_LegStipulationType{fields::LegStipulationType_NULL};
		types::STRING<32> m_LegStipulationValue{fields::LegStipulationValue_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getLegStipulationType() const noexcept { return m_LegStipulationType; }
		const types::STRING<32> &getLegStipulationValue() const noexcept { return m_LegStipulationValue; }

		// mutators
		void setLegStipulationType(const types::STRING<32> &v) noexcept { m_LegStipulationType = v; m_presence |= (1ULL << P_LegStipulationType); }
		void setLegStipulationValue(const types::STRING<32> &v) noexcept { m_LegStipulationValue = v; m_presence |= (1ULL << P_LegStipulationValue); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasLegStipulationType() const noexcept { return (m_presence & (1ULL << P_LegStipulationType)) != 0; }
		bool hasLegStipulationValue() const noexcept { return (m_presence & (1ULL << P_LegStipulationValue)) != 0; }
	};

private:
	static constexpr std::size_t P_NoLegStipulations            = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoLegStipulations> m_NoLegStipulationsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoLegStipulationsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoLegStipulations> &getRefNoLegStipulationsGroup() noexcept { return m_NoLegStipulationsGroup; }
	const std::vector<NoLegStipulations> &getRefNoLegStipulationsGroup() const noexcept { return m_NoLegStipulationsGroup; }
	std::size_t getNoLegStipulations() const noexcept { return m_NoLegStipulationsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoLegStipulationsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoLegStipulations() const noexcept { return !m_NoLegStipulationsGroup.empty(); }
};

} // namespace fix44::components
