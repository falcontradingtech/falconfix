// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: e6954fc80f138ad1

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class CpctyConfGrp {
public:
	struct NoCapacities {
		static constexpr std::size_t P_OrderCapacity                = 0;
		static constexpr std::size_t P_OrderRestrictions            = 1;
		static constexpr std::size_t P_OrderCapacityQty             = 2;

		static constexpr uint64_t REQUIRED =
			(1ULL << 0) |
			(1ULL << 2);
		static constexpr std::size_t MAX_ENCODE_SIZE = 72;

		types::CHAR m_OrderCapacity{fields::NULL_CHAR};
		types::MULTIPLEVALUESTRING<32> m_OrderRestrictions{fields::OrderRestrictions_NULL};
		types::FLOAT m_OrderCapacityQty{fields::NULL_QTY};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::CHAR &getOrderCapacity() const noexcept { return m_OrderCapacity; }
		const types::MULTIPLEVALUESTRING<32> &getOrderRestrictions() const noexcept { return m_OrderRestrictions; }
		const types::FLOAT &getOrderCapacityQty() const noexcept { return m_OrderCapacityQty; }

		// mutators
		void setOrderCapacity(types::CHAR v) noexcept { m_OrderCapacity = v; m_presence |= (1ULL << P_OrderCapacity); }
		void setOrderRestrictions(const types::MULTIPLEVALUESTRING<32> &v) noexcept { m_OrderRestrictions = v; m_presence |= (1ULL << P_OrderRestrictions); }
		void setOrderCapacityQty(types::FLOAT v) noexcept { m_OrderCapacityQty = v; m_presence |= (1ULL << P_OrderCapacityQty); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasOrderCapacity() const noexcept { return (m_presence & (1ULL << P_OrderCapacity)) != 0; }
		bool hasOrderRestrictions() const noexcept { return (m_presence & (1ULL << P_OrderRestrictions)) != 0; }
		bool hasOrderCapacityQty() const noexcept { return (m_presence & (1ULL << P_OrderCapacityQty)) != 0; }
	};

private:
	static constexpr std::size_t P_NoCapacities                 = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoCapacities> m_NoCapacitiesGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoCapacitiesGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoCapacities> &getRefNoCapacitiesGroup() noexcept { return m_NoCapacitiesGroup; }
	const std::vector<NoCapacities> &getRefNoCapacitiesGroup() const noexcept { return m_NoCapacitiesGroup; }
	std::size_t getNoCapacities() const noexcept { return m_NoCapacitiesGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoCapacitiesGroup.empty()) { return true; }
		return false;
	}
	bool hasNoCapacities() const noexcept { return !m_NoCapacitiesGroup.empty(); }
};

} // namespace fix44::components
