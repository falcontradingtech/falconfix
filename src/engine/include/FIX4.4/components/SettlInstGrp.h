// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: ea1951a514228bbf

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

#include <FIX4.4/components/Parties.h>
#include <FIX4.4/components/SettlInstructionsData.h>

namespace fix44::components {

class SettlInstGrp {
public:
	struct NoSettlInst {
		static constexpr std::size_t P_SettlInstID                  = 0;
		static constexpr std::size_t P_SettlInstTransType           = 1;
		static constexpr std::size_t P_SettlInstRefID               = 2;
		static constexpr std::size_t P_Side                         = 3;
		static constexpr std::size_t P_Product                      = 4;
		static constexpr std::size_t P_SecurityType                 = 5;
		static constexpr std::size_t P_CFICode                      = 6;
		static constexpr std::size_t P_EffectiveTime                = 7;
		static constexpr std::size_t P_ExpireTime                   = 8;
		static constexpr std::size_t P_LastUpdateTime               = 9;
		static constexpr std::size_t P_PaymentMethod                = 10;
		static constexpr std::size_t P_PaymentRef                   = 11;
		static constexpr std::size_t P_CardHolderName               = 12;
		static constexpr std::size_t P_CardNumber                   = 13;
		static constexpr std::size_t P_CardStartDate                = 14;
		static constexpr std::size_t P_CardExpDate                  = 15;
		static constexpr std::size_t P_CardIssNum                   = 16;
		static constexpr std::size_t P_PaymentDate                  = 17;
		static constexpr std::size_t P_PaymentRemitterID            = 18;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 499;

		types::STRING<32> m_SettlInstID{fields::SettlInstID_NULL};
		types::CHAR m_SettlInstTransType{fields::NULL_CHAR};
		types::STRING<32> m_SettlInstRefID{fields::SettlInstRefID_NULL};
		Parties m_Parties{};
		types::CHAR m_Side{fields::NULL_CHAR};
		types::INT m_Product{fields::NULL_INT};
		types::STRING<32> m_SecurityType{fields::SecurityType_NULL};
		types::STRING<32> m_CFICode{fields::CFICode_NULL};
		types::UTCTIMESTAMP m_EffectiveTime{fields::NULL_UTCTIMESTAMP};
		types::UTCTIMESTAMP m_ExpireTime{fields::NULL_UTCTIMESTAMP};
		types::UTCTIMESTAMP m_LastUpdateTime{fields::NULL_UTCTIMESTAMP};
		SettlInstructionsData m_SettlInstructionsData{};
		types::INT m_PaymentMethod{fields::NULL_INT};
		types::STRING<32> m_PaymentRef{fields::PaymentRef_NULL};
		types::STRING<32> m_CardHolderName{fields::CardHolderName_NULL};
		types::STRING<32> m_CardNumber{fields::CardNumber_NULL};
		types::UTCDATEONLY m_CardStartDate{fields::NULL_LOCALMKTDATE};
		types::UTCDATEONLY m_CardExpDate{fields::NULL_LOCALMKTDATE};
		types::STRING<32> m_CardIssNum{fields::CardIssNum_NULL};
		types::UTCDATEONLY m_PaymentDate{fields::NULL_LOCALMKTDATE};
		types::STRING<32> m_PaymentRemitterID{fields::PaymentRemitterID_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		std::size_t compute_buffer_size() const noexcept {
			return MAX_ENCODE_SIZE
				+ m_Parties.compute_buffer_size()
				+ m_SettlInstructionsData.compute_buffer_size();
		}

		// accessors
		Parties &getRefParties() noexcept { return m_Parties; }
		const Parties &getRefParties() const noexcept { return m_Parties; }
		SettlInstructionsData &getRefSettlInstructionsData() noexcept { return m_SettlInstructionsData; }
		const SettlInstructionsData &getRefSettlInstructionsData() const noexcept { return m_SettlInstructionsData; }
		const types::STRING<32> &getSettlInstID() const noexcept { return m_SettlInstID; }
		const types::CHAR &getSettlInstTransType() const noexcept { return m_SettlInstTransType; }
		const types::STRING<32> &getSettlInstRefID() const noexcept { return m_SettlInstRefID; }
		const types::CHAR &getSide() const noexcept { return m_Side; }
		const types::INT &getProduct() const noexcept { return m_Product; }
		const types::STRING<32> &getSecurityType() const noexcept { return m_SecurityType; }
		const types::STRING<32> &getCFICode() const noexcept { return m_CFICode; }
		const types::UTCTIMESTAMP &getEffectiveTime() const noexcept { return m_EffectiveTime; }
		const types::UTCTIMESTAMP &getExpireTime() const noexcept { return m_ExpireTime; }
		const types::UTCTIMESTAMP &getLastUpdateTime() const noexcept { return m_LastUpdateTime; }
		const types::INT &getPaymentMethod() const noexcept { return m_PaymentMethod; }
		const types::STRING<32> &getPaymentRef() const noexcept { return m_PaymentRef; }
		const types::STRING<32> &getCardHolderName() const noexcept { return m_CardHolderName; }
		const types::STRING<32> &getCardNumber() const noexcept { return m_CardNumber; }
		const types::UTCDATEONLY &getCardStartDate() const noexcept { return m_CardStartDate; }
		const types::UTCDATEONLY &getCardExpDate() const noexcept { return m_CardExpDate; }
		const types::STRING<32> &getCardIssNum() const noexcept { return m_CardIssNum; }
		const types::UTCDATEONLY &getPaymentDate() const noexcept { return m_PaymentDate; }
		const types::STRING<32> &getPaymentRemitterID() const noexcept { return m_PaymentRemitterID; }

		// mutators
		void setSettlInstID(const types::STRING<32> &v) noexcept { m_SettlInstID = v; m_presence |= (1ULL << P_SettlInstID); }
		void setSettlInstTransType(types::CHAR v) noexcept { m_SettlInstTransType = v; m_presence |= (1ULL << P_SettlInstTransType); }
		void setSettlInstRefID(const types::STRING<32> &v) noexcept { m_SettlInstRefID = v; m_presence |= (1ULL << P_SettlInstRefID); }
		void setSide(types::CHAR v) noexcept { m_Side = v; m_presence |= (1ULL << P_Side); }
		void setProduct(types::INT v) noexcept { m_Product = v; m_presence |= (1ULL << P_Product); }
		void setSecurityType(const types::STRING<32> &v) noexcept { m_SecurityType = v; m_presence |= (1ULL << P_SecurityType); }
		void setCFICode(const types::STRING<32> &v) noexcept { m_CFICode = v; m_presence |= (1ULL << P_CFICode); }
		void setEffectiveTime(types::UTCTIMESTAMP v) noexcept { m_EffectiveTime = v; m_presence |= (1ULL << P_EffectiveTime); }
		void setExpireTime(types::UTCTIMESTAMP v) noexcept { m_ExpireTime = v; m_presence |= (1ULL << P_ExpireTime); }
		void setLastUpdateTime(types::UTCTIMESTAMP v) noexcept { m_LastUpdateTime = v; m_presence |= (1ULL << P_LastUpdateTime); }
		void setPaymentMethod(types::INT v) noexcept { m_PaymentMethod = v; m_presence |= (1ULL << P_PaymentMethod); }
		void setPaymentRef(const types::STRING<32> &v) noexcept { m_PaymentRef = v; m_presence |= (1ULL << P_PaymentRef); }
		void setCardHolderName(const types::STRING<32> &v) noexcept { m_CardHolderName = v; m_presence |= (1ULL << P_CardHolderName); }
		void setCardNumber(const types::STRING<32> &v) noexcept { m_CardNumber = v; m_presence |= (1ULL << P_CardNumber); }
		void setCardStartDate(types::UTCDATEONLY v) noexcept { m_CardStartDate = v; m_presence |= (1ULL << P_CardStartDate); }
		void setCardExpDate(types::UTCDATEONLY v) noexcept { m_CardExpDate = v; m_presence |= (1ULL << P_CardExpDate); }
		void setCardIssNum(const types::STRING<32> &v) noexcept { m_CardIssNum = v; m_presence |= (1ULL << P_CardIssNum); }
		void setPaymentDate(types::UTCDATEONLY v) noexcept { m_PaymentDate = v; m_presence |= (1ULL << P_PaymentDate); }
		void setPaymentRemitterID(const types::STRING<32> &v) noexcept { m_PaymentRemitterID = v; m_presence |= (1ULL << P_PaymentRemitterID); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			if (m_Parties.hasAnySet()) { return true; }
			if (m_SettlInstructionsData.hasAnySet()) { return true; }
			return false;
		}
		bool hasSettlInstID() const noexcept { return (m_presence & (1ULL << P_SettlInstID)) != 0; }
		bool hasSettlInstTransType() const noexcept { return (m_presence & (1ULL << P_SettlInstTransType)) != 0; }
		bool hasSettlInstRefID() const noexcept { return (m_presence & (1ULL << P_SettlInstRefID)) != 0; }
		bool hasSide() const noexcept { return (m_presence & (1ULL << P_Side)) != 0; }
		bool hasProduct() const noexcept { return (m_presence & (1ULL << P_Product)) != 0; }
		bool hasSecurityType() const noexcept { return (m_presence & (1ULL << P_SecurityType)) != 0; }
		bool hasCFICode() const noexcept { return (m_presence & (1ULL << P_CFICode)) != 0; }
		bool hasEffectiveTime() const noexcept { return (m_presence & (1ULL << P_EffectiveTime)) != 0; }
		bool hasExpireTime() const noexcept { return (m_presence & (1ULL << P_ExpireTime)) != 0; }
		bool hasLastUpdateTime() const noexcept { return (m_presence & (1ULL << P_LastUpdateTime)) != 0; }
		bool hasPaymentMethod() const noexcept { return (m_presence & (1ULL << P_PaymentMethod)) != 0; }
		bool hasPaymentRef() const noexcept { return (m_presence & (1ULL << P_PaymentRef)) != 0; }
		bool hasCardHolderName() const noexcept { return (m_presence & (1ULL << P_CardHolderName)) != 0; }
		bool hasCardNumber() const noexcept { return (m_presence & (1ULL << P_CardNumber)) != 0; }
		bool hasCardStartDate() const noexcept { return (m_presence & (1ULL << P_CardStartDate)) != 0; }
		bool hasCardExpDate() const noexcept { return (m_presence & (1ULL << P_CardExpDate)) != 0; }
		bool hasCardIssNum() const noexcept { return (m_presence & (1ULL << P_CardIssNum)) != 0; }
		bool hasPaymentDate() const noexcept { return (m_presence & (1ULL << P_PaymentDate)) != 0; }
		bool hasPaymentRemitterID() const noexcept { return (m_presence & (1ULL << P_PaymentRemitterID)) != 0; }
	};

private:
	static constexpr std::size_t P_NoSettlInst                  = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoSettlInst> m_NoSettlInstGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoSettlInstGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoSettlInst> &getRefNoSettlInstGroup() noexcept { return m_NoSettlInstGroup; }
	const std::vector<NoSettlInst> &getRefNoSettlInstGroup() const noexcept { return m_NoSettlInstGroup; }
	std::size_t getNoSettlInst() const noexcept { return m_NoSettlInstGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoSettlInstGroup.empty()) { return true; }
		return false;
	}
	bool hasNoSettlInst() const noexcept { return !m_NoSettlInstGroup.empty(); }
};

} // namespace fix44::components
