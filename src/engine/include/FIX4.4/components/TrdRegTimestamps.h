// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 0164acd109702ab7

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class TrdRegTimestamps {
public:
	struct NoTrdRegTimestamps {
		static constexpr std::size_t P_TrdRegTimestamp              = 0;
		static constexpr std::size_t P_TrdRegTimestampType          = 1;
		static constexpr std::size_t P_TrdRegTimestampOrigin        = 2;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 84;

		types::UTCTIMESTAMP m_TrdRegTimestamp{fields::NULL_UTCTIMESTAMP};
		types::INT m_TrdRegTimestampType{fields::NULL_INT};
		types::STRING<32> m_TrdRegTimestampOrigin{fields::TrdRegTimestampOrigin_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::UTCTIMESTAMP &getTrdRegTimestamp() const noexcept { return m_TrdRegTimestamp; }
		const types::INT &getTrdRegTimestampType() const noexcept { return m_TrdRegTimestampType; }
		const types::STRING<32> &getTrdRegTimestampOrigin() const noexcept { return m_TrdRegTimestampOrigin; }

		// mutators
		void setTrdRegTimestamp(types::UTCTIMESTAMP v) noexcept { m_TrdRegTimestamp = v; m_presence |= (1ULL << P_TrdRegTimestamp); }
		void setTrdRegTimestampType(types::INT v) noexcept { m_TrdRegTimestampType = v; m_presence |= (1ULL << P_TrdRegTimestampType); }
		void setTrdRegTimestampOrigin(const types::STRING<32> &v) noexcept { m_TrdRegTimestampOrigin = v; m_presence |= (1ULL << P_TrdRegTimestampOrigin); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasTrdRegTimestamp() const noexcept { return (m_presence & (1ULL << P_TrdRegTimestamp)) != 0; }
		bool hasTrdRegTimestampType() const noexcept { return (m_presence & (1ULL << P_TrdRegTimestampType)) != 0; }
		bool hasTrdRegTimestampOrigin() const noexcept { return (m_presence & (1ULL << P_TrdRegTimestampOrigin)) != 0; }
	};

private:
	static constexpr std::size_t P_NoTrdRegTimestamps           = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoTrdRegTimestamps> m_NoTrdRegTimestampsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoTrdRegTimestampsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoTrdRegTimestamps> &getRefNoTrdRegTimestampsGroup() noexcept { return m_NoTrdRegTimestampsGroup; }
	const std::vector<NoTrdRegTimestamps> &getRefNoTrdRegTimestampsGroup() const noexcept { return m_NoTrdRegTimestampsGroup; }
	std::size_t getNoTrdRegTimestamps() const noexcept { return m_NoTrdRegTimestampsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoTrdRegTimestampsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoTrdRegTimestamps() const noexcept { return !m_NoTrdRegTimestampsGroup.empty(); }
};

} // namespace fix44::components
