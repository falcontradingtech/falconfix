// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 3c3f28e90ecc7b98

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

#include <FIX4.4/components/NstdPtys3SubGrp.h>

namespace fix44::components {

class NestedParties3 {
public:
	struct NoNested3PartyIDs {
		static constexpr std::size_t P_Nested3PartyID               = 0;
		static constexpr std::size_t P_Nested3PartyIDSource         = 1;
		static constexpr std::size_t P_Nested3PartyRole             = 2;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 68;

		types::STRING<32> m_Nested3PartyID{fields::Nested3PartyID_NULL};
		types::CHAR m_Nested3PartyIDSource{fields::NULL_CHAR};
		types::INT m_Nested3PartyRole{fields::NULL_INT};
		NstdPtys3SubGrp m_NstdPtys3SubGrp{};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		std::size_t compute_buffer_size() const noexcept {
			return MAX_ENCODE_SIZE
				+ m_NstdPtys3SubGrp.compute_buffer_size();
		}

		// accessors
		NstdPtys3SubGrp &getRefNstdPtys3SubGrp() noexcept { return m_NstdPtys3SubGrp; }
		const NstdPtys3SubGrp &getRefNstdPtys3SubGrp() const noexcept { return m_NstdPtys3SubGrp; }
		const types::STRING<32> &getNested3PartyID() const noexcept { return m_Nested3PartyID; }
		const types::CHAR &getNested3PartyIDSource() const noexcept { return m_Nested3PartyIDSource; }
		const types::INT &getNested3PartyRole() const noexcept { return m_Nested3PartyRole; }

		// mutators
		void setNested3PartyID(const types::STRING<32> &v) noexcept { m_Nested3PartyID = v; m_presence |= (1ULL << P_Nested3PartyID); }
		void setNested3PartyIDSource(types::CHAR v) noexcept { m_Nested3PartyIDSource = v; m_presence |= (1ULL << P_Nested3PartyIDSource); }
		void setNested3PartyRole(types::INT v) noexcept { m_Nested3PartyRole = v; m_presence |= (1ULL << P_Nested3PartyRole); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			if (m_NstdPtys3SubGrp.hasAnySet()) { return true; }
			return false;
		}
		bool hasNested3PartyID() const noexcept { return (m_presence & (1ULL << P_Nested3PartyID)) != 0; }
		bool hasNested3PartyIDSource() const noexcept { return (m_presence & (1ULL << P_Nested3PartyIDSource)) != 0; }
		bool hasNested3PartyRole() const noexcept { return (m_presence & (1ULL << P_Nested3PartyRole)) != 0; }
	};

private:
	static constexpr std::size_t P_NoNested3PartyIDs            = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoNested3PartyIDs> m_NoNested3PartyIDsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoNested3PartyIDsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoNested3PartyIDs> &getRefNoNested3PartyIDsGroup() noexcept { return m_NoNested3PartyIDsGroup; }
	const std::vector<NoNested3PartyIDs> &getRefNoNested3PartyIDsGroup() const noexcept { return m_NoNested3PartyIDsGroup; }
	std::size_t getNoNested3PartyIDs() const noexcept { return m_NoNested3PartyIDsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoNested3PartyIDsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoNested3PartyIDs() const noexcept { return !m_NoNested3PartyIDsGroup.empty(); }
};

} // namespace fix44::components
