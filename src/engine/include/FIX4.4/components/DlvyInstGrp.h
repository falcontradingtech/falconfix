// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 1004e5d90fb9e7e9

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

#include <FIX4.4/components/SettlParties.h>

namespace fix44::components {

class DlvyInstGrp {
public:
	struct NoDlvyInst {
		static constexpr std::size_t P_SettlInstSource              = 0;
		static constexpr std::size_t P_DlvyInstType                 = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 12;

		types::CHAR m_SettlInstSource{fields::NULL_CHAR};
		types::CHAR m_DlvyInstType{fields::NULL_CHAR};
		SettlParties m_SettlParties{};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		std::size_t compute_buffer_size() const noexcept {
			return MAX_ENCODE_SIZE
				+ m_SettlParties.compute_buffer_size();
		}

		// accessors
		SettlParties &getRefSettlParties() noexcept { return m_SettlParties; }
		const SettlParties &getRefSettlParties() const noexcept { return m_SettlParties; }
		const types::CHAR &getSettlInstSource() const noexcept { return m_SettlInstSource; }
		const types::CHAR &getDlvyInstType() const noexcept { return m_DlvyInstType; }

		// mutators
		void setSettlInstSource(types::CHAR v) noexcept { m_SettlInstSource = v; m_presence |= (1ULL << P_SettlInstSource); }
		void setDlvyInstType(types::CHAR v) noexcept { m_DlvyInstType = v; m_presence |= (1ULL << P_DlvyInstType); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			if (m_SettlParties.hasAnySet()) { return true; }
			return false;
		}
		bool hasSettlInstSource() const noexcept { return (m_presence & (1ULL << P_SettlInstSource)) != 0; }
		bool hasDlvyInstType() const noexcept { return (m_presence & (1ULL << P_DlvyInstType)) != 0; }
	};

private:
	static constexpr std::size_t P_NoDlvyInst                   = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 24;

	std::vector<NoDlvyInst> m_NoDlvyInstGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoDlvyInstGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoDlvyInst> &getRefNoDlvyInstGroup() noexcept { return m_NoDlvyInstGroup; }
	const std::vector<NoDlvyInst> &getRefNoDlvyInstGroup() const noexcept { return m_NoDlvyInstGroup; }
	std::size_t getNoDlvyInst() const noexcept { return m_NoDlvyInstGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoDlvyInstGroup.empty()) { return true; }
		return false;
	}
	bool hasNoDlvyInst() const noexcept { return !m_NoDlvyInstGroup.empty(); }
};

} // namespace fix44::components
