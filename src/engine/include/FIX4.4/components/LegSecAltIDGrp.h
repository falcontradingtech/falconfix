// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: afaa1cad0bda420d

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class LegSecAltIDGrp {
public:
	struct NoLegSecurityAltID {
		static constexpr std::size_t P_LegSecurityAltID             = 0;
		static constexpr std::size_t P_LegSecurityAltIDSource       = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 74;

		types::STRING<32> m_LegSecurityAltID{fields::LegSecurityAltID_NULL};
		types::STRING<32> m_LegSecurityAltIDSource{fields::LegSecurityAltIDSource_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getLegSecurityAltID() const noexcept { return m_LegSecurityAltID; }
		const types::STRING<32> &getLegSecurityAltIDSource() const noexcept { return m_LegSecurityAltIDSource; }

		// mutators
		void setLegSecurityAltID(const types::STRING<32> &v) noexcept { m_LegSecurityAltID = v; m_presence |= (1ULL << P_LegSecurityAltID); }
		void setLegSecurityAltIDSource(const types::STRING<32> &v) noexcept { m_LegSecurityAltIDSource = v; m_presence |= (1ULL << P_LegSecurityAltIDSource); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasLegSecurityAltID() const noexcept { return (m_presence & (1ULL << P_LegSecurityAltID)) != 0; }
		bool hasLegSecurityAltIDSource() const noexcept { return (m_presence & (1ULL << P_LegSecurityAltIDSource)) != 0; }
	};

private:
	static constexpr std::size_t P_NoLegSecurityAltID           = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoLegSecurityAltID> m_NoLegSecurityAltIDGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoLegSecurityAltIDGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoLegSecurityAltID> &getRefNoLegSecurityAltIDGroup() noexcept { return m_NoLegSecurityAltIDGroup; }
	const std::vector<NoLegSecurityAltID> &getRefNoLegSecurityAltIDGroup() const noexcept { return m_NoLegSecurityAltIDGroup; }
	std::size_t getNoLegSecurityAltID() const noexcept { return m_NoLegSecurityAltIDGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoLegSecurityAltIDGroup.empty()) { return true; }
		return false;
	}
	bool hasNoLegSecurityAltID() const noexcept { return !m_NoLegSecurityAltIDGroup.empty(); }
};

} // namespace fix44::components
