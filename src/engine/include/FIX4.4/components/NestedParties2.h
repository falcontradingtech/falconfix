// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 5c3e317170ba05d6

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

#include <FIX4.4/components/NstdPtys2SubGrp.h>

namespace fix44::components {

class NestedParties2 {
public:
	struct NoNested2PartyIDs {
		static constexpr std::size_t P_Nested2PartyID               = 0;
		static constexpr std::size_t P_Nested2PartyIDSource         = 1;
		static constexpr std::size_t P_Nested2PartyRole             = 2;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 68;

		types::STRING<32> m_Nested2PartyID{fields::Nested2PartyID_NULL};
		types::CHAR m_Nested2PartyIDSource{fields::NULL_CHAR};
		types::INT m_Nested2PartyRole{fields::NULL_INT};
		NstdPtys2SubGrp m_NstdPtys2SubGrp{};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		std::size_t compute_buffer_size() const noexcept {
			return MAX_ENCODE_SIZE
				+ m_NstdPtys2SubGrp.compute_buffer_size();
		}

		// accessors
		NstdPtys2SubGrp &getRefNstdPtys2SubGrp() noexcept { return m_NstdPtys2SubGrp; }
		const NstdPtys2SubGrp &getRefNstdPtys2SubGrp() const noexcept { return m_NstdPtys2SubGrp; }
		const types::STRING<32> &getNested2PartyID() const noexcept { return m_Nested2PartyID; }
		const types::CHAR &getNested2PartyIDSource() const noexcept { return m_Nested2PartyIDSource; }
		const types::INT &getNested2PartyRole() const noexcept { return m_Nested2PartyRole; }

		// mutators
		void setNested2PartyID(const types::STRING<32> &v) noexcept { m_Nested2PartyID = v; m_presence |= (1ULL << P_Nested2PartyID); }
		void setNested2PartyIDSource(types::CHAR v) noexcept { m_Nested2PartyIDSource = v; m_presence |= (1ULL << P_Nested2PartyIDSource); }
		void setNested2PartyRole(types::INT v) noexcept { m_Nested2PartyRole = v; m_presence |= (1ULL << P_Nested2PartyRole); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			if (m_NstdPtys2SubGrp.hasAnySet()) { return true; }
			return false;
		}
		bool hasNested2PartyID() const noexcept { return (m_presence & (1ULL << P_Nested2PartyID)) != 0; }
		bool hasNested2PartyIDSource() const noexcept { return (m_presence & (1ULL << P_Nested2PartyIDSource)) != 0; }
		bool hasNested2PartyRole() const noexcept { return (m_presence & (1ULL << P_Nested2PartyRole)) != 0; }
	};

private:
	static constexpr std::size_t P_NoNested2PartyIDs            = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoNested2PartyIDs> m_NoNested2PartyIDsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoNested2PartyIDsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoNested2PartyIDs> &getRefNoNested2PartyIDsGroup() noexcept { return m_NoNested2PartyIDsGroup; }
	const std::vector<NoNested2PartyIDs> &getRefNoNested2PartyIDsGroup() const noexcept { return m_NoNested2PartyIDsGroup; }
	std::size_t getNoNested2PartyIDs() const noexcept { return m_NoNested2PartyIDsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoNested2PartyIDsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoNested2PartyIDs() const noexcept { return !m_NoNested2PartyIDsGroup.empty(); }
};

} // namespace fix44::components
