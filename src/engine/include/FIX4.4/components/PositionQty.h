// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 34b2ef0d095b3b20

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

#include <FIX4.4/components/NestedParties.h>

namespace fix44::components {

class PositionQty {
public:
	struct NoPositions {
		static constexpr std::size_t P_PosType                      = 0;
		static constexpr std::size_t P_LongQty                      = 1;
		static constexpr std::size_t P_ShortQty                     = 2;
		static constexpr std::size_t P_PosQtyStatus                 = 3;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 120;

		types::STRING<32> m_PosType{fields::PosType_NULL};
		types::FLOAT m_LongQty{fields::NULL_QTY};
		types::FLOAT m_ShortQty{fields::NULL_QTY};
		types::INT m_PosQtyStatus{fields::NULL_INT};
		NestedParties m_NestedParties{};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		std::size_t compute_buffer_size() const noexcept {
			return MAX_ENCODE_SIZE
				+ m_NestedParties.compute_buffer_size();
		}

		// accessors
		NestedParties &getRefNestedParties() noexcept { return m_NestedParties; }
		const NestedParties &getRefNestedParties() const noexcept { return m_NestedParties; }
		const types::STRING<32> &getPosType() const noexcept { return m_PosType; }
		const types::FLOAT &getLongQty() const noexcept { return m_LongQty; }
		const types::FLOAT &getShortQty() const noexcept { return m_ShortQty; }
		const types::INT &getPosQtyStatus() const noexcept { return m_PosQtyStatus; }

		// mutators
		void setPosType(const types::STRING<32> &v) noexcept { m_PosType = v; m_presence |= (1ULL << P_PosType); }
		void setLongQty(types::FLOAT v) noexcept { m_LongQty = v; m_presence |= (1ULL << P_LongQty); }
		void setShortQty(types::FLOAT v) noexcept { m_ShortQty = v; m_presence |= (1ULL << P_ShortQty); }
		void setPosQtyStatus(types::INT v) noexcept { m_PosQtyStatus = v; m_presence |= (1ULL << P_PosQtyStatus); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			if (m_NestedParties.hasAnySet()) { return true; }
			return false;
		}
		bool hasPosType() const noexcept { return (m_presence & (1ULL << P_PosType)) != 0; }
		bool hasLongQty() const noexcept { return (m_presence & (1ULL << P_LongQty)) != 0; }
		bool hasShortQty() const noexcept { return (m_presence & (1ULL << P_ShortQty)) != 0; }
		bool hasPosQtyStatus() const noexcept { return (m_presence & (1ULL << P_PosQtyStatus)) != 0; }
	};

private:
	static constexpr std::size_t P_NoPositions                  = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoPositions> m_NoPositionsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoPositionsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoPositions> &getRefNoPositionsGroup() noexcept { return m_NoPositionsGroup; }
	const std::vector<NoPositions> &getRefNoPositionsGroup() const noexcept { return m_NoPositionsGroup; }
	std::size_t getNoPositions() const noexcept { return m_NoPositionsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoPositionsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoPositions() const noexcept { return !m_NoPositionsGroup.empty(); }
};

} // namespace fix44::components
