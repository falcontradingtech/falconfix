// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 45dc54c54d42367d

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class PtysSubGrp {
public:
	struct NoPartySubIDs {
		static constexpr std::size_t P_PartySubID                   = 0;
		static constexpr std::size_t P_PartySubIDType               = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 62;

		types::STRING<32> m_PartySubID{fields::PartySubID_NULL};
		types::INT m_PartySubIDType{fields::NULL_INT};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getPartySubID() const noexcept { return m_PartySubID; }
		const types::INT &getPartySubIDType() const noexcept { return m_PartySubIDType; }

		// mutators
		void setPartySubID(const types::STRING<32> &v) noexcept { m_PartySubID = v; m_presence |= (1ULL << P_PartySubID); }
		void setPartySubIDType(types::INT v) noexcept { m_PartySubIDType = v; m_presence |= (1ULL << P_PartySubIDType); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasPartySubID() const noexcept { return (m_presence & (1ULL << P_PartySubID)) != 0; }
		bool hasPartySubIDType() const noexcept { return (m_presence & (1ULL << P_PartySubIDType)) != 0; }
	};

private:
	static constexpr std::size_t P_NoPartySubIDs                = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoPartySubIDs> m_NoPartySubIDsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoPartySubIDsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoPartySubIDs> &getRefNoPartySubIDsGroup() noexcept { return m_NoPartySubIDsGroup; }
	const std::vector<NoPartySubIDs> &getRefNoPartySubIDsGroup() const noexcept { return m_NoPartySubIDsGroup; }
	std::size_t getNoPartySubIDs() const noexcept { return m_NoPartySubIDsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoPartySubIDsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoPartySubIDs() const noexcept { return !m_NoPartySubIDsGroup.empty(); }
};

} // namespace fix44::components
