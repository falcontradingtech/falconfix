// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 6344ebb191ea7075

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class NstdPtysSubGrp {
public:
	struct NoNestedPartySubIDs {
		static constexpr std::size_t P_NestedPartySubID             = 0;
		static constexpr std::size_t P_NestedPartySubIDType         = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 62;

		types::STRING<32> m_NestedPartySubID{fields::NestedPartySubID_NULL};
		types::INT m_NestedPartySubIDType{fields::NULL_INT};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getNestedPartySubID() const noexcept { return m_NestedPartySubID; }
		const types::INT &getNestedPartySubIDType() const noexcept { return m_NestedPartySubIDType; }

		// mutators
		void setNestedPartySubID(const types::STRING<32> &v) noexcept { m_NestedPartySubID = v; m_presence |= (1ULL << P_NestedPartySubID); }
		void setNestedPartySubIDType(types::INT v) noexcept { m_NestedPartySubIDType = v; m_presence |= (1ULL << P_NestedPartySubIDType); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasNestedPartySubID() const noexcept { return (m_presence & (1ULL << P_NestedPartySubID)) != 0; }
		bool hasNestedPartySubIDType() const noexcept { return (m_presence & (1ULL << P_NestedPartySubIDType)) != 0; }
	};

private:
	static constexpr std::size_t P_NoNestedPartySubIDs          = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoNestedPartySubIDs> m_NoNestedPartySubIDsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoNestedPartySubIDsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoNestedPartySubIDs> &getRefNoNestedPartySubIDsGroup() noexcept { return m_NoNestedPartySubIDsGroup; }
	const std::vector<NoNestedPartySubIDs> &getRefNoNestedPartySubIDsGroup() const noexcept { return m_NoNestedPartySubIDsGroup; }
	std::size_t getNoNestedPartySubIDs() const noexcept { return m_NoNestedPartySubIDsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoNestedPartySubIDsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoNestedPartySubIDs() const noexcept { return !m_NoNestedPartySubIDsGroup.empty(); }
};

} // namespace fix44::components
