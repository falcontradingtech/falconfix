// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 8726013855b79015

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class AttrbGrp {
public:
	struct NoInstrAttrib {
		static constexpr std::size_t P_InstrAttribType              = 0;
		static constexpr std::size_t P_InstrAttribValue             = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 62;

		types::INT m_InstrAttribType{fields::NULL_INT};
		types::STRING<32> m_InstrAttribValue{fields::InstrAttribValue_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::INT &getInstrAttribType() const noexcept { return m_InstrAttribType; }
		const types::STRING<32> &getInstrAttribValue() const noexcept { return m_InstrAttribValue; }

		// mutators
		void setInstrAttribType(types::INT v) noexcept { m_InstrAttribType = v; m_presence |= (1ULL << P_InstrAttribType); }
		void setInstrAttribValue(const types::STRING<32> &v) noexcept { m_InstrAttribValue = v; m_presence |= (1ULL << P_InstrAttribValue); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasInstrAttribType() const noexcept { return (m_presence & (1ULL << P_InstrAttribType)) != 0; }
		bool hasInstrAttribValue() const noexcept { return (m_presence & (1ULL << P_InstrAttribValue)) != 0; }
	};

private:
	static constexpr std::size_t P_NoInstrAttrib                = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoInstrAttrib> m_NoInstrAttribGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoInstrAttribGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoInstrAttrib> &getRefNoInstrAttribGroup() noexcept { return m_NoInstrAttribGroup; }
	const std::vector<NoInstrAttrib> &getRefNoInstrAttribGroup() const noexcept { return m_NoInstrAttribGroup; }
	std::size_t getNoInstrAttrib() const noexcept { return m_NoInstrAttribGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoInstrAttribGroup.empty()) { return true; }
		return false;
	}
	bool hasNoInstrAttrib() const noexcept { return !m_NoInstrAttribGroup.empty(); }
};

} // namespace fix44::components
