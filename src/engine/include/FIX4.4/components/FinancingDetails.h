// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 99cb7408603c909c

#pragma once

#include <array>
#include <cstdint>
#include <string_view>

#include <utils/error_codes.h>
#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class FinancingDetails {
	static constexpr std::size_t P_AgreementDesc                = 0;
	static constexpr std::size_t P_AgreementID                  = 1;
	static constexpr std::size_t P_AgreementDate                = 2;
	static constexpr std::size_t P_AgreementCurrency            = 3;
	static constexpr std::size_t P_TerminationType              = 4;
	static constexpr std::size_t P_StartDate                    = 5;
	static constexpr std::size_t P_EndDate                      = 6;
	static constexpr std::size_t P_DeliveryType                 = 7;
	static constexpr std::size_t P_MarginRatio                  = 8;

	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 229;

	types::STRING<32> m_AgreementDesc{fields::AgreementDesc_NULL};
	types::STRING<32> m_AgreementID{fields::AgreementID_NULL};
	types::UTCDATEONLY m_AgreementDate{fields::NULL_LOCALMKTDATE};
	types::CURRENCY<32> m_AgreementCurrency{fields::AgreementCurrency_NULL};
	types::INT m_TerminationType{fields::NULL_INT};
	types::UTCDATEONLY m_StartDate{fields::NULL_LOCALMKTDATE};
	types::UTCDATEONLY m_EndDate{fields::NULL_LOCALMKTDATE};
	types::INT m_DeliveryType{fields::NULL_INT};
	types::FLOAT m_MarginRatio{fields::NULL_PERCENTAGE};

	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

	// accessors
	const types::STRING<32> &getAgreementDesc() const noexcept { return m_AgreementDesc; }
	const types::STRING<32> &getAgreementID() const noexcept { return m_AgreementID; }
	const types::UTCDATEONLY &getAgreementDate() const noexcept { return m_AgreementDate; }
	const types::CURRENCY<32> &getAgreementCurrency() const noexcept { return m_AgreementCurrency; }
	const types::INT &getTerminationType() const noexcept { return m_TerminationType; }
	const types::UTCDATEONLY &getStartDate() const noexcept { return m_StartDate; }
	const types::UTCDATEONLY &getEndDate() const noexcept { return m_EndDate; }
	const types::INT &getDeliveryType() const noexcept { return m_DeliveryType; }
	const types::FLOAT &getMarginRatio() const noexcept { return m_MarginRatio; }

	// mutators
	void setAgreementDesc(const types::STRING<32> &v) noexcept { m_AgreementDesc = v; m_presence |= (1ULL << P_AgreementDesc); }
	void setAgreementID(const types::STRING<32> &v) noexcept { m_AgreementID = v; m_presence |= (1ULL << P_AgreementID); }
	void setAgreementDate(types::UTCDATEONLY v) noexcept { m_AgreementDate = v; m_presence |= (1ULL << P_AgreementDate); }
	void setAgreementCurrency(const types::CURRENCY<32> &v) noexcept { m_AgreementCurrency = v; m_presence |= (1ULL << P_AgreementCurrency); }
	void setTerminationType(types::INT v) noexcept { m_TerminationType = v; m_presence |= (1ULL << P_TerminationType); }
	void setStartDate(types::UTCDATEONLY v) noexcept { m_StartDate = v; m_presence |= (1ULL << P_StartDate); }
	void setEndDate(types::UTCDATEONLY v) noexcept { m_EndDate = v; m_presence |= (1ULL << P_EndDate); }
	void setDeliveryType(types::INT v) noexcept { m_DeliveryType = v; m_presence |= (1ULL << P_DeliveryType); }
	void setMarginRatio(types::FLOAT v) noexcept { m_MarginRatio = v; m_presence |= (1ULL << P_MarginRatio); }

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		return false;
	}
	bool hasAgreementDesc() const noexcept { return (m_presence & (1ULL << P_AgreementDesc)) != 0; }
	bool hasAgreementID() const noexcept { return (m_presence & (1ULL << P_AgreementID)) != 0; }
	bool hasAgreementDate() const noexcept { return (m_presence & (1ULL << P_AgreementDate)) != 0; }
	bool hasAgreementCurrency() const noexcept { return (m_presence & (1ULL << P_AgreementCurrency)) != 0; }
	bool hasTerminationType() const noexcept { return (m_presence & (1ULL << P_TerminationType)) != 0; }
	bool hasStartDate() const noexcept { return (m_presence & (1ULL << P_StartDate)) != 0; }
	bool hasEndDate() const noexcept { return (m_presence & (1ULL << P_EndDate)) != 0; }
	bool hasDeliveryType() const noexcept { return (m_presence & (1ULL << P_DeliveryType)) != 0; }
	bool hasMarginRatio() const noexcept { return (m_presence & (1ULL << P_MarginRatio)) != 0; }
};

} // namespace fix44::components
