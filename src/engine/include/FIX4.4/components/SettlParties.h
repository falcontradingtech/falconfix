// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 4b73cbb60d72c03c

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

#include <FIX4.4/components/SettlPtysSubGrp.h>

namespace fix44::components {

class SettlParties {
public:
	struct NoSettlPartyIDs {
		static constexpr std::size_t P_SettlPartyID                 = 0;
		static constexpr std::size_t P_SettlPartyIDSource           = 1;
		static constexpr std::size_t P_SettlPartyRole               = 2;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 68;

		types::STRING<32> m_SettlPartyID{fields::SettlPartyID_NULL};
		types::CHAR m_SettlPartyIDSource{fields::NULL_CHAR};
		types::INT m_SettlPartyRole{fields::NULL_INT};
		SettlPtysSubGrp m_SettlPtysSubGrp{};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		std::size_t compute_buffer_size() const noexcept {
			return MAX_ENCODE_SIZE
				+ m_SettlPtysSubGrp.compute_buffer_size();
		}

		// accessors
		SettlPtysSubGrp &getRefSettlPtysSubGrp() noexcept { return m_SettlPtysSubGrp; }
		const SettlPtysSubGrp &getRefSettlPtysSubGrp() const noexcept { return m_SettlPtysSubGrp; }
		const types::STRING<32> &getSettlPartyID() const noexcept { return m_SettlPartyID; }
		const types::CHAR &getSettlPartyIDSource() const noexcept { return m_SettlPartyIDSource; }
		const types::INT &getSettlPartyRole() const noexcept { return m_SettlPartyRole; }

		// mutators
		void setSettlPartyID(const types::STRING<32> &v) noexcept { m_SettlPartyID = v; m_presence |= (1ULL << P_SettlPartyID); }
		void setSettlPartyIDSource(types::CHAR v) noexcept { m_SettlPartyIDSource = v; m_presence |= (1ULL << P_SettlPartyIDSource); }
		void setSettlPartyRole(types::INT v) noexcept { m_SettlPartyRole = v; m_presence |= (1ULL << P_SettlPartyRole); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			if (m_SettlPtysSubGrp.hasAnySet()) { return true; }
			return false;
		}
		bool hasSettlPartyID() const noexcept { return (m_presence & (1ULL << P_SettlPartyID)) != 0; }
		bool hasSettlPartyIDSource() const noexcept { return (m_presence & (1ULL << P_SettlPartyIDSource)) != 0; }
		bool hasSettlPartyRole() const noexcept { return (m_presence & (1ULL << P_SettlPartyRole)) != 0; }
	};

private:
	static constexpr std::size_t P_NoSettlPartyIDs              = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoSettlPartyIDs> m_NoSettlPartyIDsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoSettlPartyIDsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoSettlPartyIDs> &getRefNoSettlPartyIDsGroup() noexcept { return m_NoSettlPartyIDsGroup; }
	const std::vector<NoSettlPartyIDs> &getRefNoSettlPartyIDsGroup() const noexcept { return m_NoSettlPartyIDsGroup; }
	std::size_t getNoSettlPartyIDs() const noexcept { return m_NoSettlPartyIDsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoSettlPartyIDsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoSettlPartyIDs() const noexcept { return !m_NoSettlPartyIDsGroup.empty(); }
};

} // namespace fix44::components
