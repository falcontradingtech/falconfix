// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: c814ecc04b9be466

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class MiscFeesGrp {
public:
	struct NoMiscFees {
		static constexpr std::size_t P_MiscFeeAmt                   = 0;
		static constexpr std::size_t P_MiscFeeCurr                  = 1;
		static constexpr std::size_t P_MiscFeeType                  = 2;
		static constexpr std::size_t P_MiscFeeBasis                 = 3;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 97;

		types::FLOAT m_MiscFeeAmt{fields::NULL_AMT};
		types::CURRENCY<32> m_MiscFeeCurr{fields::MiscFeeCurr_NULL};
		types::CHAR m_MiscFeeType{fields::NULL_CHAR};
		types::INT m_MiscFeeBasis{fields::NULL_INT};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::FLOAT &getMiscFeeAmt() const noexcept { return m_MiscFeeAmt; }
		const types::CURRENCY<32> &getMiscFeeCurr() const noexcept { return m_MiscFeeCurr; }
		const types::CHAR &getMiscFeeType() const noexcept { return m_MiscFeeType; }
		const types::INT &getMiscFeeBasis() const noexcept { return m_MiscFeeBasis; }

		// mutators
		void setMiscFeeAmt(types::FLOAT v) noexcept { m_MiscFeeAmt = v; m_presence |= (1ULL << P_MiscFeeAmt); }
		void setMiscFeeCurr(const types::CURRENCY<32> &v) noexcept { m_MiscFeeCurr = v; m_presence |= (1ULL << P_MiscFeeCurr); }
		void setMiscFeeType(types::CHAR v) noexcept { m_MiscFeeType = v; m_presence |= (1ULL << P_MiscFeeType); }
		void setMiscFeeBasis(types::INT v) noexcept { m_MiscFeeBasis = v; m_presence |= (1ULL << P_MiscFeeBasis); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasMiscFeeAmt() const noexcept { return (m_presence & (1ULL << P_MiscFeeAmt)) != 0; }
		bool hasMiscFeeCurr() const noexcept { return (m_presence & (1ULL << P_MiscFeeCurr)) != 0; }
		bool hasMiscFeeType() const noexcept { return (m_presence & (1ULL << P_MiscFeeType)) != 0; }
		bool hasMiscFeeBasis() const noexcept { return (m_presence & (1ULL << P_MiscFeeBasis)) != 0; }
	};

private:
	static constexpr std::size_t P_NoMiscFees                   = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoMiscFees> m_NoMiscFeesGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoMiscFeesGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoMiscFees> &getRefNoMiscFeesGroup() noexcept { return m_NoMiscFeesGroup; }
	const std::vector<NoMiscFees> &getRefNoMiscFeesGroup() const noexcept { return m_NoMiscFeesGroup; }
	std::size_t getNoMiscFees() const noexcept { return m_NoMiscFeesGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoMiscFeesGroup.empty()) { return true; }
		return false;
	}
	bool hasNoMiscFees() const noexcept { return !m_NoMiscFeesGroup.empty(); }
};

} // namespace fix44::components
