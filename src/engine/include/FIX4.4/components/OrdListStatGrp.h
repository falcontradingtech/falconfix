// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 3fe719b66b560880

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class OrdListStatGrp {
public:
	struct NoOrders {
		static constexpr std::size_t P_ClOrdID                      = 0;
		static constexpr std::size_t P_SecondaryClOrdID             = 1;
		static constexpr std::size_t P_CumQty                       = 2;
		static constexpr std::size_t P_OrdStatus                    = 3;
		static constexpr std::size_t P_WorkingIndicator             = 4;
		static constexpr std::size_t P_LeavesQty                    = 5;
		static constexpr std::size_t P_CxlQty                       = 6;
		static constexpr std::size_t P_AvgPx                        = 7;
		static constexpr std::size_t P_OrdRejReason                 = 8;
		static constexpr std::size_t P_Text                         = 9;
		static constexpr std::size_t P_EncodedTextLen               = 10;
		static constexpr std::size_t P_EncodedText                  = 11;

		static constexpr uint64_t REQUIRED =
			(1ULL << 0) |
			(1ULL << 2) |
			(1ULL << 3) |
			(1ULL << 5) |
			(1ULL << 6) |
			(1ULL << 7);
		static constexpr std::size_t MAX_ENCODE_SIZE = 319;

		types::STRING<32> m_ClOrdID{fields::ClOrdID_NULL};
		types::STRING<32> m_SecondaryClOrdID{fields::SecondaryClOrdID_NULL};
		types::FLOAT m_CumQty{fields::NULL_QTY};
		types::CHAR m_OrdStatus{fields::NULL_CHAR};
		types::BOOLEAN m_WorkingIndicator{fields::NULL_BOOLEAN};
		types::FLOAT m_LeavesQty{fields::NULL_QTY};
		types::FLOAT m_CxlQty{fields::NULL_QTY};
		types::FLOAT m_AvgPx{fields::NULL_PRICE};
		types::INT m_OrdRejReason{fields::NULL_INT};
		types::STRING<32> m_Text{fields::Text_NULL};
		types::LENGTH m_EncodedTextLen{fields::NULL_LENGTH};
		types::DATA<32> m_EncodedText{fields::EncodedText_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getClOrdID() const noexcept { return m_ClOrdID; }
		const types::STRING<32> &getSecondaryClOrdID() const noexcept { return m_SecondaryClOrdID; }
		const types::FLOAT &getCumQty() const noexcept { return m_CumQty; }
		const types::CHAR &getOrdStatus() const noexcept { return m_OrdStatus; }
		const types::BOOLEAN &getWorkingIndicator() const noexcept { return m_WorkingIndicator; }
		const types::FLOAT &getLeavesQty() const noexcept { return m_LeavesQty; }
		const types::FLOAT &getCxlQty() const noexcept { return m_CxlQty; }
		const types::FLOAT &getAvgPx() const noexcept { return m_AvgPx; }
		const types::INT &getOrdRejReason() const noexcept { return m_OrdRejReason; }
		const types::STRING<32> &getText() const noexcept { return m_Text; }
		const types::LENGTH &getEncodedTextLen() const noexcept { return m_EncodedTextLen; }
		const types::DATA<32> &getEncodedText() const noexcept { return m_EncodedText; }

		// mutators
		void setClOrdID(const types::STRING<32> &v) noexcept { m_ClOrdID = v; m_presence |= (1ULL << P_ClOrdID); }
		void setSecondaryClOrdID(const types::STRING<32> &v) noexcept { m_SecondaryClOrdID = v; m_presence |= (1ULL << P_SecondaryClOrdID); }
		void setCumQty(types::FLOAT v) noexcept { m_CumQty = v; m_presence |= (1ULL << P_CumQty); }
		void setOrdStatus(types::CHAR v) noexcept { m_OrdStatus = v; m_presence |= (1ULL << P_OrdStatus); }
		void setWorkingIndicator(types::BOOLEAN v) noexcept { m_WorkingIndicator = v; m_presence |= (1ULL << P_WorkingIndicator); }
		void setLeavesQty(types::FLOAT v) noexcept { m_LeavesQty = v; m_presence |= (1ULL << P_LeavesQty); }
		void setCxlQty(types::FLOAT v) noexcept { m_CxlQty = v; m_presence |= (1ULL << P_CxlQty); }
		void setAvgPx(types::FLOAT v) noexcept { m_AvgPx = v; m_presence |= (1ULL << P_AvgPx); }
		void setOrdRejReason(types::INT v) noexcept { m_OrdRejReason = v; m_presence |= (1ULL << P_OrdRejReason); }
		void setText(const types::STRING<32> &v) noexcept { m_Text = v; m_presence |= (1ULL << P_Text); }
		void setEncodedTextLen(types::LENGTH v) noexcept { m_EncodedTextLen = v; m_presence |= (1ULL << P_EncodedTextLen); }
		void setEncodedText(const types::DATA<32> &v) noexcept { m_EncodedText = v; m_presence |= (1ULL << P_EncodedText); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasClOrdID() const noexcept { return (m_presence & (1ULL << P_ClOrdID)) != 0; }
		bool hasSecondaryClOrdID() const noexcept { return (m_presence & (1ULL << P_SecondaryClOrdID)) != 0; }
		bool hasCumQty() const noexcept { return (m_presence & (1ULL << P_CumQty)) != 0; }
		bool hasOrdStatus() const noexcept { return (m_presence & (1ULL << P_OrdStatus)) != 0; }
		bool hasWorkingIndicator() const noexcept { return (m_presence & (1ULL << P_WorkingIndicator)) != 0; }
		bool hasLeavesQty() const noexcept { return (m_presence & (1ULL << P_LeavesQty)) != 0; }
		bool hasCxlQty() const noexcept { return (m_presence & (1ULL << P_CxlQty)) != 0; }
		bool hasAvgPx() const noexcept { return (m_presence & (1ULL << P_AvgPx)) != 0; }
		bool hasOrdRejReason() const noexcept { return (m_presence & (1ULL << P_OrdRejReason)) != 0; }
		bool hasText() const noexcept { return (m_presence & (1ULL << P_Text)) != 0; }
		bool hasEncodedTextLen() const noexcept { return (m_presence & (1ULL << P_EncodedTextLen)) != 0; }
		bool hasEncodedText() const noexcept { return (m_presence & (1ULL << P_EncodedText)) != 0; }
	};

private:
	static constexpr std::size_t P_NoOrders                     = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 24;

	std::vector<NoOrders> m_NoOrdersGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoOrdersGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoOrders> &getRefNoOrdersGroup() noexcept { return m_NoOrdersGroup; }
	const std::vector<NoOrders> &getRefNoOrdersGroup() const noexcept { return m_NoOrdersGroup; }
	std::size_t getNoOrders() const noexcept { return m_NoOrdersGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoOrdersGroup.empty()) { return true; }
		return false;
	}
	bool hasNoOrders() const noexcept { return !m_NoOrdersGroup.empty(); }
};

} // namespace fix44::components
