// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: c1f91e66d46b0b0f

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class AffectedOrdGrp {
public:
	struct NoAffectedOrders {
		static constexpr std::size_t P_OrigClOrdID                  = 0;
		static constexpr std::size_t P_AffectedOrderID              = 1;
		static constexpr std::size_t P_AffectedSecondaryOrderID     = 2;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 110;

		types::STRING<32> m_OrigClOrdID{fields::OrigClOrdID_NULL};
		types::STRING<32> m_AffectedOrderID{fields::AffectedOrderID_NULL};
		types::STRING<32> m_AffectedSecondaryOrderID{fields::AffectedSecondaryOrderID_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getOrigClOrdID() const noexcept { return m_OrigClOrdID; }
		const types::STRING<32> &getAffectedOrderID() const noexcept { return m_AffectedOrderID; }
		const types::STRING<32> &getAffectedSecondaryOrderID() const noexcept { return m_AffectedSecondaryOrderID; }

		// mutators
		void setOrigClOrdID(const types::STRING<32> &v) noexcept { m_OrigClOrdID = v; m_presence |= (1ULL << P_OrigClOrdID); }
		void setAffectedOrderID(const types::STRING<32> &v) noexcept { m_AffectedOrderID = v; m_presence |= (1ULL << P_AffectedOrderID); }
		void setAffectedSecondaryOrderID(const types::STRING<32> &v) noexcept { m_AffectedSecondaryOrderID = v; m_presence |= (1ULL << P_AffectedSecondaryOrderID); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasOrigClOrdID() const noexcept { return (m_presence & (1ULL << P_OrigClOrdID)) != 0; }
		bool hasAffectedOrderID() const noexcept { return (m_presence & (1ULL << P_AffectedOrderID)) != 0; }
		bool hasAffectedSecondaryOrderID() const noexcept { return (m_presence & (1ULL << P_AffectedSecondaryOrderID)) != 0; }
	};

private:
	static constexpr std::size_t P_NoAffectedOrders             = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoAffectedOrders> m_NoAffectedOrdersGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoAffectedOrdersGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoAffectedOrders> &getRefNoAffectedOrdersGroup() noexcept { return m_NoAffectedOrdersGroup; }
	const std::vector<NoAffectedOrders> &getRefNoAffectedOrdersGroup() const noexcept { return m_NoAffectedOrdersGroup; }
	std::size_t getNoAffectedOrders() const noexcept { return m_NoAffectedOrdersGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoAffectedOrdersGroup.empty()) { return true; }
		return false;
	}
	bool hasNoAffectedOrders() const noexcept { return !m_NoAffectedOrdersGroup.empty(); }
};

} // namespace fix44::components
