// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 0d7a2d0bef23bb40

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

#include <FIX4.4/components/NstdPtysSubGrp.h>

namespace fix44::components {

class NestedParties {
public:
	struct NoNestedPartyIDs {
		static constexpr std::size_t P_NestedPartyID                = 0;
		static constexpr std::size_t P_NestedPartyIDSource          = 1;
		static constexpr std::size_t P_NestedPartyRole              = 2;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 68;

		types::STRING<32> m_NestedPartyID{fields::NestedPartyID_NULL};
		types::CHAR m_NestedPartyIDSource{fields::NULL_CHAR};
		types::INT m_NestedPartyRole{fields::NULL_INT};
		NstdPtysSubGrp m_NstdPtysSubGrp{};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		std::size_t compute_buffer_size() const noexcept {
			return MAX_ENCODE_SIZE
				+ m_NstdPtysSubGrp.compute_buffer_size();
		}

		// accessors
		NstdPtysSubGrp &getRefNstdPtysSubGrp() noexcept { return m_NstdPtysSubGrp; }
		const NstdPtysSubGrp &getRefNstdPtysSubGrp() const noexcept { return m_NstdPtysSubGrp; }
		const types::STRING<32> &getNestedPartyID() const noexcept { return m_NestedPartyID; }
		const types::CHAR &getNestedPartyIDSource() const noexcept { return m_NestedPartyIDSource; }
		const types::INT &getNestedPartyRole() const noexcept { return m_NestedPartyRole; }

		// mutators
		void setNestedPartyID(const types::STRING<32> &v) noexcept { m_NestedPartyID = v; m_presence |= (1ULL << P_NestedPartyID); }
		void setNestedPartyIDSource(types::CHAR v) noexcept { m_NestedPartyIDSource = v; m_presence |= (1ULL << P_NestedPartyIDSource); }
		void setNestedPartyRole(types::INT v) noexcept { m_NestedPartyRole = v; m_presence |= (1ULL << P_NestedPartyRole); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			if (m_NstdPtysSubGrp.hasAnySet()) { return true; }
			return false;
		}
		bool hasNestedPartyID() const noexcept { return (m_presence & (1ULL << P_NestedPartyID)) != 0; }
		bool hasNestedPartyIDSource() const noexcept { return (m_presence & (1ULL << P_NestedPartyIDSource)) != 0; }
		bool hasNestedPartyRole() const noexcept { return (m_presence & (1ULL << P_NestedPartyRole)) != 0; }
	};

private:
	static constexpr std::size_t P_NoNestedPartyIDs             = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoNestedPartyIDs> m_NoNestedPartyIDsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoNestedPartyIDsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoNestedPartyIDs> &getRefNoNestedPartyIDsGroup() noexcept { return m_NoNestedPartyIDsGroup; }
	const std::vector<NoNestedPartyIDs> &getRefNoNestedPartyIDsGroup() const noexcept { return m_NoNestedPartyIDsGroup; }
	std::size_t getNoNestedPartyIDs() const noexcept { return m_NoNestedPartyIDsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoNestedPartyIDsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoNestedPartyIDs() const noexcept { return !m_NoNestedPartyIDsGroup.empty(); }
};

} // namespace fix44::components
