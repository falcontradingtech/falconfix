// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 9753efa651215b74

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class MDReqGrp {
public:
	struct NoMDEntryTypes {
		static constexpr std::size_t P_MDEntryType                  = 0;

		static constexpr uint64_t REQUIRED = (1ULL << 0);
		static constexpr std::size_t MAX_ENCODE_SIZE = 6;

		types::CHAR m_MDEntryType{fields::NULL_CHAR};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::CHAR &getMDEntryType() const noexcept { return m_MDEntryType; }

		// mutators
		void setMDEntryType(types::CHAR v) noexcept { m_MDEntryType = v; m_presence |= (1ULL << P_MDEntryType); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasMDEntryType() const noexcept { return (m_presence & (1ULL << P_MDEntryType)) != 0; }
	};

private:
	static constexpr std::size_t P_NoMDEntryTypes               = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoMDEntryTypes> m_NoMDEntryTypesGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoMDEntryTypesGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoMDEntryTypes> &getRefNoMDEntryTypesGroup() noexcept { return m_NoMDEntryTypesGroup; }
	const std::vector<NoMDEntryTypes> &getRefNoMDEntryTypesGroup() const noexcept { return m_NoMDEntryTypesGroup; }
	std::size_t getNoMDEntryTypes() const noexcept { return m_NoMDEntryTypesGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoMDEntryTypesGroup.empty()) { return true; }
		return false;
	}
	bool hasNoMDEntryTypes() const noexcept { return !m_NoMDEntryTypesGroup.empty(); }
};

} // namespace fix44::components
