// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 6657afe4e520c8ba

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class EvntGrp {
public:
	struct NoEvents {
		static constexpr std::size_t P_EventType                    = 0;
		static constexpr std::size_t P_EventDate                    = 1;
		static constexpr std::size_t P_EventPx                      = 2;
		static constexpr std::size_t P_EventText                    = 3;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 104;

		types::INT m_EventType{fields::NULL_INT};
		types::UTCDATEONLY m_EventDate{fields::NULL_LOCALMKTDATE};
		types::FLOAT m_EventPx{fields::NULL_PRICE};
		types::STRING<32> m_EventText{fields::EventText_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::INT &getEventType() const noexcept { return m_EventType; }
		const types::UTCDATEONLY &getEventDate() const noexcept { return m_EventDate; }
		const types::FLOAT &getEventPx() const noexcept { return m_EventPx; }
		const types::STRING<32> &getEventText() const noexcept { return m_EventText; }

		// mutators
		void setEventType(types::INT v) noexcept { m_EventType = v; m_presence |= (1ULL << P_EventType); }
		void setEventDate(types::UTCDATEONLY v) noexcept { m_EventDate = v; m_presence |= (1ULL << P_EventDate); }
		void setEventPx(types::FLOAT v) noexcept { m_EventPx = v; m_presence |= (1ULL << P_EventPx); }
		void setEventText(const types::STRING<32> &v) noexcept { m_EventText = v; m_presence |= (1ULL << P_EventText); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasEventType() const noexcept { return (m_presence & (1ULL << P_EventType)) != 0; }
		bool hasEventDate() const noexcept { return (m_presence & (1ULL << P_EventDate)) != 0; }
		bool hasEventPx() const noexcept { return (m_presence & (1ULL << P_EventPx)) != 0; }
		bool hasEventText() const noexcept { return (m_presence & (1ULL << P_EventText)) != 0; }
	};

private:
	static constexpr std::size_t P_NoEvents                     = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoEvents> m_NoEventsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoEventsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoEvents> &getRefNoEventsGroup() noexcept { return m_NoEventsGroup; }
	const std::vector<NoEvents> &getRefNoEventsGroup() const noexcept { return m_NoEventsGroup; }
	std::size_t getNoEvents() const noexcept { return m_NoEventsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoEventsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoEvents() const noexcept { return !m_NoEventsGroup.empty(); }
};

} // namespace fix44::components
