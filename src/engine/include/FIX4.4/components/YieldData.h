// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: ef8eaea1f589e144

#pragma once

#include <array>
#include <cstdint>
#include <string_view>

#include <utils/error_codes.h>
#include <FIX4.4/core/field_id.h>
#include <FIX4.4/core/null_fields.h>
#include <FIX4.4/core/types.h>

namespace fix44::components {

class YieldData {
	static constexpr std::size_t P_YieldType                    = 0;
	static constexpr std::size_t P_Yield                        = 1;
	static constexpr std::size_t P_YieldCalcDate                = 2;
	static constexpr std::size_t P_YieldRedemptionDate          = 3;
	static constexpr std::size_t P_YieldRedemptionPrice         = 4;
	static constexpr std::size_t P_YieldRedemptionPriceType     = 5;

	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 146;

	types::STRING<32> m_YieldType{fields::YieldType_NULL};
	types::FLOAT m_Yield{fields::NULL_PERCENTAGE};
	types::UTCDATEONLY m_YieldCalcDate{fields::NULL_LOCALMKTDATE};
	types::UTCDATEONLY m_YieldRedemptionDate{fields::NULL_LOCALMKTDATE};
	types::FLOAT m_YieldRedemptionPrice{fields::NULL_PRICE};
	types::INT m_YieldRedemptionPriceType{fields::NULL_INT};

	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

	// accessors
	const types::STRING<32> &getYieldType() const noexcept { return m_YieldType; }
	const types::FLOAT &getYield() const noexcept { return m_Yield; }
	const types::UTCDATEONLY &getYieldCalcDate() const noexcept { return m_YieldCalcDate; }
	const types::UTCDATEONLY &getYieldRedemptionDate() const noexcept { return m_YieldRedemptionDate; }
	const types::FLOAT &getYieldRedemptionPrice() const noexcept { return m_YieldRedemptionPrice; }
	const types::INT &getYieldRedemptionPriceType() const noexcept { return m_YieldRedemptionPriceType; }

	// mutators
	void setYieldType(const types::STRING<32> &v) noexcept { m_YieldType = v; m_presence |= (1ULL << P_YieldType); }
	void setYield(types::FLOAT v) noexcept { m_Yield = v; m_presence |= (1ULL << P_Yield); }
	void setYieldCalcDate(types::UTCDATEONLY v) noexcept { m_YieldCalcDate = v; m_presence |= (1ULL << P_YieldCalcDate); }
	void setYieldRedemptionDate(types::UTCDATEONLY v) noexcept { m_YieldRedemptionDate = v; m_presence |= (1ULL << P_YieldRedemptionDate); }
	void setYieldRedemptionPrice(types::FLOAT v) noexcept { m_YieldRedemptionPrice = v; m_presence |= (1ULL << P_YieldRedemptionPrice); }
	void setYieldRedemptionPriceType(types::INT v) noexcept { m_YieldRedemptionPriceType = v; m_presence |= (1ULL << P_YieldRedemptionPriceType); }

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		return false;
	}
	bool hasYieldType() const noexcept { return (m_presence & (1ULL << P_YieldType)) != 0; }
	bool hasYield() const noexcept { return (m_presence & (1ULL << P_Yield)) != 0; }
	bool hasYieldCalcDate() const noexcept { return (m_presence & (1ULL << P_YieldCalcDate)) != 0; }
	bool hasYieldRedemptionDate() const noexcept { return (m_presence & (1ULL << P_YieldRedemptionDate)) != 0; }
	bool hasYieldRedemptionPrice() const noexcept { return (m_presence & (1ULL << P_YieldRedemptionPrice)) != 0; }
	bool hasYieldRedemptionPriceType() const noexcept { return (m_presence & (1ULL << P_YieldRedemptionPriceType)) != 0; }
};

} // namespace fix44::components
