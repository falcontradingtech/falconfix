// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: d4836f8e0ed4a1fd

#pragma once

// ----------------------------------------------------------------------------- 
// Branch prediction hints â€” High-Frequency Trading optimizations
// ----------------------------------------------------------------------------- 
// ----------------------------------------------------------------------------- 
// DECLARE_FIELD
// ----------------------------------------------------------------------------- 
#ifndef DECLARE_FIELD
	#define DECLARE_FIELD(type, name, init) type name = init;
	#define DECLARE_STATIC_FIELD(type, name, init) static inline constexpr type name = init;
#endif

// ----------------------------------------------------------------------------- 
// PARSER - CONTEXT
// ----------------------------------------------------------------------------- 
#define ACTIVATE_CTX(obj) do {                                                          \
	ctx_obj = &(obj);                                                                    \
	ctx_fn  = [](void *o, int32_t tag, const char *b, const char *e) noexcept -> bool { \
		return static_cast<decltype(&(obj))>(o)->setField(tag, b, e);                    \
	};                                                                                  \
} while(0)

// ----------------------------------------------------------------------------- 
// SERIALIZER
// ----------------------------------------------------------------------------- 
#define WRITE_TAG1(tag)  *reinterpret_cast<uint32_t*>(p) = (tag); p += 1
#define WRITE_TAG2(tag)  *reinterpret_cast<uint32_t*>(p) = (tag); p += 2
#define WRITE_TAG3(tag)  *reinterpret_cast<uint32_t*>(p) = (tag); p += 3
#define WRITE_TAG4(tag)  *reinterpret_cast<uint32_t*>(p) = (tag); p += 4
#define WRITE_TAG5(tag)  *reinterpret_cast<uint32_t*>(p) = (tag); p += 5
#define WRITE_TAG6(tag)  *reinterpret_cast<uint32_t*>(p) = (tag); p += 6
#define WRITE_TAG7(tag)  *reinterpret_cast<uint32_t*>(p) = (tag); p += 7
#define WRITE_TAG8(tag)  *reinterpret_cast<uint32_t*>(p) = (tag); p += 8
#define WRITE_TAG9(tag)  *reinterpret_cast<uint32_t*>(p) = (tag); p += 9

namespace fix44 {

} // namespace
