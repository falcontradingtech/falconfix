// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 226e40f6080f11b4

#pragma once

#include <utils/error_codes.h>
#include <session/session_id.h>
#include <FIX4.2/utils/datetime.h>

namespace fix42::engine {

template<typename Msg>
inline FFStatus prepareOutbound(Msg &msg, int64_t seqNum, const falconfix::SessionID &sid) noexcept {
	auto &header = msg.getRefHeader();

	header.setBeginString(sid.beginString());
	header.setSenderCompID(sid.senderCompID());
	header.setTargetCompID(sid.targetCompID());
	header.setMsgSeqNum(seqNum);
	header.setSendingTime(fix42::datetime::now_UTCTimestamp());

	return FF_OK();
}

template<typename Msg>
inline FFStatus prepareOutboundWithoutSeqOverride(Msg &msg, const falconfix::SessionID &sid) noexcept {
	auto &header = msg.getRefHeader();

	header.setBeginString(sid.beginString());
	header.setSenderCompID(sid.senderCompID());
	header.setTargetCompID(sid.targetCompID());
	header.setSendingTime(fix42::datetime::now_UTCTimestamp());

	return FF_OK();
}

} // namespace fix42::engine
