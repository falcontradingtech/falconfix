// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 3bb69703816e7c4e

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.3/core/field_id.h>
#include <FIX4.3/core/null_fields.h>
#include <FIX4.3/core/types.h>

namespace fix43::components {

class Instrument {
public:
	struct NoSecurityAltID {
		static constexpr std::size_t P_SecurityAltID                = 0;
		static constexpr std::size_t P_SecurityAltIDSource          = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 74;

		types::STRING<32> m_SecurityAltID{fields::SecurityAltID_NULL};
		types::STRING<32> m_SecurityAltIDSource{fields::SecurityAltIDSource_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getSecurityAltID() const noexcept { return m_SecurityAltID; }
		const types::STRING<32> &getSecurityAltIDSource() const noexcept { return m_SecurityAltIDSource; }

		// mutators
		void setSecurityAltID(const types::STRING<32> &v) noexcept { m_SecurityAltID = v; m_presence |= (1ULL << P_SecurityAltID); }
		void setSecurityAltIDSource(const types::STRING<32> &v) noexcept { m_SecurityAltIDSource = v; m_presence |= (1ULL << P_SecurityAltIDSource); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasSecurityAltID() const noexcept { return (m_presence & (1ULL << P_SecurityAltID)) != 0; }
		bool hasSecurityAltIDSource() const noexcept { return (m_presence & (1ULL << P_SecurityAltIDSource)) != 0; }
	};

private:
	static constexpr std::size_t P_NoSecurityAltID              = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoSecurityAltID> m_NoSecurityAltIDGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoSecurityAltIDGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoSecurityAltID> &getRefNoSecurityAltIDGroup() noexcept { return m_NoSecurityAltIDGroup; }
	const std::vector<NoSecurityAltID> &getRefNoSecurityAltIDGroup() const noexcept { return m_NoSecurityAltIDGroup; }
	std::size_t getNoSecurityAltID() const noexcept { return m_NoSecurityAltIDGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoSecurityAltIDGroup.empty()) { return true; }
		return false;
	}
	bool hasNoSecurityAltID() const noexcept { return !m_NoSecurityAltIDGroup.empty(); }
};

} // namespace fix43::components
