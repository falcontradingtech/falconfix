// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 2699aa923b44445a

#pragma once

#include <array>
#include <cstdint>
#include <string_view>

#include <utils/error_codes.h>
#include <FIX4.3/core/field_id.h>
#include <FIX4.3/core/null_fields.h>
#include <FIX4.3/core/types.h>

namespace fix43::components {

class OrderQtyData {
	static constexpr std::size_t P_OrderQty                     = 0;
	static constexpr std::size_t P_CashOrderQty                 = 1;
	static constexpr std::size_t P_OrderPercent                 = 2;
	static constexpr std::size_t P_RoundingDirection            = 3;
	static constexpr std::size_t P_RoundingModulus              = 4;

	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 121;

	types::FLOAT m_OrderQty{fields::NULL_QTY};
	types::FLOAT m_CashOrderQty{fields::NULL_QTY};
	types::FLOAT m_OrderPercent{fields::NULL_PERCENTAGE};
	types::CHAR m_RoundingDirection{fields::NULL_CHAR};
	types::FLOAT m_RoundingModulus{fields::NULL_FLOAT};

	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

	// accessors
	const types::FLOAT &getOrderQty() const noexcept { return m_OrderQty; }
	const types::FLOAT &getCashOrderQty() const noexcept { return m_CashOrderQty; }
	const types::FLOAT &getOrderPercent() const noexcept { return m_OrderPercent; }
	const types::CHAR &getRoundingDirection() const noexcept { return m_RoundingDirection; }
	const types::FLOAT &getRoundingModulus() const noexcept { return m_RoundingModulus; }

	// mutators
	void setOrderQty(types::FLOAT v) noexcept { m_OrderQty = v; m_presence |= (1ULL << P_OrderQty); }
	void setCashOrderQty(types::FLOAT v) noexcept { m_CashOrderQty = v; m_presence |= (1ULL << P_CashOrderQty); }
	void setOrderPercent(types::FLOAT v) noexcept { m_OrderPercent = v; m_presence |= (1ULL << P_OrderPercent); }
	void setRoundingDirection(types::CHAR v) noexcept { m_RoundingDirection = v; m_presence |= (1ULL << P_RoundingDirection); }
	void setRoundingModulus(types::FLOAT v) noexcept { m_RoundingModulus = v; m_presence |= (1ULL << P_RoundingModulus); }

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		return false;
	}
	bool hasOrderQty() const noexcept { return (m_presence & (1ULL << P_OrderQty)) != 0; }
	bool hasCashOrderQty() const noexcept { return (m_presence & (1ULL << P_CashOrderQty)) != 0; }
	bool hasOrderPercent() const noexcept { return (m_presence & (1ULL << P_OrderPercent)) != 0; }
	bool hasRoundingDirection() const noexcept { return (m_presence & (1ULL << P_RoundingDirection)) != 0; }
	bool hasRoundingModulus() const noexcept { return (m_presence & (1ULL << P_RoundingModulus)) != 0; }
};

} // namespace fix43::components
