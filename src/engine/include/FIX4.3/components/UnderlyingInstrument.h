// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Auto-generated FIX code. Do not edit by hand.

// Generated by FalconFIX Generator
// ContentHash: ff9d48b8aacbb778

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.3/core/field_id.h>
#include <FIX4.3/core/null_fields.h>
#include <FIX4.3/core/types.h>

namespace fix43::components {

class UnderlyingInstrument {
public:
	struct NoUnderlyingSecurityAltID {
		static constexpr std::size_t P_UnderlyingSecurityAltID      = 0;
		static constexpr std::size_t P_UnderlyingSecurityAltIDSource = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 74;

		types::STRING<32> m_UnderlyingSecurityAltID{fields::UnderlyingSecurityAltID_NULL};
		types::STRING<32> m_UnderlyingSecurityAltIDSource{fields::UnderlyingSecurityAltIDSource_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getUnderlyingSecurityAltID() const noexcept { return m_UnderlyingSecurityAltID; }
		const types::STRING<32> &getUnderlyingSecurityAltIDSource() const noexcept { return m_UnderlyingSecurityAltIDSource; }

		// mutators
		void setUnderlyingSecurityAltID(const types::STRING<32> &v) noexcept { m_UnderlyingSecurityAltID = v; m_presence |= (1ULL << P_UnderlyingSecurityAltID); }
		void setUnderlyingSecurityAltIDSource(const types::STRING<32> &v) noexcept { m_UnderlyingSecurityAltIDSource = v; m_presence |= (1ULL << P_UnderlyingSecurityAltIDSource); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasUnderlyingSecurityAltID() const noexcept { return (m_presence & (1ULL << P_UnderlyingSecurityAltID)) != 0; }
		bool hasUnderlyingSecurityAltIDSource() const noexcept { return (m_presence & (1ULL << P_UnderlyingSecurityAltIDSource)) != 0; }
	};

private:
	static constexpr std::size_t P_NoUnderlyingSecurityAltID    = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoUnderlyingSecurityAltID> m_NoUnderlyingSecurityAltIDGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoUnderlyingSecurityAltIDGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoUnderlyingSecurityAltID> &getRefNoUnderlyingSecurityAltIDGroup() noexcept { return m_NoUnderlyingSecurityAltIDGroup; }
	const std::vector<NoUnderlyingSecurityAltID> &getRefNoUnderlyingSecurityAltIDGroup() const noexcept { return m_NoUnderlyingSecurityAltIDGroup; }
	std::size_t getNoUnderlyingSecurityAltID() const noexcept { return m_NoUnderlyingSecurityAltIDGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoUnderlyingSecurityAltIDGroup.empty()) { return true; }
		return false;
	}
	bool hasNoUnderlyingSecurityAltID() const noexcept { return !m_NoUnderlyingSecurityAltIDGroup.empty(); }
};

} // namespace fix43::components
