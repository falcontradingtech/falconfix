// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 4752422bb6bbd197

#pragma once

#include <array>
#include <cstddef>
#include <cstdint>
#include <string_view>
#include <vector>

#include <FIX4.3/core/field_id.h>
#include <FIX4.3/core/null_fields.h>
#include <FIX4.3/core/types.h>

namespace fix43::components {

class Parties {
public:
	struct NoPartyIDs {
		static constexpr std::size_t P_PartyID                      = 0;
		static constexpr std::size_t P_PartyIDSource                = 1;
		static constexpr std::size_t P_PartyRole                    = 2;
		static constexpr std::size_t P_PartySubID                   = 3;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 105;

		types::STRING<32> m_PartyID{fields::PartyID_NULL};
		types::CHAR m_PartyIDSource{fields::NULL_CHAR};
		types::INT m_PartyRole{fields::NULL_INT};
		types::STRING<32> m_PartySubID{fields::PartySubID_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;
		bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getPartyID() const noexcept { return m_PartyID; }
		const types::CHAR &getPartyIDSource() const noexcept { return m_PartyIDSource; }
		const types::INT &getPartyRole() const noexcept { return m_PartyRole; }
		const types::STRING<32> &getPartySubID() const noexcept { return m_PartySubID; }

		// mutators
		void setPartyID(const types::STRING<32> &v) noexcept { m_PartyID = v; m_presence |= (1ULL << P_PartyID); }
		void setPartyIDSource(types::CHAR v) noexcept { m_PartyIDSource = v; m_presence |= (1ULL << P_PartyIDSource); }
		void setPartyRole(types::INT v) noexcept { m_PartyRole = v; m_presence |= (1ULL << P_PartyRole); }
		void setPartySubID(const types::STRING<32> &v) noexcept { m_PartySubID = v; m_presence |= (1ULL << P_PartySubID); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasPartyID() const noexcept { return (m_presence & (1ULL << P_PartyID)) != 0; }
		bool hasPartyIDSource() const noexcept { return (m_presence & (1ULL << P_PartyIDSource)) != 0; }
		bool hasPartyRole() const noexcept { return (m_presence & (1ULL << P_PartyRole)) != 0; }
		bool hasPartySubID() const noexcept { return (m_presence & (1ULL << P_PartySubID)) != 0; }
	};

private:
	static constexpr std::size_t P_NoPartyIDs                   = 0;
	static constexpr uint64_t REQUIRED = 0ULL;
	static constexpr std::size_t MAX_ENCODE_SIZE = 25;

	std::vector<NoPartyIDs> m_NoPartyIDsGroup{};
	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;
	bool decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	inline bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		for (const auto &entry : m_NoPartyIDsGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	std::vector<NoPartyIDs> &getRefNoPartyIDsGroup() noexcept { return m_NoPartyIDsGroup; }
	const std::vector<NoPartyIDs> &getRefNoPartyIDsGroup() const noexcept { return m_NoPartyIDsGroup; }
	std::size_t getNoPartyIDs() const noexcept { return m_NoPartyIDsGroup.size(); }

	// mutators

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoPartyIDsGroup.empty()) { return true; }
		return false;
	}
	bool hasNoPartyIDs() const noexcept { return !m_NoPartyIDsGroup.empty(); }
};

} // namespace fix43::components
