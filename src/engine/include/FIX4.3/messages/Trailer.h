// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: ee5e92ee8516caa5

#pragma once

#include <array>
#include <cstdint>
#include <FIX4.3/core/types.h>
#include <FIX4.3/core/null_fields.h>
#include <FIX4.3/core/field_id.h>

namespace fix43::messages {

class Trailer {
	static constexpr std::size_t P_SignatureLength              = 0;
	static constexpr std::size_t P_Signature                    = 1;
	static constexpr std::size_t P_CheckSum                     = 2;

	static constexpr uint64_t REQUIRED = (1ULL << 2);
	static constexpr std::size_t MAX_ENCODE_SIZE = 96;

	types::LENGTH m_SignatureLength{fields::NULL_LENGTH};
	types::DATA<32> m_Signature{fields::Signature_NULL};
	types::STRING<32> m_CheckSum{fields::CheckSum_NULL};

	uint64_t m_presence{0};

public:
	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	bool decode(const char *&p, const char *end) noexcept;

	// encode
	char *encode(char *p, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE;
		return total;
	}

	// accessors
	const types::LENGTH &getSignatureLength() const noexcept { return m_SignatureLength; }
	const types::DATA<32> &getSignature() const noexcept { return m_Signature; }
	const types::STRING<32> &getCheckSum() const noexcept { return m_CheckSum; }

	// mutators
	void setSignatureLength(types::LENGTH v) noexcept { m_SignatureLength = v; m_presence |= (1ULL << P_SignatureLength); }
	void setSignature(const types::DATA<32> &v) noexcept { m_Signature = v; m_presence |= (1ULL << P_Signature); }
	void setCheckSum(const types::STRING<32> &v) noexcept { m_CheckSum = v; m_presence |= (1ULL << P_CheckSum); }

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		return false;
	}
	bool hasSignatureLength() const noexcept { return (m_presence & (1ULL << P_SignatureLength)) != 0; }
	bool hasSignature() const noexcept { return (m_presence & (1ULL << P_Signature)) != 0; }
	bool hasCheckSum() const noexcept { return (m_presence & (1ULL << P_CheckSum)) != 0; }
};

} // namespace fix43::messages
