// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 0be2b354cd85e8b0

#pragma once

#include <array>
#include <cstdint>
#include <string_view>
#include <utils/fast_buffer.h>
#include <utils/error_codes.h>
#include <utility>
#include <vector>
#include <FIX4.3/core/types.h>
#include <FIX4.3/core/null_fields.h>
#include <FIX4.3/core/field_id.h>
#include <FIX4.3/engine/protocol.h>
#include <FIX4.3/messages/Header.h>
#include <FIX4.3/messages/Trailer.h>

#include <FIX4.3/components/Instrument.h>

namespace fix43::messages {

class News {
	static constexpr std::size_t P_OrigTime                     = 0;
	static constexpr std::size_t P_Urgency                      = 1;
	static constexpr std::size_t P_Headline                     = 2;
	static constexpr std::size_t P_EncodedHeadlineLen           = 3;
	static constexpr std::size_t P_EncodedHeadline              = 4;
	static constexpr std::size_t P_URLLink                      = 5;
	static constexpr std::size_t P_RawDataLength                = 6;
	static constexpr std::size_t P_RawData                      = 7;

	struct NoRoutingIDs {
		static constexpr std::size_t P_RoutingType                  = 0;
		static constexpr std::size_t P_RoutingID                    = 1;

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 62;

		types::INT m_RoutingType{fields::NULL_INT};
		types::STRING<32> m_RoutingID{fields::RoutingID_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::INT &getRoutingType() const noexcept { return m_RoutingType; }
		const types::STRING<32> &getRoutingID() const noexcept { return m_RoutingID; }

		// mutators
		void setRoutingType(types::INT v) noexcept { m_RoutingType = v; m_presence |= (1ULL << P_RoutingType); }
		void setRoutingID(const types::STRING<32> &v) noexcept { m_RoutingID = v; m_presence |= (1ULL << P_RoutingID); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasRoutingType() const noexcept { return (m_presence & (1ULL << P_RoutingType)) != 0; }
		bool hasRoutingID() const noexcept { return (m_presence & (1ULL << P_RoutingID)) != 0; }
	};

	struct NoRelatedSym {

		static constexpr uint64_t REQUIRED = 0ULL;
		static constexpr std::size_t MAX_ENCODE_SIZE = 32;

		components::Instrument m_Instrument{};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		std::size_t compute_buffer_size() const noexcept {
			return MAX_ENCODE_SIZE
				+ m_Instrument.compute_buffer_size();
		}

		// accessors
		components::Instrument &getRefInstrument() noexcept { return m_Instrument; }
		const components::Instrument &getRefInstrument() const noexcept { return m_Instrument; }

		// mutators

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			if (m_Instrument.hasAnySet()) { return true; }
			return false;
		}
	};

	struct LinesOfText {
		static constexpr std::size_t P_Text                         = 0;
		static constexpr std::size_t P_EncodedTextLen               = 1;
		static constexpr std::size_t P_EncodedText                  = 2;

		static constexpr uint64_t REQUIRED = (1ULL << 0);
		static constexpr std::size_t MAX_ENCODE_SIZE = 98;

		types::STRING<32> m_Text{fields::Text_NULL};
		types::LENGTH m_EncodedTextLen{fields::NULL_LENGTH};
		types::DATA<32> m_EncodedText{fields::EncodedText_NULL};

		uint64_t m_presence{0};

	public:
		// lifecycle
		void reset() noexcept;

		// validation
		bool checkRequired() const noexcept;

		// decode
		bool decode(const char *&p, const char *end) noexcept;

		// encode
		char *encode(char *p, bool includeMilliseconds = true) const noexcept;
		bool setField(int32_t tag, const char *begin, const char *end) noexcept;
		static constexpr std::size_t compute_buffer_size() noexcept { return MAX_ENCODE_SIZE; }

		// accessors
		const types::STRING<32> &getText() const noexcept { return m_Text; }
		const types::LENGTH &getEncodedTextLen() const noexcept { return m_EncodedTextLen; }
		const types::DATA<32> &getEncodedText() const noexcept { return m_EncodedText; }

		// mutators
		void setText(const types::STRING<32> &v) noexcept { m_Text = v; m_presence |= (1ULL << P_Text); }
		void setEncodedTextLen(types::LENGTH v) noexcept { m_EncodedTextLen = v; m_presence |= (1ULL << P_EncodedTextLen); }
		void setEncodedText(const types::DATA<32> &v) noexcept { m_EncodedText = v; m_presence |= (1ULL << P_EncodedText); }

		// presence
		bool hasAnySet() const noexcept {
			if (m_presence != 0) { return true; }
			return false;
		}
		bool hasText() const noexcept { return (m_presence & (1ULL << P_Text)) != 0; }
		bool hasEncodedTextLen() const noexcept { return (m_presence & (1ULL << P_EncodedTextLen)) != 0; }
		bool hasEncodedText() const noexcept { return (m_presence & (1ULL << P_EncodedText)) != 0; }
	};


	static constexpr uint64_t REQUIRED = (1ULL << 2);
	static constexpr std::size_t MAX_ENCODE_SIZE = 222;

	Header m_Header{};
	Trailer m_Trailer{};

	types::UTCTIMESTAMP m_OrigTime{fields::NULL_UTCTIMESTAMP};
	types::CHAR m_Urgency{fields::NULL_CHAR};
	types::STRING<32> m_Headline{fields::Headline_NULL};
	types::LENGTH m_EncodedHeadlineLen{fields::NULL_LENGTH};
	types::DATA<32> m_EncodedHeadline{fields::EncodedHeadline_NULL};
	std::vector<NoRoutingIDs> m_NoRoutingIDsGroup{};
	std::vector<NoRelatedSym> m_NoRelatedSymGroup{};
	std::vector<LinesOfText> m_LinesOfTextGroup{};
	types::STRING<32> m_URLLink{fields::URLLink_NULL};
	types::LENGTH m_RawDataLength{fields::NULL_LENGTH};
	types::DATA<32> m_RawData{fields::RawData_NULL};

	uint64_t m_presence{0};

public:
	using Protocol = fix43::Protocol;
	static constexpr falconfix::ProtocolVersion PROTOCOL = Protocol::VERSION;
	static constexpr std::string_view MSGTYPE = "B";
	static constexpr bool IS_ADMIN = false;

	// lifecycle
	void reset() noexcept;

	// validation
	bool checkRequired() const noexcept;

	// decode
	FFStatus setString(const char *&p, const char *end) noexcept;
	FFStatus setString(std::string_view sv) noexcept {
		const char *p = sv.data();
		const char *end = p + sv.size();
		return setString(p, end);
	}

	// encode
	FFStatus toString(falconfix::FastBuffer &out, bool includeMilliseconds = true) const noexcept;
	bool setField(int32_t tag, const char *begin, const char *end) noexcept;
	bool setField(int32_t tag, std::string_view value) noexcept { return setField(tag, value.data(), value.data() + value.size()); }
	std::size_t compute_buffer_size() const noexcept {
		std::size_t total = MAX_ENCODE_SIZE
			+ m_Header.compute_buffer_size()
			+ m_Trailer.compute_buffer_size();
		for (const auto &entry : m_NoRoutingIDsGroup) { total += entry.compute_buffer_size(); }
		for (const auto &entry : m_NoRelatedSymGroup) { total += entry.compute_buffer_size(); }
		for (const auto &entry : m_LinesOfTextGroup) { total += entry.compute_buffer_size(); }
		return total;
	}

	// accessors
	Header &getRefHeader() noexcept { return m_Header; }
	const Header &getRefHeader() const noexcept { return m_Header; }
	Trailer &getRefTrailer() noexcept { return m_Trailer; }
	const Trailer &getRefTrailer() const noexcept { return m_Trailer; }

	const types::UTCTIMESTAMP &getOrigTime() const noexcept { return m_OrigTime; }
	const types::CHAR &getUrgency() const noexcept { return m_Urgency; }
	const types::STRING<32> &getHeadline() const noexcept { return m_Headline; }
	const types::LENGTH &getEncodedHeadlineLen() const noexcept { return m_EncodedHeadlineLen; }
	const types::DATA<32> &getEncodedHeadline() const noexcept { return m_EncodedHeadline; }
	std::vector<NoRoutingIDs> &getRefNoRoutingIDsGroup() noexcept { return m_NoRoutingIDsGroup; }
	const std::vector<NoRoutingIDs> &getRefNoRoutingIDsGroup() const noexcept { return m_NoRoutingIDsGroup; }
	std::vector<NoRelatedSym> &getRefNoRelatedSymGroup() noexcept { return m_NoRelatedSymGroup; }
	const std::vector<NoRelatedSym> &getRefNoRelatedSymGroup() const noexcept { return m_NoRelatedSymGroup; }
	std::vector<LinesOfText> &getRefLinesOfTextGroup() noexcept { return m_LinesOfTextGroup; }
	const std::vector<LinesOfText> &getRefLinesOfTextGroup() const noexcept { return m_LinesOfTextGroup; }
	const types::STRING<32> &getURLLink() const noexcept { return m_URLLink; }
	const types::LENGTH &getRawDataLength() const noexcept { return m_RawDataLength; }
	const types::DATA<32> &getRawData() const noexcept { return m_RawData; }

	// mutators
	void setOrigTime(types::UTCTIMESTAMP v) noexcept { m_OrigTime = v; m_presence |= (1ULL << P_OrigTime); }
	void setUrgency(types::CHAR v) noexcept { m_Urgency = v; m_presence |= (1ULL << P_Urgency); }
	void setHeadline(const types::STRING<32> &v) noexcept { m_Headline = v; m_presence |= (1ULL << P_Headline); }
	void setEncodedHeadlineLen(types::LENGTH v) noexcept { m_EncodedHeadlineLen = v; m_presence |= (1ULL << P_EncodedHeadlineLen); }
	void setEncodedHeadline(const types::DATA<32> &v) noexcept { m_EncodedHeadline = v; m_presence |= (1ULL << P_EncodedHeadline); }
	void addNoRoutingIDsGroup(const NoRoutingIDs &item) { m_NoRoutingIDsGroup.push_back(item); }
	void addNoRoutingIDsGroup(NoRoutingIDs &&item) noexcept { m_NoRoutingIDsGroup.push_back(std::move(item)); }
	void addNoRelatedSymGroup(const NoRelatedSym &item) { m_NoRelatedSymGroup.push_back(item); }
	void addNoRelatedSymGroup(NoRelatedSym &&item) noexcept { m_NoRelatedSymGroup.push_back(std::move(item)); }
	void addLinesOfTextGroup(const LinesOfText &item) { m_LinesOfTextGroup.push_back(item); }
	void addLinesOfTextGroup(LinesOfText &&item) noexcept { m_LinesOfTextGroup.push_back(std::move(item)); }
	void setURLLink(const types::STRING<32> &v) noexcept { m_URLLink = v; m_presence |= (1ULL << P_URLLink); }
	void setRawDataLength(types::LENGTH v) noexcept { m_RawDataLength = v; m_presence |= (1ULL << P_RawDataLength); }
	void setRawData(const types::DATA<32> &v) noexcept { m_RawData = v; m_presence |= (1ULL << P_RawData); }

	// presence
	bool hasAnySet() const noexcept {
		if (m_presence != 0) { return true; }
		if (!m_NoRoutingIDsGroup.empty()) { return true; }
		if (!m_NoRelatedSymGroup.empty()) { return true; }
		if (!m_LinesOfTextGroup.empty()) { return true; }
		return false;
	}
	bool hasOrigTime() const noexcept { return (m_presence & (1ULL << P_OrigTime)) != 0; }
	bool hasUrgency() const noexcept { return (m_presence & (1ULL << P_Urgency)) != 0; }
	bool hasHeadline() const noexcept { return (m_presence & (1ULL << P_Headline)) != 0; }
	bool hasEncodedHeadlineLen() const noexcept { return (m_presence & (1ULL << P_EncodedHeadlineLen)) != 0; }
	bool hasEncodedHeadline() const noexcept { return (m_presence & (1ULL << P_EncodedHeadline)) != 0; }
	bool hasNoRoutingIDs() const noexcept { return !m_NoRoutingIDsGroup.empty(); }
	bool hasNoRelatedSym() const noexcept { return !m_NoRelatedSymGroup.empty(); }
	bool hasLinesOfText() const noexcept { return !m_LinesOfTextGroup.empty(); }
	bool hasURLLink() const noexcept { return (m_presence & (1ULL << P_URLLink)) != 0; }
	bool hasRawDataLength() const noexcept { return (m_presence & (1ULL << P_RawDataLength)) != 0; }
	bool hasRawData() const noexcept { return (m_presence & (1ULL << P_RawData)) != 0; }
};

} // namespace fix43::messages
