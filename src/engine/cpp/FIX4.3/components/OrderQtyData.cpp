// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Auto-generated FIX code. Do not edit by hand.

// Generated by FalconFIX Generator
// ContentHash: a6e62964a8144282

#include <FIX4.3/components/OrderQtyData.h>

#include <cstring>

#include <FIX4.3/utils/macros.h>
#include <FIX4.3/utils/parser.h>
#include <FIX4.3/utils/serializer.h>
#include <FIX4.3/utils/tag_constants.h>
#include <FIX4.3/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix43::components {

void OrderQtyData::reset() noexcept {
	m_OrderQty = fields::NULL_QTY;
	m_CashOrderQty = fields::NULL_QTY;
	m_OrderPercent = fields::NULL_PERCENTAGE;
	m_RoundingDirection = fields::NULL_CHAR;
	m_RoundingModulus = fields::NULL_FLOAT;
	m_presence = 0;
}

bool OrderQtyData::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool OrderQtyData::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool OrderQtyData::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix43::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return true;
}

char *OrderQtyData::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasOrderQty()) { WRITE_TAG3(fix43::tag_const::OrderQty); p += falconfix::dtoa(m_OrderQty, p); *p++ = fix43::serialize::SOH; }
	if (hasCashOrderQty()) { WRITE_TAG4(fix43::tag_const::CashOrderQty); p += falconfix::dtoa(m_CashOrderQty, p); *p++ = fix43::serialize::SOH; }
	if (hasOrderPercent()) { WRITE_TAG4(fix43::tag_const::OrderPercent); p += falconfix::dtoa(m_OrderPercent, p); *p++ = fix43::serialize::SOH; }
	if (hasRoundingDirection()) { WRITE_TAG4(fix43::tag_const::RoundingDirection); *p++ = m_RoundingDirection; *p++ = fix43::serialize::SOH; }
	if (hasRoundingModulus()) { WRITE_TAG4(fix43::tag_const::RoundingModulus); p += falconfix::dtoa(m_RoundingModulus, p); *p++ = fix43::serialize::SOH; }
	return p;
}

bool OrderQtyData::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::OrderQty: setOrderQty(fix43::parser::parse_double(begin, end)); return true;
	case field_id::CashOrderQty: setCashOrderQty(fix43::parser::parse_double(begin, end)); return true;
	case field_id::OrderPercent: setOrderPercent(fix43::parser::parse_double(begin, end)); return true;
	case field_id::RoundingDirection: setRoundingDirection(fix43::parser::parse_char(begin, end)); return true;
	case field_id::RoundingModulus: setRoundingModulus(fix43::parser::parse_double(begin, end)); return true;
	default: return false;
	}
}

} // namespace fix43::components
