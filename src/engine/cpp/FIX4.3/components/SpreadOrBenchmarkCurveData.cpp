// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: c654599d5b689b1b

#include <FIX4.3/components/SpreadOrBenchmarkCurveData.h>

#include <cstring>

#include <FIX4.3/utils/macros.h>
#include <FIX4.3/utils/parser.h>
#include <FIX4.3/utils/serializer.h>
#include <FIX4.3/utils/tag_constants.h>
#include <FIX4.3/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix43::components {

void SpreadOrBenchmarkCurveData::reset() noexcept {
	m_Spread = fields::NULL_PRICEOFFSET;
	m_BenchmarkCurveCurrency = fields::BenchmarkCurveCurrency_NULL;
	m_BenchmarkCurveName = fields::BenchmarkCurveName_NULL;
	m_BenchmarkCurvePoint = fields::BenchmarkCurvePoint_NULL;
	m_presence = 0;
}

bool SpreadOrBenchmarkCurveData::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool SpreadOrBenchmarkCurveData::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool SpreadOrBenchmarkCurveData::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix43::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return true;
}

char *SpreadOrBenchmarkCurveData::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasSpread()) { WRITE_TAG4(fix43::tag_const::Spread); p += falconfix::dtoa(m_Spread, p); *p++ = fix43::serialize::SOH; }
	if (hasBenchmarkCurveCurrency()) { WRITE_TAG4(fix43::tag_const::BenchmarkCurveCurrency); std::memcpy(p, m_BenchmarkCurveCurrency.data(), m_BenchmarkCurveCurrency.size()); p += m_BenchmarkCurveCurrency.size(); *p++ = fix43::serialize::SOH; }
	if (hasBenchmarkCurveName()) { WRITE_TAG4(fix43::tag_const::BenchmarkCurveName); std::memcpy(p, m_BenchmarkCurveName.data(), m_BenchmarkCurveName.size()); p += m_BenchmarkCurveName.size(); *p++ = fix43::serialize::SOH; }
	if (hasBenchmarkCurvePoint()) { WRITE_TAG4(fix43::tag_const::BenchmarkCurvePoint); std::memcpy(p, m_BenchmarkCurvePoint.data(), m_BenchmarkCurvePoint.size()); p += m_BenchmarkCurvePoint.size(); *p++ = fix43::serialize::SOH; }
	return p;
}

bool SpreadOrBenchmarkCurveData::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::Spread: setSpread(fix43::parser::parse_double(begin, end)); return true;
	case field_id::BenchmarkCurveCurrency: setBenchmarkCurveCurrency({begin, len}); return true;
	case field_id::BenchmarkCurveName: setBenchmarkCurveName({begin, len}); return true;
	case field_id::BenchmarkCurvePoint: setBenchmarkCurvePoint({begin, len}); return true;
	default: return false;
	}
}

} // namespace fix43::components
