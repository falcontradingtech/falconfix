// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 225b74507b793cba

#include <FIX4.3/components/NestedParties.h>

#include <cstring>

#include <FIX4.3/utils/macros.h>
#include <FIX4.3/utils/parser.h>
#include <FIX4.3/utils/serializer.h>
#include <FIX4.3/utils/tag_constants.h>
#include <FIX4.3/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix43::components {

void NestedParties::NoNestedPartyIDs::reset() noexcept {
	m_NestedPartyID = fields::NestedPartyID_NULL;
	m_NestedPartyIDSource = fields::NULL_CHAR;
	m_NestedPartyRole = fields::NULL_INT;
	m_NestedPartySubID = fields::NestedPartySubID_NULL;
	m_presence = 0;
}

bool NestedParties::NoNestedPartyIDs::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool NestedParties::NoNestedPartyIDs::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::NestedPartyID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix43::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *NestedParties::NoNestedPartyIDs::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasNestedPartyID()) { WRITE_TAG4(fix43::tag_const::NestedPartyID); std::memcpy(p, m_NestedPartyID.data(), m_NestedPartyID.size()); p += m_NestedPartyID.size(); *p++ = fix43::serialize::SOH; }
	if (hasNestedPartyIDSource()) { WRITE_TAG4(fix43::tag_const::NestedPartyIDSource); *p++ = m_NestedPartyIDSource; *p++ = fix43::serialize::SOH; }
	if (hasNestedPartyRole()) { WRITE_TAG4(fix43::tag_const::NestedPartyRole); p += falconfix::itoa(m_NestedPartyRole, p); *p++ = fix43::serialize::SOH; }
	if (hasNestedPartySubID()) { WRITE_TAG4(fix43::tag_const::NestedPartySubID); std::memcpy(p, m_NestedPartySubID.data(), m_NestedPartySubID.size()); p += m_NestedPartySubID.size(); *p++ = fix43::serialize::SOH; }
	return p;
}

bool NestedParties::NoNestedPartyIDs::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::NestedPartyID: setNestedPartyID({begin, len}); return true;
	case field_id::NestedPartyIDSource: setNestedPartyIDSource(fix43::parser::parse_char(begin, end)); return true;
	case field_id::NestedPartyRole: setNestedPartyRole(falconfix::atoi(begin, end)); return true;
	case field_id::NestedPartySubID: setNestedPartySubID({begin, len}); return true;
	default: return false;
	}
}

void NestedParties::reset() noexcept {
	m_NoNestedPartyIDsGroup.clear();
	m_presence = 0;
}

bool NestedParties::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoNestedPartyIDsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool NestedParties::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool NestedParties::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoNestedPartyIDs: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix43::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoNestedPartyIDsGroup.clear();
			m_NoNestedPartyIDsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoNestedPartyIDsGroup.emplace_back();
				if (!m_NoNestedPartyIDsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *NestedParties::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoNestedPartyIDsGroup.empty()) {
		WRITE_TAG4(fix43::tag_const::NoNestedPartyIDs);
		p += falconfix::itoa(static_cast<int64_t>(m_NoNestedPartyIDsGroup.size()), p);
		*p++ = fix43::serialize::SOH;
		for (const auto &entry : m_NoNestedPartyIDsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool NestedParties::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix43::components
