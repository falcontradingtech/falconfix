// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Auto-generated FIX code. Do not edit by hand.

// Generated by FalconFIX Generator
// ContentHash: c273e3d3de2ae3ab

#include <FIX4.3/components/Instrument.h>

#include <cstring>

#include <FIX4.3/utils/macros.h>
#include <FIX4.3/utils/parser.h>
#include <FIX4.3/utils/serializer.h>
#include <FIX4.3/utils/tag_constants.h>
#include <FIX4.3/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix43::components {

void Instrument::NoSecurityAltID::reset() noexcept {
	m_SecurityAltID = fields::SecurityAltID_NULL;
	m_SecurityAltIDSource = fields::SecurityAltIDSource_NULL;
	m_presence = 0;
}

bool Instrument::NoSecurityAltID::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool Instrument::NoSecurityAltID::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::SecurityAltID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix43::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *Instrument::NoSecurityAltID::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasSecurityAltID()) { WRITE_TAG4(fix43::tag_const::SecurityAltID); std::memcpy(p, m_SecurityAltID.data(), m_SecurityAltID.size()); p += m_SecurityAltID.size(); *p++ = fix43::serialize::SOH; }
	if (hasSecurityAltIDSource()) { WRITE_TAG4(fix43::tag_const::SecurityAltIDSource); std::memcpy(p, m_SecurityAltIDSource.data(), m_SecurityAltIDSource.size()); p += m_SecurityAltIDSource.size(); *p++ = fix43::serialize::SOH; }
	return p;
}

bool Instrument::NoSecurityAltID::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::SecurityAltID: setSecurityAltID({begin, len}); return true;
	case field_id::SecurityAltIDSource: setSecurityAltIDSource({begin, len}); return true;
	default: return false;
	}
}

void Instrument::reset() noexcept {
	m_NoSecurityAltIDGroup.clear();
	m_presence = 0;
}

bool Instrument::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoSecurityAltIDGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool Instrument::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool Instrument::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoSecurityAltID: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix43::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoSecurityAltIDGroup.clear();
			m_NoSecurityAltIDGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoSecurityAltIDGroup.emplace_back();
				if (!m_NoSecurityAltIDGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *Instrument::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoSecurityAltIDGroup.empty()) {
		WRITE_TAG4(fix43::tag_const::NoSecurityAltID);
		p += falconfix::itoa(static_cast<int64_t>(m_NoSecurityAltIDGroup.size()), p);
		*p++ = fix43::serialize::SOH;
		for (const auto &entry : m_NoSecurityAltIDGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool Instrument::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix43::components
