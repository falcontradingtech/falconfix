// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: ac235839a4d4c4f1

#include <FIX4.3/components/UnderlyingInstrument.h>

#include <cstring>

#include <FIX4.3/utils/macros.h>
#include <FIX4.3/utils/parser.h>
#include <FIX4.3/utils/serializer.h>
#include <FIX4.3/utils/tag_constants.h>
#include <FIX4.3/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix43::components {

void UnderlyingInstrument::NoUnderlyingSecurityAltID::reset() noexcept {
	m_UnderlyingSecurityAltID = fields::UnderlyingSecurityAltID_NULL;
	m_UnderlyingSecurityAltIDSource = fields::UnderlyingSecurityAltIDSource_NULL;
	m_presence = 0;
}

bool UnderlyingInstrument::NoUnderlyingSecurityAltID::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool UnderlyingInstrument::NoUnderlyingSecurityAltID::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::UnderlyingSecurityAltID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix43::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *UnderlyingInstrument::NoUnderlyingSecurityAltID::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasUnderlyingSecurityAltID()) { WRITE_TAG4(fix43::tag_const::UnderlyingSecurityAltID); std::memcpy(p, m_UnderlyingSecurityAltID.data(), m_UnderlyingSecurityAltID.size()); p += m_UnderlyingSecurityAltID.size(); *p++ = fix43::serialize::SOH; }
	if (hasUnderlyingSecurityAltIDSource()) { WRITE_TAG4(fix43::tag_const::UnderlyingSecurityAltIDSource); std::memcpy(p, m_UnderlyingSecurityAltIDSource.data(), m_UnderlyingSecurityAltIDSource.size()); p += m_UnderlyingSecurityAltIDSource.size(); *p++ = fix43::serialize::SOH; }
	return p;
}

bool UnderlyingInstrument::NoUnderlyingSecurityAltID::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::UnderlyingSecurityAltID: setUnderlyingSecurityAltID({begin, len}); return true;
	case field_id::UnderlyingSecurityAltIDSource: setUnderlyingSecurityAltIDSource({begin, len}); return true;
	default: return false;
	}
}

void UnderlyingInstrument::reset() noexcept {
	m_NoUnderlyingSecurityAltIDGroup.clear();
	m_presence = 0;
}

bool UnderlyingInstrument::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoUnderlyingSecurityAltIDGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool UnderlyingInstrument::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool UnderlyingInstrument::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoUnderlyingSecurityAltID: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix43::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoUnderlyingSecurityAltIDGroup.clear();
			m_NoUnderlyingSecurityAltIDGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoUnderlyingSecurityAltIDGroup.emplace_back();
				if (!m_NoUnderlyingSecurityAltIDGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *UnderlyingInstrument::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoUnderlyingSecurityAltIDGroup.empty()) {
		WRITE_TAG4(fix43::tag_const::NoUnderlyingSecurityAltID);
		p += falconfix::itoa(static_cast<int64_t>(m_NoUnderlyingSecurityAltIDGroup.size()), p);
		*p++ = fix43::serialize::SOH;
		for (const auto &entry : m_NoUnderlyingSecurityAltIDGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool UnderlyingInstrument::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix43::components
