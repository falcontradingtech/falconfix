// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Auto-generated FIX code. Do not edit by hand.

// Generated by FalconFIX Generator
// ContentHash: aaac7a6498c6375f

#include <FIX4.3/components/Parties.h>

#include <cstring>

#include <FIX4.3/utils/macros.h>
#include <FIX4.3/utils/parser.h>
#include <FIX4.3/utils/serializer.h>
#include <FIX4.3/utils/tag_constants.h>
#include <FIX4.3/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix43::components {

void Parties::NoPartyIDs::reset() noexcept {
	m_PartyID = fields::PartyID_NULL;
	m_PartyIDSource = fields::NULL_CHAR;
	m_PartyRole = fields::NULL_INT;
	m_PartySubID = fields::PartySubID_NULL;
	m_presence = 0;
}

bool Parties::NoPartyIDs::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool Parties::NoPartyIDs::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::PartyID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix43::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *Parties::NoPartyIDs::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasPartyID()) { WRITE_TAG4(fix43::tag_const::PartyID); std::memcpy(p, m_PartyID.data(), m_PartyID.size()); p += m_PartyID.size(); *p++ = fix43::serialize::SOH; }
	if (hasPartyIDSource()) { WRITE_TAG4(fix43::tag_const::PartyIDSource); *p++ = m_PartyIDSource; *p++ = fix43::serialize::SOH; }
	if (hasPartyRole()) { WRITE_TAG4(fix43::tag_const::PartyRole); p += falconfix::itoa(m_PartyRole, p); *p++ = fix43::serialize::SOH; }
	if (hasPartySubID()) { WRITE_TAG4(fix43::tag_const::PartySubID); std::memcpy(p, m_PartySubID.data(), m_PartySubID.size()); p += m_PartySubID.size(); *p++ = fix43::serialize::SOH; }
	return p;
}

bool Parties::NoPartyIDs::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::PartyID: setPartyID({begin, len}); return true;
	case field_id::PartyIDSource: setPartyIDSource(fix43::parser::parse_char(begin, end)); return true;
	case field_id::PartyRole: setPartyRole(falconfix::atoi(begin, end)); return true;
	case field_id::PartySubID: setPartySubID({begin, len}); return true;
	default: return false;
	}
}

void Parties::reset() noexcept {
	m_NoPartyIDsGroup.clear();
	m_presence = 0;
}

bool Parties::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoPartyIDsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool Parties::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool Parties::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoPartyIDs: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix43::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoPartyIDsGroup.clear();
			m_NoPartyIDsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoPartyIDsGroup.emplace_back();
				if (!m_NoPartyIDsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *Parties::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoPartyIDsGroup.empty()) {
		WRITE_TAG4(fix43::tag_const::NoPartyIDs);
		p += falconfix::itoa(static_cast<int64_t>(m_NoPartyIDsGroup.size()), p);
		*p++ = fix43::serialize::SOH;
		for (const auto &entry : m_NoPartyIDsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool Parties::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix43::components
