// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 646d36f6b4d1c1f6

#include <FIX4.3/components/Stipulations.h>

#include <cstring>

#include <FIX4.3/utils/macros.h>
#include <FIX4.3/utils/parser.h>
#include <FIX4.3/utils/serializer.h>
#include <FIX4.3/utils/tag_constants.h>
#include <FIX4.3/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix43::components {

void Stipulations::NoStipulations::reset() noexcept {
	m_StipulationType = fields::StipulationType_NULL;
	m_StipulationValue = fields::StipulationValue_NULL;
	m_presence = 0;
}

bool Stipulations::NoStipulations::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool Stipulations::NoStipulations::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::StipulationType) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix43::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *Stipulations::NoStipulations::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasStipulationType()) { WRITE_TAG4(fix43::tag_const::StipulationType); std::memcpy(p, m_StipulationType.data(), m_StipulationType.size()); p += m_StipulationType.size(); *p++ = fix43::serialize::SOH; }
	if (hasStipulationValue()) { WRITE_TAG4(fix43::tag_const::StipulationValue); std::memcpy(p, m_StipulationValue.data(), m_StipulationValue.size()); p += m_StipulationValue.size(); *p++ = fix43::serialize::SOH; }
	return p;
}

bool Stipulations::NoStipulations::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::StipulationType: setStipulationType({begin, len}); return true;
	case field_id::StipulationValue: setStipulationValue({begin, len}); return true;
	default: return false;
	}
}

void Stipulations::reset() noexcept {
	m_NoStipulationsGroup.clear();
	m_presence = 0;
}

bool Stipulations::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoStipulationsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool Stipulations::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool Stipulations::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoStipulations: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix43::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoStipulationsGroup.clear();
			m_NoStipulationsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoStipulationsGroup.emplace_back();
				if (!m_NoStipulationsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *Stipulations::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoStipulationsGroup.empty()) {
		WRITE_TAG4(fix43::tag_const::NoStipulations);
		p += falconfix::itoa(static_cast<int64_t>(m_NoStipulationsGroup.size()), p);
		*p++ = fix43::serialize::SOH;
		for (const auto &entry : m_NoStipulationsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool Stipulations::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix43::components
