// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 10706d0d1e8c4a32

#include <FIX4.4/components/ContAmtGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void ContAmtGrp::NoContAmts::reset() noexcept {
	m_ContAmtType = fields::NULL_INT;
	m_ContAmtValue = fields::NULL_FLOAT;
	m_ContAmtCurr = fields::ContAmtCurr_NULL;
	m_presence = 0;
}

bool ContAmtGrp::NoContAmts::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool ContAmtGrp::NoContAmts::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::ContAmtType) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *ContAmtGrp::NoContAmts::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasContAmtType()) { WRITE_TAG4(fix44::tag_const::ContAmtType); p += falconfix::itoa(m_ContAmtType, p); *p++ = fix44::serialize::SOH; }
	if (hasContAmtValue()) { WRITE_TAG4(fix44::tag_const::ContAmtValue); p += falconfix::dtoa(m_ContAmtValue, p); *p++ = fix44::serialize::SOH; }
	if (hasContAmtCurr()) { WRITE_TAG4(fix44::tag_const::ContAmtCurr); std::memcpy(p, m_ContAmtCurr.data(), m_ContAmtCurr.size()); p += m_ContAmtCurr.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool ContAmtGrp::NoContAmts::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::ContAmtType: setContAmtType(falconfix::atoi(begin, end)); return true;
	case field_id::ContAmtValue: setContAmtValue(fix44::parser::parse_double(begin, end)); return true;
	case field_id::ContAmtCurr: setContAmtCurr({begin, len}); return true;
	default: return false;
	}
}

void ContAmtGrp::reset() noexcept {
	m_NoContAmtsGroup.clear();
	m_presence = 0;
}

bool ContAmtGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoContAmtsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool ContAmtGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool ContAmtGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoContAmts: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoContAmtsGroup.clear();
			m_NoContAmtsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoContAmtsGroup.emplace_back();
				if (!m_NoContAmtsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *ContAmtGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoContAmtsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoContAmts);
		p += falconfix::itoa(static_cast<int64_t>(m_NoContAmtsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoContAmtsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool ContAmtGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
