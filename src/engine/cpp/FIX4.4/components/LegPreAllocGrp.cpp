// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: bf46cc289095257a

#include <FIX4.4/components/LegPreAllocGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void LegPreAllocGrp::NoLegAllocs::reset() noexcept {
	m_LegAllocAccount = fields::LegAllocAccount_NULL;
	m_LegIndividualAllocID = fields::LegIndividualAllocID_NULL;
	m_NestedParties2.reset();
	m_LegAllocQty = fields::NULL_QTY;
	m_LegAllocAcctIDSource = fields::LegAllocAcctIDSource_NULL;
	m_LegSettlCurrency = fields::LegSettlCurrency_NULL;
	m_presence = 0;
}

bool LegPreAllocGrp::NoLegAllocs::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_NestedParties2.hasAnySet() || m_NestedParties2.checkRequired());
}

bool LegPreAllocGrp::NoLegAllocs::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::LegAllocAccount) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoNested2PartyIDs:
			p = tag_start;
			if (!m_NestedParties2.decode(p, end, field_id::LegAllocAccount, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *LegPreAllocGrp::NoLegAllocs::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasLegAllocAccount()) { WRITE_TAG4(fix44::tag_const::LegAllocAccount); std::memcpy(p, m_LegAllocAccount.data(), m_LegAllocAccount.size()); p += m_LegAllocAccount.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegIndividualAllocID()) { WRITE_TAG4(fix44::tag_const::LegIndividualAllocID); std::memcpy(p, m_LegIndividualAllocID.data(), m_LegIndividualAllocID.size()); p += m_LegIndividualAllocID.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_NestedParties2.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasLegAllocQty()) { WRITE_TAG4(fix44::tag_const::LegAllocQty); p += falconfix::dtoa(m_LegAllocQty, p); *p++ = fix44::serialize::SOH; }
	if (hasLegAllocAcctIDSource()) { WRITE_TAG4(fix44::tag_const::LegAllocAcctIDSource); std::memcpy(p, m_LegAllocAcctIDSource.data(), m_LegAllocAcctIDSource.size()); p += m_LegAllocAcctIDSource.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegSettlCurrency()) { WRITE_TAG4(fix44::tag_const::LegSettlCurrency); std::memcpy(p, m_LegSettlCurrency.data(), m_LegSettlCurrency.size()); p += m_LegSettlCurrency.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool LegPreAllocGrp::NoLegAllocs::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::LegAllocAccount: setLegAllocAccount({begin, len}); return true;
	case field_id::LegIndividualAllocID: setLegIndividualAllocID({begin, len}); return true;
	case field_id::LegAllocQty: setLegAllocQty(fix44::parser::parse_double(begin, end)); return true;
	case field_id::LegAllocAcctIDSource: setLegAllocAcctIDSource({begin, len}); return true;
	case field_id::LegSettlCurrency: setLegSettlCurrency({begin, len}); return true;
	default: return false;
	}
}

void LegPreAllocGrp::reset() noexcept {
	m_NoLegAllocsGroup.clear();
	m_presence = 0;
}

bool LegPreAllocGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoLegAllocsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool LegPreAllocGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool LegPreAllocGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoLegAllocs: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoLegAllocsGroup.clear();
			m_NoLegAllocsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoLegAllocsGroup.emplace_back();
				if (!m_NoLegAllocsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *LegPreAllocGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoLegAllocsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoLegAllocs);
		p += falconfix::itoa(static_cast<int64_t>(m_NoLegAllocsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoLegAllocsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool LegPreAllocGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
