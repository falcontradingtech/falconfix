// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 4f9925edecdf7e01

#include <FIX4.4/components/PegInstructions.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void PegInstructions::reset() noexcept {
	m_PegOffsetValue = fields::NULL_FLOAT;
	m_PegMoveType = fields::NULL_INT;
	m_PegOffsetType = fields::NULL_INT;
	m_PegLimitType = fields::NULL_INT;
	m_PegRoundDirection = fields::NULL_INT;
	m_PegScope = fields::NULL_INT;
	m_presence = 0;
}

bool PegInstructions::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool PegInstructions::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool PegInstructions::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return true;
}

char *PegInstructions::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasPegOffsetValue()) { WRITE_TAG4(fix44::tag_const::PegOffsetValue); p += falconfix::dtoa(m_PegOffsetValue, p); *p++ = fix44::serialize::SOH; }
	if (hasPegMoveType()) { WRITE_TAG4(fix44::tag_const::PegMoveType); p += falconfix::itoa(m_PegMoveType, p); *p++ = fix44::serialize::SOH; }
	if (hasPegOffsetType()) { WRITE_TAG4(fix44::tag_const::PegOffsetType); p += falconfix::itoa(m_PegOffsetType, p); *p++ = fix44::serialize::SOH; }
	if (hasPegLimitType()) { WRITE_TAG4(fix44::tag_const::PegLimitType); p += falconfix::itoa(m_PegLimitType, p); *p++ = fix44::serialize::SOH; }
	if (hasPegRoundDirection()) { WRITE_TAG4(fix44::tag_const::PegRoundDirection); p += falconfix::itoa(m_PegRoundDirection, p); *p++ = fix44::serialize::SOH; }
	if (hasPegScope()) { WRITE_TAG4(fix44::tag_const::PegScope); p += falconfix::itoa(m_PegScope, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool PegInstructions::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::PegOffsetValue: setPegOffsetValue(fix44::parser::parse_double(begin, end)); return true;
	case field_id::PegMoveType: setPegMoveType(falconfix::atoi(begin, end)); return true;
	case field_id::PegOffsetType: setPegOffsetType(falconfix::atoi(begin, end)); return true;
	case field_id::PegLimitType: setPegLimitType(falconfix::atoi(begin, end)); return true;
	case field_id::PegRoundDirection: setPegRoundDirection(falconfix::atoi(begin, end)); return true;
	case field_id::PegScope: setPegScope(falconfix::atoi(begin, end)); return true;
	default: return false;
	}
}

} // namespace fix44::components
