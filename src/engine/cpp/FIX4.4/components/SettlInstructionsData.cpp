// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 1d38550df55593ab

#include <FIX4.4/components/SettlInstructionsData.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void SettlInstructionsData::reset() noexcept {
	m_SettlDeliveryType = fields::NULL_INT;
	m_StandInstDbType = fields::NULL_INT;
	m_StandInstDbName = fields::StandInstDbName_NULL;
	m_StandInstDbID = fields::StandInstDbID_NULL;
	m_DlvyInstGrp.reset();
	m_presence = 0;
}

bool SettlInstructionsData::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_DlvyInstGrp.hasAnySet() || m_DlvyInstGrp.checkRequired());
}

bool SettlInstructionsData::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool SettlInstructionsData::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoDlvyInst:
			p = tag_start;
			if (!m_DlvyInstGrp.decode(p, end, stop_tag, stop_on_repetition)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return true;
}

char *SettlInstructionsData::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasSettlDeliveryType()) { WRITE_TAG4(fix44::tag_const::SettlDeliveryType); p += falconfix::itoa(m_SettlDeliveryType, p); *p++ = fix44::serialize::SOH; }
	if (hasStandInstDbType()) { WRITE_TAG4(fix44::tag_const::StandInstDbType); p += falconfix::itoa(m_StandInstDbType, p); *p++ = fix44::serialize::SOH; }
	if (hasStandInstDbName()) { WRITE_TAG4(fix44::tag_const::StandInstDbName); std::memcpy(p, m_StandInstDbName.data(), m_StandInstDbName.size()); p += m_StandInstDbName.size(); *p++ = fix44::serialize::SOH; }
	if (hasStandInstDbID()) { WRITE_TAG4(fix44::tag_const::StandInstDbID); std::memcpy(p, m_StandInstDbID.data(), m_StandInstDbID.size()); p += m_StandInstDbID.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_DlvyInstGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool SettlInstructionsData::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::SettlDeliveryType: setSettlDeliveryType(falconfix::atoi(begin, end)); return true;
	case field_id::StandInstDbType: setStandInstDbType(falconfix::atoi(begin, end)); return true;
	case field_id::StandInstDbName: setStandInstDbName({begin, len}); return true;
	case field_id::StandInstDbID: setStandInstDbID({begin, len}); return true;
	default: return false;
	}
}

} // namespace fix44::components
