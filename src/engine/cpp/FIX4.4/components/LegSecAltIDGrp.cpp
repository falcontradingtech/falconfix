// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: a4c3a36c99c63dc4

#include <FIX4.4/components/LegSecAltIDGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void LegSecAltIDGrp::NoLegSecurityAltID::reset() noexcept {
	m_LegSecurityAltID = fields::LegSecurityAltID_NULL;
	m_LegSecurityAltIDSource = fields::LegSecurityAltIDSource_NULL;
	m_presence = 0;
}

bool LegSecAltIDGrp::NoLegSecurityAltID::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool LegSecAltIDGrp::NoLegSecurityAltID::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::LegSecurityAltID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *LegSecAltIDGrp::NoLegSecurityAltID::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasLegSecurityAltID()) { WRITE_TAG4(fix44::tag_const::LegSecurityAltID); std::memcpy(p, m_LegSecurityAltID.data(), m_LegSecurityAltID.size()); p += m_LegSecurityAltID.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegSecurityAltIDSource()) { WRITE_TAG4(fix44::tag_const::LegSecurityAltIDSource); std::memcpy(p, m_LegSecurityAltIDSource.data(), m_LegSecurityAltIDSource.size()); p += m_LegSecurityAltIDSource.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool LegSecAltIDGrp::NoLegSecurityAltID::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::LegSecurityAltID: setLegSecurityAltID({begin, len}); return true;
	case field_id::LegSecurityAltIDSource: setLegSecurityAltIDSource({begin, len}); return true;
	default: return false;
	}
}

void LegSecAltIDGrp::reset() noexcept {
	m_NoLegSecurityAltIDGroup.clear();
	m_presence = 0;
}

bool LegSecAltIDGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoLegSecurityAltIDGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool LegSecAltIDGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool LegSecAltIDGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoLegSecurityAltID: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoLegSecurityAltIDGroup.clear();
			m_NoLegSecurityAltIDGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoLegSecurityAltIDGroup.emplace_back();
				if (!m_NoLegSecurityAltIDGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *LegSecAltIDGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoLegSecurityAltIDGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoLegSecurityAltID);
		p += falconfix::itoa(static_cast<int64_t>(m_NoLegSecurityAltIDGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoLegSecurityAltIDGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool LegSecAltIDGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
