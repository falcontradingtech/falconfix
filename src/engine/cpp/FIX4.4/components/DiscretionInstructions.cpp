// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: bfcc7f8a29fc705e

#include <FIX4.4/components/DiscretionInstructions.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void DiscretionInstructions::reset() noexcept {
	m_DiscretionInst = fields::NULL_CHAR;
	m_DiscretionOffsetValue = fields::NULL_FLOAT;
	m_DiscretionMoveType = fields::NULL_INT;
	m_DiscretionOffsetType = fields::NULL_INT;
	m_DiscretionLimitType = fields::NULL_INT;
	m_DiscretionRoundDirection = fields::NULL_INT;
	m_DiscretionScope = fields::NULL_INT;
	m_presence = 0;
}

bool DiscretionInstructions::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool DiscretionInstructions::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool DiscretionInstructions::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return true;
}

char *DiscretionInstructions::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasDiscretionInst()) { WRITE_TAG4(fix44::tag_const::DiscretionInst); *p++ = m_DiscretionInst; *p++ = fix44::serialize::SOH; }
	if (hasDiscretionOffsetValue()) { WRITE_TAG4(fix44::tag_const::DiscretionOffsetValue); p += falconfix::dtoa(m_DiscretionOffsetValue, p); *p++ = fix44::serialize::SOH; }
	if (hasDiscretionMoveType()) { WRITE_TAG4(fix44::tag_const::DiscretionMoveType); p += falconfix::itoa(m_DiscretionMoveType, p); *p++ = fix44::serialize::SOH; }
	if (hasDiscretionOffsetType()) { WRITE_TAG4(fix44::tag_const::DiscretionOffsetType); p += falconfix::itoa(m_DiscretionOffsetType, p); *p++ = fix44::serialize::SOH; }
	if (hasDiscretionLimitType()) { WRITE_TAG4(fix44::tag_const::DiscretionLimitType); p += falconfix::itoa(m_DiscretionLimitType, p); *p++ = fix44::serialize::SOH; }
	if (hasDiscretionRoundDirection()) { WRITE_TAG4(fix44::tag_const::DiscretionRoundDirection); p += falconfix::itoa(m_DiscretionRoundDirection, p); *p++ = fix44::serialize::SOH; }
	if (hasDiscretionScope()) { WRITE_TAG4(fix44::tag_const::DiscretionScope); p += falconfix::itoa(m_DiscretionScope, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool DiscretionInstructions::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::DiscretionInst: setDiscretionInst(fix44::parser::parse_char(begin, end)); return true;
	case field_id::DiscretionOffsetValue: setDiscretionOffsetValue(fix44::parser::parse_double(begin, end)); return true;
	case field_id::DiscretionMoveType: setDiscretionMoveType(falconfix::atoi(begin, end)); return true;
	case field_id::DiscretionOffsetType: setDiscretionOffsetType(falconfix::atoi(begin, end)); return true;
	case field_id::DiscretionLimitType: setDiscretionLimitType(falconfix::atoi(begin, end)); return true;
	case field_id::DiscretionRoundDirection: setDiscretionRoundDirection(falconfix::atoi(begin, end)); return true;
	case field_id::DiscretionScope: setDiscretionScope(falconfix::atoi(begin, end)); return true;
	default: return false;
	}
}

} // namespace fix44::components
