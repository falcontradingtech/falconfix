// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: c242d122b8088513

#include <FIX4.4/components/FinancingDetails.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void FinancingDetails::reset() noexcept {
	m_AgreementDesc = fields::AgreementDesc_NULL;
	m_AgreementID = fields::AgreementID_NULL;
	m_AgreementDate = fields::NULL_LOCALMKTDATE;
	m_AgreementCurrency = fields::AgreementCurrency_NULL;
	m_TerminationType = fields::NULL_INT;
	m_StartDate = fields::NULL_LOCALMKTDATE;
	m_EndDate = fields::NULL_LOCALMKTDATE;
	m_DeliveryType = fields::NULL_INT;
	m_MarginRatio = fields::NULL_PERCENTAGE;
	m_presence = 0;
}

bool FinancingDetails::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool FinancingDetails::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool FinancingDetails::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return true;
}

char *FinancingDetails::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasAgreementDesc()) { WRITE_TAG4(fix44::tag_const::AgreementDesc); std::memcpy(p, m_AgreementDesc.data(), m_AgreementDesc.size()); p += m_AgreementDesc.size(); *p++ = fix44::serialize::SOH; }
	if (hasAgreementID()) { WRITE_TAG4(fix44::tag_const::AgreementID); std::memcpy(p, m_AgreementID.data(), m_AgreementID.size()); p += m_AgreementID.size(); *p++ = fix44::serialize::SOH; }
	if (hasAgreementDate()) { WRITE_TAG4(fix44::tag_const::AgreementDate); p += fix44::datetime::format_UTCDateOnly(m_AgreementDate, p); *p++ = fix44::serialize::SOH; }
	if (hasAgreementCurrency()) { WRITE_TAG4(fix44::tag_const::AgreementCurrency); std::memcpy(p, m_AgreementCurrency.data(), m_AgreementCurrency.size()); p += m_AgreementCurrency.size(); *p++ = fix44::serialize::SOH; }
	if (hasTerminationType()) { WRITE_TAG4(fix44::tag_const::TerminationType); p += falconfix::itoa(m_TerminationType, p); *p++ = fix44::serialize::SOH; }
	if (hasStartDate()) { WRITE_TAG4(fix44::tag_const::StartDate); p += fix44::datetime::format_UTCDateOnly(m_StartDate, p); *p++ = fix44::serialize::SOH; }
	if (hasEndDate()) { WRITE_TAG4(fix44::tag_const::EndDate); p += fix44::datetime::format_UTCDateOnly(m_EndDate, p); *p++ = fix44::serialize::SOH; }
	if (hasDeliveryType()) { WRITE_TAG4(fix44::tag_const::DeliveryType); p += falconfix::itoa(m_DeliveryType, p); *p++ = fix44::serialize::SOH; }
	if (hasMarginRatio()) { WRITE_TAG4(fix44::tag_const::MarginRatio); p += falconfix::dtoa(m_MarginRatio, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool FinancingDetails::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::AgreementDesc: setAgreementDesc({begin, len}); return true;
	case field_id::AgreementID: setAgreementID({begin, len}); return true;
	case field_id::AgreementDate: setAgreementDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::AgreementCurrency: setAgreementCurrency({begin, len}); return true;
	case field_id::TerminationType: setTerminationType(falconfix::atoi(begin, end)); return true;
	case field_id::StartDate: setStartDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::EndDate: setEndDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::DeliveryType: setDeliveryType(falconfix::atoi(begin, end)); return true;
	case field_id::MarginRatio: setMarginRatio(fix44::parser::parse_double(begin, end)); return true;
	default: return false;
	}
}

} // namespace fix44::components
