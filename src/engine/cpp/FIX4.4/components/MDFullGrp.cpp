// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: e1ddf4ef78f254d8

#include <FIX4.4/components/MDFullGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void MDFullGrp::NoMDEntries::reset() noexcept {
	m_MDEntryType = fields::NULL_CHAR;
	m_MDEntryPx = fields::NULL_PRICE;
	m_Currency = fields::Currency_NULL;
	m_MDEntrySize = fields::NULL_QTY;
	m_MDEntryDate = fields::NULL_UTCDATEONLY;
	m_MDEntryTime = fields::NULL_UTCTIMEONLY;
	m_TickDirection = fields::NULL_CHAR;
	m_MDMkt = fields::MDMkt_NULL;
	m_TradingSessionID = fields::TradingSessionID_NULL;
	m_TradingSessionSubID = fields::TradingSessionSubID_NULL;
	m_QuoteCondition = fields::QuoteCondition_NULL;
	m_TradeCondition = fields::TradeCondition_NULL;
	m_MDEntryOriginator = fields::MDEntryOriginator_NULL;
	m_LocationID = fields::LocationID_NULL;
	m_DeskID = fields::DeskID_NULL;
	m_OpenCloseSettlFlag = fields::OpenCloseSettlFlag_NULL;
	m_TimeInForce = fields::NULL_CHAR;
	m_ExpireDate = fields::NULL_LOCALMKTDATE;
	m_ExpireTime = fields::NULL_UTCTIMESTAMP;
	m_MinQty = fields::NULL_QTY;
	m_ExecInst = fields::ExecInst_NULL;
	m_SellerDays = fields::NULL_INT;
	m_OrderID = fields::OrderID_NULL;
	m_QuoteEntryID = fields::QuoteEntryID_NULL;
	m_MDEntryBuyer = fields::MDEntryBuyer_NULL;
	m_MDEntrySeller = fields::MDEntrySeller_NULL;
	m_NumberOfOrders = fields::NULL_INT;
	m_MDEntryPositionNo = fields::NULL_INT;
	m_Scope = fields::Scope_NULL;
	m_PriceDelta = fields::NULL_FLOAT;
	m_Text = fields::Text_NULL;
	m_EncodedTextLen = fields::NULL_LENGTH;
	m_EncodedText = fields::EncodedText_NULL;
	m_presence = 0;
}

bool MDFullGrp::NoMDEntries::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool MDFullGrp::NoMDEntries::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::MDEntryType) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *MDFullGrp::NoMDEntries::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasMDEntryType()) { WRITE_TAG4(fix44::tag_const::MDEntryType); *p++ = m_MDEntryType; *p++ = fix44::serialize::SOH; }
	if (hasMDEntryPx()) { WRITE_TAG4(fix44::tag_const::MDEntryPx); p += falconfix::dtoa(m_MDEntryPx, p); *p++ = fix44::serialize::SOH; }
	if (hasCurrency()) { WRITE_TAG3(fix44::tag_const::Currency); std::memcpy(p, m_Currency.data(), m_Currency.size()); p += m_Currency.size(); *p++ = fix44::serialize::SOH; }
	if (hasMDEntrySize()) { WRITE_TAG4(fix44::tag_const::MDEntrySize); p += falconfix::dtoa(m_MDEntrySize, p); *p++ = fix44::serialize::SOH; }
	if (hasMDEntryDate()) { WRITE_TAG4(fix44::tag_const::MDEntryDate); p += fix44::datetime::format_UTCDateOnly(m_MDEntryDate, p); *p++ = fix44::serialize::SOH; }
	if (hasMDEntryTime()) { WRITE_TAG4(fix44::tag_const::MDEntryTime); fix44::serialize::write_utctimeonly(p, m_MDEntryTime, includeMilliseconds); *p++ = fix44::serialize::SOH; }
	if (hasTickDirection()) { WRITE_TAG4(fix44::tag_const::TickDirection); *p++ = m_TickDirection; *p++ = fix44::serialize::SOH; }
	if (hasMDMkt()) { WRITE_TAG4(fix44::tag_const::MDMkt); std::memcpy(p, m_MDMkt.data(), m_MDMkt.size()); p += m_MDMkt.size(); *p++ = fix44::serialize::SOH; }
	if (hasTradingSessionID()) { WRITE_TAG4(fix44::tag_const::TradingSessionID); std::memcpy(p, m_TradingSessionID.data(), m_TradingSessionID.size()); p += m_TradingSessionID.size(); *p++ = fix44::serialize::SOH; }
	if (hasTradingSessionSubID()) { WRITE_TAG4(fix44::tag_const::TradingSessionSubID); std::memcpy(p, m_TradingSessionSubID.data(), m_TradingSessionSubID.size()); p += m_TradingSessionSubID.size(); *p++ = fix44::serialize::SOH; }
	if (hasQuoteCondition()) { WRITE_TAG4(fix44::tag_const::QuoteCondition); std::memcpy(p, m_QuoteCondition.data(), m_QuoteCondition.size()); p += m_QuoteCondition.size(); *p++ = fix44::serialize::SOH; }
	if (hasTradeCondition()) { WRITE_TAG4(fix44::tag_const::TradeCondition); std::memcpy(p, m_TradeCondition.data(), m_TradeCondition.size()); p += m_TradeCondition.size(); *p++ = fix44::serialize::SOH; }
	if (hasMDEntryOriginator()) { WRITE_TAG4(fix44::tag_const::MDEntryOriginator); std::memcpy(p, m_MDEntryOriginator.data(), m_MDEntryOriginator.size()); p += m_MDEntryOriginator.size(); *p++ = fix44::serialize::SOH; }
	if (hasLocationID()) { WRITE_TAG4(fix44::tag_const::LocationID); std::memcpy(p, m_LocationID.data(), m_LocationID.size()); p += m_LocationID.size(); *p++ = fix44::serialize::SOH; }
	if (hasDeskID()) { WRITE_TAG4(fix44::tag_const::DeskID); std::memcpy(p, m_DeskID.data(), m_DeskID.size()); p += m_DeskID.size(); *p++ = fix44::serialize::SOH; }
	if (hasOpenCloseSettlFlag()) { WRITE_TAG4(fix44::tag_const::OpenCloseSettlFlag); std::memcpy(p, m_OpenCloseSettlFlag.data(), m_OpenCloseSettlFlag.size()); p += m_OpenCloseSettlFlag.size(); *p++ = fix44::serialize::SOH; }
	if (hasTimeInForce()) { WRITE_TAG3(fix44::tag_const::TimeInForce); *p++ = m_TimeInForce; *p++ = fix44::serialize::SOH; }
	if (hasExpireDate()) { WRITE_TAG4(fix44::tag_const::ExpireDate); p += fix44::datetime::format_UTCDateOnly(m_ExpireDate, p); *p++ = fix44::serialize::SOH; }
	if (hasExpireTime()) { WRITE_TAG4(fix44::tag_const::ExpireTime); p += fix44::datetime::format_UTCTimestamp_compact(m_ExpireTime, p, includeMilliseconds); *p++ = fix44::serialize::SOH; }
	if (hasMinQty()) { WRITE_TAG4(fix44::tag_const::MinQty); p += falconfix::dtoa(m_MinQty, p); *p++ = fix44::serialize::SOH; }
	if (hasExecInst()) { WRITE_TAG3(fix44::tag_const::ExecInst); std::memcpy(p, m_ExecInst.data(), m_ExecInst.size()); p += m_ExecInst.size(); *p++ = fix44::serialize::SOH; }
	if (hasSellerDays()) { WRITE_TAG4(fix44::tag_const::SellerDays); p += falconfix::itoa(m_SellerDays, p); *p++ = fix44::serialize::SOH; }
	if (hasOrderID()) { WRITE_TAG3(fix44::tag_const::OrderID); std::memcpy(p, m_OrderID.data(), m_OrderID.size()); p += m_OrderID.size(); *p++ = fix44::serialize::SOH; }
	if (hasQuoteEntryID()) { WRITE_TAG4(fix44::tag_const::QuoteEntryID); std::memcpy(p, m_QuoteEntryID.data(), m_QuoteEntryID.size()); p += m_QuoteEntryID.size(); *p++ = fix44::serialize::SOH; }
	if (hasMDEntryBuyer()) { WRITE_TAG4(fix44::tag_const::MDEntryBuyer); std::memcpy(p, m_MDEntryBuyer.data(), m_MDEntryBuyer.size()); p += m_MDEntryBuyer.size(); *p++ = fix44::serialize::SOH; }
	if (hasMDEntrySeller()) { WRITE_TAG4(fix44::tag_const::MDEntrySeller); std::memcpy(p, m_MDEntrySeller.data(), m_MDEntrySeller.size()); p += m_MDEntrySeller.size(); *p++ = fix44::serialize::SOH; }
	if (hasNumberOfOrders()) { WRITE_TAG4(fix44::tag_const::NumberOfOrders); p += falconfix::itoa(m_NumberOfOrders, p); *p++ = fix44::serialize::SOH; }
	if (hasMDEntryPositionNo()) { WRITE_TAG4(fix44::tag_const::MDEntryPositionNo); p += falconfix::itoa(m_MDEntryPositionNo, p); *p++ = fix44::serialize::SOH; }
	if (hasScope()) { WRITE_TAG4(fix44::tag_const::Scope); std::memcpy(p, m_Scope.data(), m_Scope.size()); p += m_Scope.size(); *p++ = fix44::serialize::SOH; }
	if (hasPriceDelta()) { WRITE_TAG4(fix44::tag_const::PriceDelta); p += falconfix::dtoa(m_PriceDelta, p); *p++ = fix44::serialize::SOH; }
	if (hasText()) { WRITE_TAG3(fix44::tag_const::Text); std::memcpy(p, m_Text.data(), m_Text.size()); p += m_Text.size(); *p++ = fix44::serialize::SOH; }
	if (hasEncodedTextLen()) { WRITE_TAG4(fix44::tag_const::EncodedTextLen); p += falconfix::itoa(m_EncodedTextLen, p); *p++ = fix44::serialize::SOH; }
	if (hasEncodedText()) { WRITE_TAG4(fix44::tag_const::EncodedText); std::memcpy(p, m_EncodedText.data(), m_EncodedText.size()); p += m_EncodedText.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool MDFullGrp::NoMDEntries::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::MDEntryType: setMDEntryType(fix44::parser::parse_char(begin, end)); return true;
	case field_id::MDEntryPx: setMDEntryPx(fix44::parser::parse_double(begin, end)); return true;
	case field_id::Currency: setCurrency({begin, len}); return true;
	case field_id::MDEntrySize: setMDEntrySize(fix44::parser::parse_double(begin, end)); return true;
	case field_id::MDEntryDate: setMDEntryDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::MDEntryTime: setMDEntryTime(fix44::parser::parse_timeonly32(begin, end)); return true;
	case field_id::TickDirection: setTickDirection(fix44::parser::parse_char(begin, end)); return true;
	case field_id::MDMkt: setMDMkt({begin, len}); return true;
	case field_id::TradingSessionID: setTradingSessionID({begin, len}); return true;
	case field_id::TradingSessionSubID: setTradingSessionSubID({begin, len}); return true;
	case field_id::QuoteCondition: setQuoteCondition({begin, len}); return true;
	case field_id::TradeCondition: setTradeCondition({begin, len}); return true;
	case field_id::MDEntryOriginator: setMDEntryOriginator({begin, len}); return true;
	case field_id::LocationID: setLocationID({begin, len}); return true;
	case field_id::DeskID: setDeskID({begin, len}); return true;
	case field_id::OpenCloseSettlFlag: setOpenCloseSettlFlag({begin, len}); return true;
	case field_id::TimeInForce: setTimeInForce(fix44::parser::parse_char(begin, end)); return true;
	case field_id::ExpireDate: setExpireDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::ExpireTime: setExpireTime(falconfix::atoi(begin, end)); return true;
	case field_id::MinQty: setMinQty(fix44::parser::parse_double(begin, end)); return true;
	case field_id::ExecInst: setExecInst({begin, len}); return true;
	case field_id::SellerDays: setSellerDays(falconfix::atoi(begin, end)); return true;
	case field_id::OrderID: setOrderID({begin, len}); return true;
	case field_id::QuoteEntryID: setQuoteEntryID({begin, len}); return true;
	case field_id::MDEntryBuyer: setMDEntryBuyer({begin, len}); return true;
	case field_id::MDEntrySeller: setMDEntrySeller({begin, len}); return true;
	case field_id::NumberOfOrders: setNumberOfOrders(falconfix::atoi(begin, end)); return true;
	case field_id::MDEntryPositionNo: setMDEntryPositionNo(falconfix::atoi(begin, end)); return true;
	case field_id::Scope: setScope({begin, len}); return true;
	case field_id::PriceDelta: setPriceDelta(fix44::parser::parse_double(begin, end)); return true;
	case field_id::Text: setText({begin, len}); return true;
	case field_id::EncodedTextLen: setEncodedTextLen(falconfix::atoi(begin, end)); return true;
	case field_id::EncodedText: setEncodedText({begin, len}); return true;
	default: return false;
	}
}

void MDFullGrp::reset() noexcept {
	m_NoMDEntriesGroup.clear();
	m_presence = 0;
}

bool MDFullGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoMDEntriesGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool MDFullGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool MDFullGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoMDEntries: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoMDEntriesGroup.clear();
			m_NoMDEntriesGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoMDEntriesGroup.emplace_back();
				if (!m_NoMDEntriesGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *MDFullGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoMDEntriesGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoMDEntries);
		p += falconfix::itoa(static_cast<int64_t>(m_NoMDEntriesGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoMDEntriesGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool MDFullGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
