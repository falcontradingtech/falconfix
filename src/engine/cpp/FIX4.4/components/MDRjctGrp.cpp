// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 6a5387c4b2ab7625

#include <FIX4.4/components/MDRjctGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void MDRjctGrp::NoAltMDSource::reset() noexcept {
	m_AltMDSourceID = fields::AltMDSourceID_NULL;
	m_presence = 0;
}

bool MDRjctGrp::NoAltMDSource::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool MDRjctGrp::NoAltMDSource::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::AltMDSourceID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *MDRjctGrp::NoAltMDSource::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasAltMDSourceID()) { WRITE_TAG4(fix44::tag_const::AltMDSourceID); std::memcpy(p, m_AltMDSourceID.data(), m_AltMDSourceID.size()); p += m_AltMDSourceID.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool MDRjctGrp::NoAltMDSource::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::AltMDSourceID: setAltMDSourceID({begin, len}); return true;
	default: return false;
	}
}

void MDRjctGrp::reset() noexcept {
	m_NoAltMDSourceGroup.clear();
	m_presence = 0;
}

bool MDRjctGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoAltMDSourceGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool MDRjctGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool MDRjctGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoAltMDSource: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoAltMDSourceGroup.clear();
			m_NoAltMDSourceGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoAltMDSourceGroup.emplace_back();
				if (!m_NoAltMDSourceGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *MDRjctGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoAltMDSourceGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoAltMDSource);
		p += falconfix::itoa(static_cast<int64_t>(m_NoAltMDSourceGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoAltMDSourceGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool MDRjctGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
