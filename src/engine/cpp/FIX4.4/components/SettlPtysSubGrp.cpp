// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: ce211c35856a04e6

#include <FIX4.4/components/SettlPtysSubGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void SettlPtysSubGrp::NoSettlPartySubIDs::reset() noexcept {
	m_SettlPartySubID = fields::SettlPartySubID_NULL;
	m_SettlPartySubIDType = fields::NULL_INT;
	m_presence = 0;
}

bool SettlPtysSubGrp::NoSettlPartySubIDs::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool SettlPtysSubGrp::NoSettlPartySubIDs::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::SettlPartySubID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *SettlPtysSubGrp::NoSettlPartySubIDs::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasSettlPartySubID()) { WRITE_TAG4(fix44::tag_const::SettlPartySubID); std::memcpy(p, m_SettlPartySubID.data(), m_SettlPartySubID.size()); p += m_SettlPartySubID.size(); *p++ = fix44::serialize::SOH; }
	if (hasSettlPartySubIDType()) { WRITE_TAG4(fix44::tag_const::SettlPartySubIDType); p += falconfix::itoa(m_SettlPartySubIDType, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool SettlPtysSubGrp::NoSettlPartySubIDs::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::SettlPartySubID: setSettlPartySubID({begin, len}); return true;
	case field_id::SettlPartySubIDType: setSettlPartySubIDType(falconfix::atoi(begin, end)); return true;
	default: return false;
	}
}

void SettlPtysSubGrp::reset() noexcept {
	m_NoSettlPartySubIDsGroup.clear();
	m_presence = 0;
}

bool SettlPtysSubGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoSettlPartySubIDsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool SettlPtysSubGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool SettlPtysSubGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoSettlPartySubIDs: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoSettlPartySubIDsGroup.clear();
			m_NoSettlPartySubIDsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoSettlPartySubIDsGroup.emplace_back();
				if (!m_NoSettlPartySubIDsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *SettlPtysSubGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoSettlPartySubIDsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoSettlPartySubIDs);
		p += falconfix::itoa(static_cast<int64_t>(m_NoSettlPartySubIDsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoSettlPartySubIDsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool SettlPtysSubGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
