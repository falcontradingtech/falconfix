// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 94e71a8f32222811

#include <FIX4.4/components/RgstDtlsGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void RgstDtlsGrp::NoRegistDtls::reset() noexcept {
	m_RegistDtls = fields::RegistDtls_NULL;
	m_RegistEmail = fields::RegistEmail_NULL;
	m_MailingDtls = fields::MailingDtls_NULL;
	m_MailingInst = fields::MailingInst_NULL;
	m_NestedParties.reset();
	m_OwnerType = fields::NULL_INT;
	m_DateOfBirth = fields::NULL_LOCALMKTDATE;
	m_InvestorCountryOfResidence = fields::InvestorCountryOfResidence_NULL;
	m_presence = 0;
}

bool RgstDtlsGrp::NoRegistDtls::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_NestedParties.hasAnySet() || m_NestedParties.checkRequired());
}

bool RgstDtlsGrp::NoRegistDtls::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::RegistDtls) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoNestedPartyIDs:
			p = tag_start;
			if (!m_NestedParties.decode(p, end, field_id::RegistDtls, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *RgstDtlsGrp::NoRegistDtls::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasRegistDtls()) { WRITE_TAG4(fix44::tag_const::RegistDtls); std::memcpy(p, m_RegistDtls.data(), m_RegistDtls.size()); p += m_RegistDtls.size(); *p++ = fix44::serialize::SOH; }
	if (hasRegistEmail()) { WRITE_TAG4(fix44::tag_const::RegistEmail); std::memcpy(p, m_RegistEmail.data(), m_RegistEmail.size()); p += m_RegistEmail.size(); *p++ = fix44::serialize::SOH; }
	if (hasMailingDtls()) { WRITE_TAG4(fix44::tag_const::MailingDtls); std::memcpy(p, m_MailingDtls.data(), m_MailingDtls.size()); p += m_MailingDtls.size(); *p++ = fix44::serialize::SOH; }
	if (hasMailingInst()) { WRITE_TAG4(fix44::tag_const::MailingInst); std::memcpy(p, m_MailingInst.data(), m_MailingInst.size()); p += m_MailingInst.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_NestedParties.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasOwnerType()) { WRITE_TAG4(fix44::tag_const::OwnerType); p += falconfix::itoa(m_OwnerType, p); *p++ = fix44::serialize::SOH; }
	if (hasDateOfBirth()) { WRITE_TAG4(fix44::tag_const::DateOfBirth); p += fix44::datetime::format_UTCDateOnly(m_DateOfBirth, p); *p++ = fix44::serialize::SOH; }
	if (hasInvestorCountryOfResidence()) { WRITE_TAG4(fix44::tag_const::InvestorCountryOfResidence); std::memcpy(p, m_InvestorCountryOfResidence.data(), m_InvestorCountryOfResidence.size()); p += m_InvestorCountryOfResidence.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool RgstDtlsGrp::NoRegistDtls::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::RegistDtls: setRegistDtls({begin, len}); return true;
	case field_id::RegistEmail: setRegistEmail({begin, len}); return true;
	case field_id::MailingDtls: setMailingDtls({begin, len}); return true;
	case field_id::MailingInst: setMailingInst({begin, len}); return true;
	case field_id::OwnerType: setOwnerType(falconfix::atoi(begin, end)); return true;
	case field_id::DateOfBirth: setDateOfBirth(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::InvestorCountryOfResidence: setInvestorCountryOfResidence({begin, len}); return true;
	default: return false;
	}
}

void RgstDtlsGrp::reset() noexcept {
	m_NoRegistDtlsGroup.clear();
	m_presence = 0;
}

bool RgstDtlsGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoRegistDtlsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool RgstDtlsGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool RgstDtlsGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoRegistDtls: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoRegistDtlsGroup.clear();
			m_NoRegistDtlsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoRegistDtlsGroup.emplace_back();
				if (!m_NoRegistDtlsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *RgstDtlsGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoRegistDtlsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoRegistDtls);
		p += falconfix::itoa(static_cast<int64_t>(m_NoRegistDtlsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoRegistDtlsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool RgstDtlsGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
