// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: c5afd158cda7a518

#include <FIX4.4/components/UnderlyingStipulations.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void UnderlyingStipulations::NoUnderlyingStips::reset() noexcept {
	m_UnderlyingStipType = fields::UnderlyingStipType_NULL;
	m_UnderlyingStipValue = fields::UnderlyingStipValue_NULL;
	m_presence = 0;
}

bool UnderlyingStipulations::NoUnderlyingStips::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool UnderlyingStipulations::NoUnderlyingStips::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::UnderlyingStipType) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *UnderlyingStipulations::NoUnderlyingStips::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasUnderlyingStipType()) { WRITE_TAG4(fix44::tag_const::UnderlyingStipType); std::memcpy(p, m_UnderlyingStipType.data(), m_UnderlyingStipType.size()); p += m_UnderlyingStipType.size(); *p++ = fix44::serialize::SOH; }
	if (hasUnderlyingStipValue()) { WRITE_TAG4(fix44::tag_const::UnderlyingStipValue); std::memcpy(p, m_UnderlyingStipValue.data(), m_UnderlyingStipValue.size()); p += m_UnderlyingStipValue.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool UnderlyingStipulations::NoUnderlyingStips::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::UnderlyingStipType: setUnderlyingStipType({begin, len}); return true;
	case field_id::UnderlyingStipValue: setUnderlyingStipValue({begin, len}); return true;
	default: return false;
	}
}

void UnderlyingStipulations::reset() noexcept {
	m_NoUnderlyingStipsGroup.clear();
	m_presence = 0;
}

bool UnderlyingStipulations::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoUnderlyingStipsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool UnderlyingStipulations::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool UnderlyingStipulations::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoUnderlyingStips: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoUnderlyingStipsGroup.clear();
			m_NoUnderlyingStipsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoUnderlyingStipsGroup.emplace_back();
				if (!m_NoUnderlyingStipsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *UnderlyingStipulations::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoUnderlyingStipsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoUnderlyingStips);
		p += falconfix::itoa(static_cast<int64_t>(m_NoUnderlyingStipsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoUnderlyingStipsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool UnderlyingStipulations::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
