// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 5cb16c597add9897

#include <FIX4.4/components/InstrmtLegSecListGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void InstrmtLegSecListGrp::NoLegs::reset() noexcept {
	m_InstrumentLeg.reset();
	m_LegSwapType = fields::NULL_INT;
	m_LegSettlType = fields::NULL_CHAR;
	m_LegStipulations.reset();
	m_LegBenchmarkCurveData.reset();
	m_presence = 0;
}

bool InstrmtLegSecListGrp::NoLegs::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_InstrumentLeg.hasAnySet() || m_InstrumentLeg.checkRequired()) &&
			(!m_LegStipulations.hasAnySet() || m_LegStipulations.checkRequired()) &&
			(!m_LegBenchmarkCurveData.hasAnySet() || m_LegBenchmarkCurveData.checkRequired());
}

bool InstrmtLegSecListGrp::NoLegs::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::LegSymbol) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::LegSymbol:
			p = tag_start;
			if (!m_InstrumentLeg.decode(p, end, field_id::LegSymbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoLegStipulations:
			p = tag_start;
			if (!m_LegStipulations.decode(p, end, field_id::LegSymbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::LegBenchmarkCurveCurrency:
			p = tag_start;
			if (!m_LegBenchmarkCurveData.decode(p, end, field_id::LegSymbol, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *InstrmtLegSecListGrp::NoLegs::encode(char *p, bool includeMilliseconds) const noexcept {
	if ((p = m_InstrumentLeg.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasLegSwapType()) { WRITE_TAG4(fix44::tag_const::LegSwapType); p += falconfix::itoa(m_LegSwapType, p); *p++ = fix44::serialize::SOH; }
	if (hasLegSettlType()) { WRITE_TAG4(fix44::tag_const::LegSettlType); *p++ = m_LegSettlType; *p++ = fix44::serialize::SOH; }
	if ((p = m_LegStipulations.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if ((p = m_LegBenchmarkCurveData.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool InstrmtLegSecListGrp::NoLegs::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::LegSwapType: setLegSwapType(falconfix::atoi(begin, end)); return true;
	case field_id::LegSettlType: setLegSettlType(fix44::parser::parse_char(begin, end)); return true;
	default: return false;
	}
}

void InstrmtLegSecListGrp::reset() noexcept {
	m_NoLegsGroup.clear();
	m_presence = 0;
}

bool InstrmtLegSecListGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoLegsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool InstrmtLegSecListGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool InstrmtLegSecListGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoLegs: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoLegsGroup.clear();
			m_NoLegsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoLegsGroup.emplace_back();
				if (!m_NoLegsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *InstrmtLegSecListGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoLegsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoLegs);
		p += falconfix::itoa(static_cast<int64_t>(m_NoLegsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoLegsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool InstrmtLegSecListGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
