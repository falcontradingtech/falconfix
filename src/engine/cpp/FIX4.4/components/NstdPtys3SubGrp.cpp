// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 8969bdb97acd1e2b

#include <FIX4.4/components/NstdPtys3SubGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void NstdPtys3SubGrp::NoNested3PartySubIDs::reset() noexcept {
	m_Nested3PartySubID = fields::Nested3PartySubID_NULL;
	m_Nested3PartySubIDType = fields::NULL_INT;
	m_presence = 0;
}

bool NstdPtys3SubGrp::NoNested3PartySubIDs::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool NstdPtys3SubGrp::NoNested3PartySubIDs::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::Nested3PartySubID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *NstdPtys3SubGrp::NoNested3PartySubIDs::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasNested3PartySubID()) { WRITE_TAG4(fix44::tag_const::Nested3PartySubID); std::memcpy(p, m_Nested3PartySubID.data(), m_Nested3PartySubID.size()); p += m_Nested3PartySubID.size(); *p++ = fix44::serialize::SOH; }
	if (hasNested3PartySubIDType()) { WRITE_TAG4(fix44::tag_const::Nested3PartySubIDType); p += falconfix::itoa(m_Nested3PartySubIDType, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool NstdPtys3SubGrp::NoNested3PartySubIDs::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::Nested3PartySubID: setNested3PartySubID({begin, len}); return true;
	case field_id::Nested3PartySubIDType: setNested3PartySubIDType(falconfix::atoi(begin, end)); return true;
	default: return false;
	}
}

void NstdPtys3SubGrp::reset() noexcept {
	m_NoNested3PartySubIDsGroup.clear();
	m_presence = 0;
}

bool NstdPtys3SubGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoNested3PartySubIDsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool NstdPtys3SubGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool NstdPtys3SubGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoNested3PartySubIDs: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoNested3PartySubIDsGroup.clear();
			m_NoNested3PartySubIDsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoNested3PartySubIDsGroup.emplace_back();
				if (!m_NoNested3PartySubIDsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *NstdPtys3SubGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoNested3PartySubIDsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoNested3PartySubIDs);
		p += falconfix::itoa(static_cast<int64_t>(m_NoNested3PartySubIDsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoNested3PartySubIDsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool NstdPtys3SubGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
