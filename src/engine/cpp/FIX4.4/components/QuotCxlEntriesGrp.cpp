// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 157c5aa3f5da95c3

#include <FIX4.4/components/QuotCxlEntriesGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void QuotCxlEntriesGrp::NoQuoteEntries::reset() noexcept {
	m_Instrument.reset();
	m_FinancingDetails.reset();
	m_UndInstrmtGrp.reset();
	m_InstrmtLegGrp.reset();
	m_presence = 0;
}

bool QuotCxlEntriesGrp::NoQuoteEntries::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_Instrument.hasAnySet() || m_Instrument.checkRequired()) &&
			(!m_FinancingDetails.hasAnySet() || m_FinancingDetails.checkRequired()) &&
			(!m_UndInstrmtGrp.hasAnySet() || m_UndInstrmtGrp.checkRequired()) &&
			(!m_InstrmtLegGrp.hasAnySet() || m_InstrmtLegGrp.checkRequired());
}

bool QuotCxlEntriesGrp::NoQuoteEntries::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::Symbol) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::Symbol:
			p = tag_start;
			if (!m_Instrument.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::AgreementDesc:
			p = tag_start;
			if (!m_FinancingDetails.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoUnderlyings:
			p = tag_start;
			if (!m_UndInstrmtGrp.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoLegs:
			p = tag_start;
			if (!m_InstrmtLegGrp.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *QuotCxlEntriesGrp::NoQuoteEntries::encode(char *p, bool includeMilliseconds) const noexcept {
	if ((p = m_Instrument.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if ((p = m_FinancingDetails.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if ((p = m_UndInstrmtGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if ((p = m_InstrmtLegGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool QuotCxlEntriesGrp::NoQuoteEntries::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	default: return false;
	}
}

void QuotCxlEntriesGrp::reset() noexcept {
	m_NoQuoteEntriesGroup.clear();
	m_presence = 0;
}

bool QuotCxlEntriesGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoQuoteEntriesGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool QuotCxlEntriesGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool QuotCxlEntriesGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoQuoteEntries: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoQuoteEntriesGroup.clear();
			m_NoQuoteEntriesGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoQuoteEntriesGroup.emplace_back();
				if (!m_NoQuoteEntriesGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *QuotCxlEntriesGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoQuoteEntriesGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoQuoteEntries);
		p += falconfix::itoa(static_cast<int64_t>(m_NoQuoteEntriesGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoQuoteEntriesGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool QuotCxlEntriesGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
