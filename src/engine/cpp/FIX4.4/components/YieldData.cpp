// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 180b88c458f5347f

#include <FIX4.4/components/YieldData.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void YieldData::reset() noexcept {
	m_YieldType = fields::YieldType_NULL;
	m_Yield = fields::NULL_PERCENTAGE;
	m_YieldCalcDate = fields::NULL_LOCALMKTDATE;
	m_YieldRedemptionDate = fields::NULL_LOCALMKTDATE;
	m_YieldRedemptionPrice = fields::NULL_PRICE;
	m_YieldRedemptionPriceType = fields::NULL_INT;
	m_presence = 0;
}

bool YieldData::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool YieldData::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool YieldData::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return true;
}

char *YieldData::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasYieldType()) { WRITE_TAG4(fix44::tag_const::YieldType); std::memcpy(p, m_YieldType.data(), m_YieldType.size()); p += m_YieldType.size(); *p++ = fix44::serialize::SOH; }
	if (hasYield()) { WRITE_TAG4(fix44::tag_const::Yield); p += falconfix::dtoa(m_Yield, p); *p++ = fix44::serialize::SOH; }
	if (hasYieldCalcDate()) { WRITE_TAG4(fix44::tag_const::YieldCalcDate); p += fix44::datetime::format_UTCDateOnly(m_YieldCalcDate, p); *p++ = fix44::serialize::SOH; }
	if (hasYieldRedemptionDate()) { WRITE_TAG4(fix44::tag_const::YieldRedemptionDate); p += fix44::datetime::format_UTCDateOnly(m_YieldRedemptionDate, p); *p++ = fix44::serialize::SOH; }
	if (hasYieldRedemptionPrice()) { WRITE_TAG4(fix44::tag_const::YieldRedemptionPrice); p += falconfix::dtoa(m_YieldRedemptionPrice, p); *p++ = fix44::serialize::SOH; }
	if (hasYieldRedemptionPriceType()) { WRITE_TAG4(fix44::tag_const::YieldRedemptionPriceType); p += falconfix::itoa(m_YieldRedemptionPriceType, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool YieldData::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::YieldType: setYieldType({begin, len}); return true;
	case field_id::Yield: setYield(fix44::parser::parse_double(begin, end)); return true;
	case field_id::YieldCalcDate: setYieldCalcDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::YieldRedemptionDate: setYieldRedemptionDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::YieldRedemptionPrice: setYieldRedemptionPrice(fix44::parser::parse_double(begin, end)); return true;
	case field_id::YieldRedemptionPriceType: setYieldRedemptionPriceType(falconfix::atoi(begin, end)); return true;
	default: return false;
	}
}

} // namespace fix44::components
