// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 1714962ec2ba429f

#include <FIX4.4/components/TrdCapRptSideGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void TrdCapRptSideGrp::NoSides::reset() noexcept {
	m_Side = fields::NULL_CHAR;
	m_OrderID = fields::OrderID_NULL;
	m_SecondaryOrderID = fields::SecondaryOrderID_NULL;
	m_ClOrdID = fields::ClOrdID_NULL;
	m_SecondaryClOrdID = fields::SecondaryClOrdID_NULL;
	m_ListID = fields::ListID_NULL;
	m_Parties.reset();
	m_Account = fields::Account_NULL;
	m_AcctIDSource = fields::NULL_INT;
	m_AccountType = fields::NULL_INT;
	m_ProcessCode = fields::NULL_CHAR;
	m_OddLot = fields::NULL_BOOLEAN;
	m_ClrInstGrp.reset();
	m_TradeInputSource = fields::TradeInputSource_NULL;
	m_TradeInputDevice = fields::TradeInputDevice_NULL;
	m_OrderInputDevice = fields::OrderInputDevice_NULL;
	m_Currency = fields::Currency_NULL;
	m_ComplianceID = fields::ComplianceID_NULL;
	m_SolicitedFlag = fields::NULL_BOOLEAN;
	m_OrderCapacity = fields::NULL_CHAR;
	m_OrderRestrictions = fields::OrderRestrictions_NULL;
	m_CustOrderCapacity = fields::NULL_INT;
	m_OrdType = fields::NULL_CHAR;
	m_ExecInst = fields::ExecInst_NULL;
	m_TransBkdTime = fields::NULL_UTCTIMESTAMP;
	m_TradingSessionID = fields::TradingSessionID_NULL;
	m_TradingSessionSubID = fields::TradingSessionSubID_NULL;
	m_TimeBracket = fields::TimeBracket_NULL;
	m_CommissionData.reset();
	m_GrossTradeAmt = fields::NULL_AMT;
	m_NumDaysInterest = fields::NULL_INT;
	m_ExDate = fields::NULL_LOCALMKTDATE;
	m_AccruedInterestRate = fields::NULL_PERCENTAGE;
	m_AccruedInterestAmt = fields::NULL_AMT;
	m_InterestAtMaturity = fields::NULL_AMT;
	m_EndAccruedInterestAmt = fields::NULL_AMT;
	m_StartCash = fields::NULL_AMT;
	m_EndCash = fields::NULL_AMT;
	m_Concession = fields::NULL_AMT;
	m_TotalTakedown = fields::NULL_AMT;
	m_NetMoney = fields::NULL_AMT;
	m_SettlCurrAmt = fields::NULL_AMT;
	m_SettlCurrency = fields::SettlCurrency_NULL;
	m_SettlCurrFxRate = fields::NULL_FLOAT;
	m_SettlCurrFxRateCalc = fields::NULL_CHAR;
	m_PositionEffect = fields::NULL_CHAR;
	m_Text = fields::Text_NULL;
	m_EncodedTextLen = fields::NULL_LENGTH;
	m_EncodedText = fields::EncodedText_NULL;
	m_SideMultiLegReportingType = fields::NULL_INT;
	m_ContAmtGrp.reset();
	m_Stipulations.reset();
	m_MiscFeesGrp.reset();
	m_ExchangeRule = fields::ExchangeRule_NULL;
	m_TradeAllocIndicator = fields::NULL_INT;
	m_PreallocMethod = fields::NULL_CHAR;
	m_AllocID = fields::AllocID_NULL;
	m_TrdAllocGrp.reset();
	m_presence = 0;
}

bool TrdCapRptSideGrp::NoSides::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_Parties.hasAnySet() || m_Parties.checkRequired()) &&
			(!m_ClrInstGrp.hasAnySet() || m_ClrInstGrp.checkRequired()) &&
			(!m_CommissionData.hasAnySet() || m_CommissionData.checkRequired()) &&
			(!m_ContAmtGrp.hasAnySet() || m_ContAmtGrp.checkRequired()) &&
			(!m_Stipulations.hasAnySet() || m_Stipulations.checkRequired()) &&
			(!m_MiscFeesGrp.hasAnySet() || m_MiscFeesGrp.checkRequired()) &&
			(!m_TrdAllocGrp.hasAnySet() || m_TrdAllocGrp.checkRequired());
}

bool TrdCapRptSideGrp::NoSides::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::Side) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoPartyIDs:
			p = tag_start;
			if (!m_Parties.decode(p, end, field_id::Side, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoClearingInstructions:
			p = tag_start;
			if (!m_ClrInstGrp.decode(p, end, field_id::Side, true)) { return false; }
			consumed = true;
			continue;
		case field_id::Commission:
			p = tag_start;
			if (!m_CommissionData.decode(p, end, field_id::Side, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoContAmts:
			p = tag_start;
			if (!m_ContAmtGrp.decode(p, end, field_id::Side, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoStipulations:
			p = tag_start;
			if (!m_Stipulations.decode(p, end, field_id::Side, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoMiscFees:
			p = tag_start;
			if (!m_MiscFeesGrp.decode(p, end, field_id::Side, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoAllocs:
			p = tag_start;
			if (!m_TrdAllocGrp.decode(p, end, field_id::Side, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *TrdCapRptSideGrp::NoSides::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasSide()) { WRITE_TAG3(fix44::tag_const::Side); *p++ = m_Side; *p++ = fix44::serialize::SOH; }
	if (hasOrderID()) { WRITE_TAG3(fix44::tag_const::OrderID); std::memcpy(p, m_OrderID.data(), m_OrderID.size()); p += m_OrderID.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecondaryOrderID()) { WRITE_TAG4(fix44::tag_const::SecondaryOrderID); std::memcpy(p, m_SecondaryOrderID.data(), m_SecondaryOrderID.size()); p += m_SecondaryOrderID.size(); *p++ = fix44::serialize::SOH; }
	if (hasClOrdID()) { WRITE_TAG3(fix44::tag_const::ClOrdID); std::memcpy(p, m_ClOrdID.data(), m_ClOrdID.size()); p += m_ClOrdID.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecondaryClOrdID()) { WRITE_TAG4(fix44::tag_const::SecondaryClOrdID); std::memcpy(p, m_SecondaryClOrdID.data(), m_SecondaryClOrdID.size()); p += m_SecondaryClOrdID.size(); *p++ = fix44::serialize::SOH; }
	if (hasListID()) { WRITE_TAG3(fix44::tag_const::ListID); std::memcpy(p, m_ListID.data(), m_ListID.size()); p += m_ListID.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_Parties.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasAccount()) { WRITE_TAG2(fix44::tag_const::Account); std::memcpy(p, m_Account.data(), m_Account.size()); p += m_Account.size(); *p++ = fix44::serialize::SOH; }
	if (hasAcctIDSource()) { WRITE_TAG4(fix44::tag_const::AcctIDSource); p += falconfix::itoa(m_AcctIDSource, p); *p++ = fix44::serialize::SOH; }
	if (hasAccountType()) { WRITE_TAG4(fix44::tag_const::AccountType); p += falconfix::itoa(m_AccountType, p); *p++ = fix44::serialize::SOH; }
	if (hasProcessCode()) { WRITE_TAG3(fix44::tag_const::ProcessCode); *p++ = m_ProcessCode; *p++ = fix44::serialize::SOH; }
	if (hasOddLot()) { WRITE_TAG4(fix44::tag_const::OddLot); *p++ = m_OddLot ? 'Y' : 'N'; *p++ = fix44::serialize::SOH; }
	if ((p = m_ClrInstGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasTradeInputSource()) { WRITE_TAG4(fix44::tag_const::TradeInputSource); std::memcpy(p, m_TradeInputSource.data(), m_TradeInputSource.size()); p += m_TradeInputSource.size(); *p++ = fix44::serialize::SOH; }
	if (hasTradeInputDevice()) { WRITE_TAG4(fix44::tag_const::TradeInputDevice); std::memcpy(p, m_TradeInputDevice.data(), m_TradeInputDevice.size()); p += m_TradeInputDevice.size(); *p++ = fix44::serialize::SOH; }
	if (hasOrderInputDevice()) { WRITE_TAG4(fix44::tag_const::OrderInputDevice); std::memcpy(p, m_OrderInputDevice.data(), m_OrderInputDevice.size()); p += m_OrderInputDevice.size(); *p++ = fix44::serialize::SOH; }
	if (hasCurrency()) { WRITE_TAG3(fix44::tag_const::Currency); std::memcpy(p, m_Currency.data(), m_Currency.size()); p += m_Currency.size(); *p++ = fix44::serialize::SOH; }
	if (hasComplianceID()) { WRITE_TAG4(fix44::tag_const::ComplianceID); std::memcpy(p, m_ComplianceID.data(), m_ComplianceID.size()); p += m_ComplianceID.size(); *p++ = fix44::serialize::SOH; }
	if (hasSolicitedFlag()) { WRITE_TAG4(fix44::tag_const::SolicitedFlag); *p++ = m_SolicitedFlag ? 'Y' : 'N'; *p++ = fix44::serialize::SOH; }
	if (hasOrderCapacity()) { WRITE_TAG4(fix44::tag_const::OrderCapacity); *p++ = m_OrderCapacity; *p++ = fix44::serialize::SOH; }
	if (hasOrderRestrictions()) { WRITE_TAG4(fix44::tag_const::OrderRestrictions); std::memcpy(p, m_OrderRestrictions.data(), m_OrderRestrictions.size()); p += m_OrderRestrictions.size(); *p++ = fix44::serialize::SOH; }
	if (hasCustOrderCapacity()) { WRITE_TAG4(fix44::tag_const::CustOrderCapacity); p += falconfix::itoa(m_CustOrderCapacity, p); *p++ = fix44::serialize::SOH; }
	if (hasOrdType()) { WRITE_TAG3(fix44::tag_const::OrdType); *p++ = m_OrdType; *p++ = fix44::serialize::SOH; }
	if (hasExecInst()) { WRITE_TAG3(fix44::tag_const::ExecInst); std::memcpy(p, m_ExecInst.data(), m_ExecInst.size()); p += m_ExecInst.size(); *p++ = fix44::serialize::SOH; }
	if (hasTransBkdTime()) { WRITE_TAG4(fix44::tag_const::TransBkdTime); p += fix44::datetime::format_UTCTimestamp_compact(m_TransBkdTime, p, includeMilliseconds); *p++ = fix44::serialize::SOH; }
	if (hasTradingSessionID()) { WRITE_TAG4(fix44::tag_const::TradingSessionID); std::memcpy(p, m_TradingSessionID.data(), m_TradingSessionID.size()); p += m_TradingSessionID.size(); *p++ = fix44::serialize::SOH; }
	if (hasTradingSessionSubID()) { WRITE_TAG4(fix44::tag_const::TradingSessionSubID); std::memcpy(p, m_TradingSessionSubID.data(), m_TradingSessionSubID.size()); p += m_TradingSessionSubID.size(); *p++ = fix44::serialize::SOH; }
	if (hasTimeBracket()) { WRITE_TAG4(fix44::tag_const::TimeBracket); std::memcpy(p, m_TimeBracket.data(), m_TimeBracket.size()); p += m_TimeBracket.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_CommissionData.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasGrossTradeAmt()) { WRITE_TAG4(fix44::tag_const::GrossTradeAmt); p += falconfix::dtoa(m_GrossTradeAmt, p); *p++ = fix44::serialize::SOH; }
	if (hasNumDaysInterest()) { WRITE_TAG4(fix44::tag_const::NumDaysInterest); p += falconfix::itoa(m_NumDaysInterest, p); *p++ = fix44::serialize::SOH; }
	if (hasExDate()) { WRITE_TAG4(fix44::tag_const::ExDate); p += fix44::datetime::format_UTCDateOnly(m_ExDate, p); *p++ = fix44::serialize::SOH; }
	if (hasAccruedInterestRate()) { WRITE_TAG4(fix44::tag_const::AccruedInterestRate); p += falconfix::dtoa(m_AccruedInterestRate, p); *p++ = fix44::serialize::SOH; }
	if (hasAccruedInterestAmt()) { WRITE_TAG4(fix44::tag_const::AccruedInterestAmt); p += falconfix::dtoa(m_AccruedInterestAmt, p); *p++ = fix44::serialize::SOH; }
	if (hasInterestAtMaturity()) { WRITE_TAG4(fix44::tag_const::InterestAtMaturity); p += falconfix::dtoa(m_InterestAtMaturity, p); *p++ = fix44::serialize::SOH; }
	if (hasEndAccruedInterestAmt()) { WRITE_TAG4(fix44::tag_const::EndAccruedInterestAmt); p += falconfix::dtoa(m_EndAccruedInterestAmt, p); *p++ = fix44::serialize::SOH; }
	if (hasStartCash()) { WRITE_TAG4(fix44::tag_const::StartCash); p += falconfix::dtoa(m_StartCash, p); *p++ = fix44::serialize::SOH; }
	if (hasEndCash()) { WRITE_TAG4(fix44::tag_const::EndCash); p += falconfix::dtoa(m_EndCash, p); *p++ = fix44::serialize::SOH; }
	if (hasConcession()) { WRITE_TAG4(fix44::tag_const::Concession); p += falconfix::dtoa(m_Concession, p); *p++ = fix44::serialize::SOH; }
	if (hasTotalTakedown()) { WRITE_TAG4(fix44::tag_const::TotalTakedown); p += falconfix::dtoa(m_TotalTakedown, p); *p++ = fix44::serialize::SOH; }
	if (hasNetMoney()) { WRITE_TAG4(fix44::tag_const::NetMoney); p += falconfix::dtoa(m_NetMoney, p); *p++ = fix44::serialize::SOH; }
	if (hasSettlCurrAmt()) { WRITE_TAG4(fix44::tag_const::SettlCurrAmt); p += falconfix::dtoa(m_SettlCurrAmt, p); *p++ = fix44::serialize::SOH; }
	if (hasSettlCurrency()) { WRITE_TAG4(fix44::tag_const::SettlCurrency); std::memcpy(p, m_SettlCurrency.data(), m_SettlCurrency.size()); p += m_SettlCurrency.size(); *p++ = fix44::serialize::SOH; }
	if (hasSettlCurrFxRate()) { WRITE_TAG4(fix44::tag_const::SettlCurrFxRate); p += falconfix::dtoa(m_SettlCurrFxRate, p); *p++ = fix44::serialize::SOH; }
	if (hasSettlCurrFxRateCalc()) { WRITE_TAG4(fix44::tag_const::SettlCurrFxRateCalc); *p++ = m_SettlCurrFxRateCalc; *p++ = fix44::serialize::SOH; }
	if (hasPositionEffect()) { WRITE_TAG3(fix44::tag_const::PositionEffect); *p++ = m_PositionEffect; *p++ = fix44::serialize::SOH; }
	if (hasText()) { WRITE_TAG3(fix44::tag_const::Text); std::memcpy(p, m_Text.data(), m_Text.size()); p += m_Text.size(); *p++ = fix44::serialize::SOH; }
	if (hasEncodedTextLen()) { WRITE_TAG4(fix44::tag_const::EncodedTextLen); p += falconfix::itoa(m_EncodedTextLen, p); *p++ = fix44::serialize::SOH; }
	if (hasEncodedText()) { WRITE_TAG4(fix44::tag_const::EncodedText); std::memcpy(p, m_EncodedText.data(), m_EncodedText.size()); p += m_EncodedText.size(); *p++ = fix44::serialize::SOH; }
	if (hasSideMultiLegReportingType()) { WRITE_TAG4(fix44::tag_const::SideMultiLegReportingType); p += falconfix::itoa(m_SideMultiLegReportingType, p); *p++ = fix44::serialize::SOH; }
	if ((p = m_ContAmtGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if ((p = m_Stipulations.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if ((p = m_MiscFeesGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasExchangeRule()) { WRITE_TAG4(fix44::tag_const::ExchangeRule); std::memcpy(p, m_ExchangeRule.data(), m_ExchangeRule.size()); p += m_ExchangeRule.size(); *p++ = fix44::serialize::SOH; }
	if (hasTradeAllocIndicator()) { WRITE_TAG4(fix44::tag_const::TradeAllocIndicator); p += falconfix::itoa(m_TradeAllocIndicator, p); *p++ = fix44::serialize::SOH; }
	if (hasPreallocMethod()) { WRITE_TAG4(fix44::tag_const::PreallocMethod); *p++ = m_PreallocMethod; *p++ = fix44::serialize::SOH; }
	if (hasAllocID()) { WRITE_TAG3(fix44::tag_const::AllocID); std::memcpy(p, m_AllocID.data(), m_AllocID.size()); p += m_AllocID.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_TrdAllocGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool TrdCapRptSideGrp::NoSides::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::Side: setSide(fix44::parser::parse_char(begin, end)); return true;
	case field_id::OrderID: setOrderID({begin, len}); return true;
	case field_id::SecondaryOrderID: setSecondaryOrderID({begin, len}); return true;
	case field_id::ClOrdID: setClOrdID({begin, len}); return true;
	case field_id::SecondaryClOrdID: setSecondaryClOrdID({begin, len}); return true;
	case field_id::ListID: setListID({begin, len}); return true;
	case field_id::Account: setAccount({begin, len}); return true;
	case field_id::AcctIDSource: setAcctIDSource(falconfix::atoi(begin, end)); return true;
	case field_id::AccountType: setAccountType(falconfix::atoi(begin, end)); return true;
	case field_id::ProcessCode: setProcessCode(fix44::parser::parse_char(begin, end)); return true;
	case field_id::OddLot: setOddLot(fix44::parser::parse_bool(begin, end)); return true;
	case field_id::TradeInputSource: setTradeInputSource({begin, len}); return true;
	case field_id::TradeInputDevice: setTradeInputDevice({begin, len}); return true;
	case field_id::OrderInputDevice: setOrderInputDevice({begin, len}); return true;
	case field_id::Currency: setCurrency({begin, len}); return true;
	case field_id::ComplianceID: setComplianceID({begin, len}); return true;
	case field_id::SolicitedFlag: setSolicitedFlag(fix44::parser::parse_bool(begin, end)); return true;
	case field_id::OrderCapacity: setOrderCapacity(fix44::parser::parse_char(begin, end)); return true;
	case field_id::OrderRestrictions: setOrderRestrictions({begin, len}); return true;
	case field_id::CustOrderCapacity: setCustOrderCapacity(falconfix::atoi(begin, end)); return true;
	case field_id::OrdType: setOrdType(fix44::parser::parse_char(begin, end)); return true;
	case field_id::ExecInst: setExecInst({begin, len}); return true;
	case field_id::TransBkdTime: setTransBkdTime(falconfix::atoi(begin, end)); return true;
	case field_id::TradingSessionID: setTradingSessionID({begin, len}); return true;
	case field_id::TradingSessionSubID: setTradingSessionSubID({begin, len}); return true;
	case field_id::TimeBracket: setTimeBracket({begin, len}); return true;
	case field_id::GrossTradeAmt: setGrossTradeAmt(fix44::parser::parse_double(begin, end)); return true;
	case field_id::NumDaysInterest: setNumDaysInterest(falconfix::atoi(begin, end)); return true;
	case field_id::ExDate: setExDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::AccruedInterestRate: setAccruedInterestRate(fix44::parser::parse_double(begin, end)); return true;
	case field_id::AccruedInterestAmt: setAccruedInterestAmt(fix44::parser::parse_double(begin, end)); return true;
	case field_id::InterestAtMaturity: setInterestAtMaturity(fix44::parser::parse_double(begin, end)); return true;
	case field_id::EndAccruedInterestAmt: setEndAccruedInterestAmt(fix44::parser::parse_double(begin, end)); return true;
	case field_id::StartCash: setStartCash(fix44::parser::parse_double(begin, end)); return true;
	case field_id::EndCash: setEndCash(fix44::parser::parse_double(begin, end)); return true;
	case field_id::Concession: setConcession(fix44::parser::parse_double(begin, end)); return true;
	case field_id::TotalTakedown: setTotalTakedown(fix44::parser::parse_double(begin, end)); return true;
	case field_id::NetMoney: setNetMoney(fix44::parser::parse_double(begin, end)); return true;
	case field_id::SettlCurrAmt: setSettlCurrAmt(fix44::parser::parse_double(begin, end)); return true;
	case field_id::SettlCurrency: setSettlCurrency({begin, len}); return true;
	case field_id::SettlCurrFxRate: setSettlCurrFxRate(fix44::parser::parse_double(begin, end)); return true;
	case field_id::SettlCurrFxRateCalc: setSettlCurrFxRateCalc(fix44::parser::parse_char(begin, end)); return true;
	case field_id::PositionEffect: setPositionEffect(fix44::parser::parse_char(begin, end)); return true;
	case field_id::Text: setText({begin, len}); return true;
	case field_id::EncodedTextLen: setEncodedTextLen(falconfix::atoi(begin, end)); return true;
	case field_id::EncodedText: setEncodedText({begin, len}); return true;
	case field_id::SideMultiLegReportingType: setSideMultiLegReportingType(falconfix::atoi(begin, end)); return true;
	case field_id::ExchangeRule: setExchangeRule({begin, len}); return true;
	case field_id::TradeAllocIndicator: setTradeAllocIndicator(falconfix::atoi(begin, end)); return true;
	case field_id::PreallocMethod: setPreallocMethod(fix44::parser::parse_char(begin, end)); return true;
	case field_id::AllocID: setAllocID({begin, len}); return true;
	default: return false;
	}
}

void TrdCapRptSideGrp::reset() noexcept {
	m_NoSidesGroup.clear();
	m_presence = 0;
}

bool TrdCapRptSideGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoSidesGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool TrdCapRptSideGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool TrdCapRptSideGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoSides: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoSidesGroup.clear();
			m_NoSidesGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoSidesGroup.emplace_back();
				if (!m_NoSidesGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *TrdCapRptSideGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoSidesGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoSides);
		p += falconfix::itoa(static_cast<int64_t>(m_NoSidesGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoSidesGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool TrdCapRptSideGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
