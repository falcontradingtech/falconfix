// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 06cd3d6f9eab6634

#include <FIX4.4/components/InstrumentLeg.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void InstrumentLeg::reset() noexcept {
	m_LegSymbol = fields::LegSymbol_NULL;
	m_LegSymbolSfx = fields::LegSymbolSfx_NULL;
	m_LegSecurityID = fields::LegSecurityID_NULL;
	m_LegSecurityIDSource = fields::LegSecurityIDSource_NULL;
	m_LegSecAltIDGrp.reset();
	m_LegProduct = fields::NULL_INT;
	m_LegCFICode = fields::LegCFICode_NULL;
	m_LegSecurityType = fields::LegSecurityType_NULL;
	m_LegSecuritySubType = fields::LegSecuritySubType_NULL;
	m_LegMaturityMonthYear = fields::LegMaturityMonthYear_NULL;
	m_LegMaturityDate = fields::NULL_LOCALMKTDATE;
	m_LegCouponPaymentDate = fields::NULL_LOCALMKTDATE;
	m_LegIssueDate = fields::NULL_LOCALMKTDATE;
	m_LegRepoCollateralSecurityType = fields::LegRepoCollateralSecurityType_NULL;
	m_LegRepurchaseTerm = fields::NULL_INT;
	m_LegRepurchaseRate = fields::NULL_PERCENTAGE;
	m_LegFactor = fields::NULL_FLOAT;
	m_LegCreditRating = fields::LegCreditRating_NULL;
	m_LegInstrRegistry = fields::LegInstrRegistry_NULL;
	m_LegCountryOfIssue = fields::LegCountryOfIssue_NULL;
	m_LegStateOrProvinceOfIssue = fields::LegStateOrProvinceOfIssue_NULL;
	m_LegLocaleOfIssue = fields::LegLocaleOfIssue_NULL;
	m_LegRedemptionDate = fields::NULL_LOCALMKTDATE;
	m_LegStrikePrice = fields::NULL_PRICE;
	m_LegStrikeCurrency = fields::LegStrikeCurrency_NULL;
	m_LegOptAttribute = fields::NULL_CHAR;
	m_LegContractMultiplier = fields::NULL_FLOAT;
	m_LegCouponRate = fields::NULL_PERCENTAGE;
	m_LegSecurityExchange = fields::LegSecurityExchange_NULL;
	m_LegIssuer = fields::LegIssuer_NULL;
	m_EncodedLegIssuerLen = fields::NULL_LENGTH;
	m_EncodedLegIssuer = fields::EncodedLegIssuer_NULL;
	m_LegSecurityDesc = fields::LegSecurityDesc_NULL;
	m_EncodedLegSecurityDescLen = fields::NULL_LENGTH;
	m_EncodedLegSecurityDesc = fields::EncodedLegSecurityDesc_NULL;
	m_LegRatioQty = fields::NULL_FLOAT;
	m_LegSide = fields::NULL_CHAR;
	m_LegCurrency = fields::LegCurrency_NULL;
	m_LegPool = fields::LegPool_NULL;
	m_LegDatedDate = fields::NULL_LOCALMKTDATE;
	m_LegContractSettlMonth = fields::LegContractSettlMonth_NULL;
	m_LegInterestAccrualDate = fields::NULL_LOCALMKTDATE;
	m_presence = 0;
}

bool InstrumentLeg::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_LegSecAltIDGrp.hasAnySet() || m_LegSecAltIDGrp.checkRequired());
}

bool InstrumentLeg::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool InstrumentLeg::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoLegSecurityAltID:
			p = tag_start;
			if (!m_LegSecAltIDGrp.decode(p, end, stop_tag, stop_on_repetition)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return true;
}

char *InstrumentLeg::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasLegSymbol()) { WRITE_TAG4(fix44::tag_const::LegSymbol); std::memcpy(p, m_LegSymbol.data(), m_LegSymbol.size()); p += m_LegSymbol.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegSymbolSfx()) { WRITE_TAG4(fix44::tag_const::LegSymbolSfx); std::memcpy(p, m_LegSymbolSfx.data(), m_LegSymbolSfx.size()); p += m_LegSymbolSfx.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegSecurityID()) { WRITE_TAG4(fix44::tag_const::LegSecurityID); std::memcpy(p, m_LegSecurityID.data(), m_LegSecurityID.size()); p += m_LegSecurityID.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegSecurityIDSource()) { WRITE_TAG4(fix44::tag_const::LegSecurityIDSource); std::memcpy(p, m_LegSecurityIDSource.data(), m_LegSecurityIDSource.size()); p += m_LegSecurityIDSource.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_LegSecAltIDGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasLegProduct()) { WRITE_TAG4(fix44::tag_const::LegProduct); p += falconfix::itoa(m_LegProduct, p); *p++ = fix44::serialize::SOH; }
	if (hasLegCFICode()) { WRITE_TAG4(fix44::tag_const::LegCFICode); std::memcpy(p, m_LegCFICode.data(), m_LegCFICode.size()); p += m_LegCFICode.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegSecurityType()) { WRITE_TAG4(fix44::tag_const::LegSecurityType); std::memcpy(p, m_LegSecurityType.data(), m_LegSecurityType.size()); p += m_LegSecurityType.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegSecuritySubType()) { WRITE_TAG4(fix44::tag_const::LegSecuritySubType); std::memcpy(p, m_LegSecuritySubType.data(), m_LegSecuritySubType.size()); p += m_LegSecuritySubType.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegMaturityMonthYear()) { WRITE_TAG4(fix44::tag_const::LegMaturityMonthYear); std::memcpy(p, m_LegMaturityMonthYear.data(), m_LegMaturityMonthYear.size()); p += m_LegMaturityMonthYear.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegMaturityDate()) { WRITE_TAG4(fix44::tag_const::LegMaturityDate); p += fix44::datetime::format_UTCDateOnly(m_LegMaturityDate, p); *p++ = fix44::serialize::SOH; }
	if (hasLegCouponPaymentDate()) { WRITE_TAG4(fix44::tag_const::LegCouponPaymentDate); p += fix44::datetime::format_UTCDateOnly(m_LegCouponPaymentDate, p); *p++ = fix44::serialize::SOH; }
	if (hasLegIssueDate()) { WRITE_TAG4(fix44::tag_const::LegIssueDate); p += fix44::datetime::format_UTCDateOnly(m_LegIssueDate, p); *p++ = fix44::serialize::SOH; }
	if (hasLegRepoCollateralSecurityType()) { WRITE_TAG4(fix44::tag_const::LegRepoCollateralSecurityType); std::memcpy(p, m_LegRepoCollateralSecurityType.data(), m_LegRepoCollateralSecurityType.size()); p += m_LegRepoCollateralSecurityType.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegRepurchaseTerm()) { WRITE_TAG4(fix44::tag_const::LegRepurchaseTerm); p += falconfix::itoa(m_LegRepurchaseTerm, p); *p++ = fix44::serialize::SOH; }
	if (hasLegRepurchaseRate()) { WRITE_TAG4(fix44::tag_const::LegRepurchaseRate); p += falconfix::dtoa(m_LegRepurchaseRate, p); *p++ = fix44::serialize::SOH; }
	if (hasLegFactor()) { WRITE_TAG4(fix44::tag_const::LegFactor); p += falconfix::dtoa(m_LegFactor, p); *p++ = fix44::serialize::SOH; }
	if (hasLegCreditRating()) { WRITE_TAG4(fix44::tag_const::LegCreditRating); std::memcpy(p, m_LegCreditRating.data(), m_LegCreditRating.size()); p += m_LegCreditRating.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegInstrRegistry()) { WRITE_TAG4(fix44::tag_const::LegInstrRegistry); std::memcpy(p, m_LegInstrRegistry.data(), m_LegInstrRegistry.size()); p += m_LegInstrRegistry.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegCountryOfIssue()) { WRITE_TAG4(fix44::tag_const::LegCountryOfIssue); std::memcpy(p, m_LegCountryOfIssue.data(), m_LegCountryOfIssue.size()); p += m_LegCountryOfIssue.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegStateOrProvinceOfIssue()) { WRITE_TAG4(fix44::tag_const::LegStateOrProvinceOfIssue); std::memcpy(p, m_LegStateOrProvinceOfIssue.data(), m_LegStateOrProvinceOfIssue.size()); p += m_LegStateOrProvinceOfIssue.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegLocaleOfIssue()) { WRITE_TAG4(fix44::tag_const::LegLocaleOfIssue); std::memcpy(p, m_LegLocaleOfIssue.data(), m_LegLocaleOfIssue.size()); p += m_LegLocaleOfIssue.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegRedemptionDate()) { WRITE_TAG4(fix44::tag_const::LegRedemptionDate); p += fix44::datetime::format_UTCDateOnly(m_LegRedemptionDate, p); *p++ = fix44::serialize::SOH; }
	if (hasLegStrikePrice()) { WRITE_TAG4(fix44::tag_const::LegStrikePrice); p += falconfix::dtoa(m_LegStrikePrice, p); *p++ = fix44::serialize::SOH; }
	if (hasLegStrikeCurrency()) { WRITE_TAG4(fix44::tag_const::LegStrikeCurrency); std::memcpy(p, m_LegStrikeCurrency.data(), m_LegStrikeCurrency.size()); p += m_LegStrikeCurrency.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegOptAttribute()) { WRITE_TAG4(fix44::tag_const::LegOptAttribute); *p++ = m_LegOptAttribute; *p++ = fix44::serialize::SOH; }
	if (hasLegContractMultiplier()) { WRITE_TAG4(fix44::tag_const::LegContractMultiplier); p += falconfix::dtoa(m_LegContractMultiplier, p); *p++ = fix44::serialize::SOH; }
	if (hasLegCouponRate()) { WRITE_TAG4(fix44::tag_const::LegCouponRate); p += falconfix::dtoa(m_LegCouponRate, p); *p++ = fix44::serialize::SOH; }
	if (hasLegSecurityExchange()) { WRITE_TAG4(fix44::tag_const::LegSecurityExchange); std::memcpy(p, m_LegSecurityExchange.data(), m_LegSecurityExchange.size()); p += m_LegSecurityExchange.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegIssuer()) { WRITE_TAG4(fix44::tag_const::LegIssuer); std::memcpy(p, m_LegIssuer.data(), m_LegIssuer.size()); p += m_LegIssuer.size(); *p++ = fix44::serialize::SOH; }
	if (hasEncodedLegIssuerLen()) { WRITE_TAG4(fix44::tag_const::EncodedLegIssuerLen); p += falconfix::itoa(m_EncodedLegIssuerLen, p); *p++ = fix44::serialize::SOH; }
	if (hasEncodedLegIssuer()) { WRITE_TAG4(fix44::tag_const::EncodedLegIssuer); std::memcpy(p, m_EncodedLegIssuer.data(), m_EncodedLegIssuer.size()); p += m_EncodedLegIssuer.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegSecurityDesc()) { WRITE_TAG4(fix44::tag_const::LegSecurityDesc); std::memcpy(p, m_LegSecurityDesc.data(), m_LegSecurityDesc.size()); p += m_LegSecurityDesc.size(); *p++ = fix44::serialize::SOH; }
	if (hasEncodedLegSecurityDescLen()) { WRITE_TAG4(fix44::tag_const::EncodedLegSecurityDescLen); p += falconfix::itoa(m_EncodedLegSecurityDescLen, p); *p++ = fix44::serialize::SOH; }
	if (hasEncodedLegSecurityDesc()) { WRITE_TAG4(fix44::tag_const::EncodedLegSecurityDesc); std::memcpy(p, m_EncodedLegSecurityDesc.data(), m_EncodedLegSecurityDesc.size()); p += m_EncodedLegSecurityDesc.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegRatioQty()) { WRITE_TAG4(fix44::tag_const::LegRatioQty); p += falconfix::dtoa(m_LegRatioQty, p); *p++ = fix44::serialize::SOH; }
	if (hasLegSide()) { WRITE_TAG4(fix44::tag_const::LegSide); *p++ = m_LegSide; *p++ = fix44::serialize::SOH; }
	if (hasLegCurrency()) { WRITE_TAG4(fix44::tag_const::LegCurrency); std::memcpy(p, m_LegCurrency.data(), m_LegCurrency.size()); p += m_LegCurrency.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegPool()) { WRITE_TAG4(fix44::tag_const::LegPool); std::memcpy(p, m_LegPool.data(), m_LegPool.size()); p += m_LegPool.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegDatedDate()) { WRITE_TAG4(fix44::tag_const::LegDatedDate); p += fix44::datetime::format_UTCDateOnly(m_LegDatedDate, p); *p++ = fix44::serialize::SOH; }
	if (hasLegContractSettlMonth()) { WRITE_TAG4(fix44::tag_const::LegContractSettlMonth); std::memcpy(p, m_LegContractSettlMonth.data(), m_LegContractSettlMonth.size()); p += m_LegContractSettlMonth.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegInterestAccrualDate()) { WRITE_TAG4(fix44::tag_const::LegInterestAccrualDate); p += fix44::datetime::format_UTCDateOnly(m_LegInterestAccrualDate, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool InstrumentLeg::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::LegSymbol: setLegSymbol({begin, len}); return true;
	case field_id::LegSymbolSfx: setLegSymbolSfx({begin, len}); return true;
	case field_id::LegSecurityID: setLegSecurityID({begin, len}); return true;
	case field_id::LegSecurityIDSource: setLegSecurityIDSource({begin, len}); return true;
	case field_id::LegProduct: setLegProduct(falconfix::atoi(begin, end)); return true;
	case field_id::LegCFICode: setLegCFICode({begin, len}); return true;
	case field_id::LegSecurityType: setLegSecurityType({begin, len}); return true;
	case field_id::LegSecuritySubType: setLegSecuritySubType({begin, len}); return true;
	case field_id::LegMaturityMonthYear: setLegMaturityMonthYear({begin, len}); return true;
	case field_id::LegMaturityDate: setLegMaturityDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::LegCouponPaymentDate: setLegCouponPaymentDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::LegIssueDate: setLegIssueDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::LegRepoCollateralSecurityType: setLegRepoCollateralSecurityType({begin, len}); return true;
	case field_id::LegRepurchaseTerm: setLegRepurchaseTerm(falconfix::atoi(begin, end)); return true;
	case field_id::LegRepurchaseRate: setLegRepurchaseRate(fix44::parser::parse_double(begin, end)); return true;
	case field_id::LegFactor: setLegFactor(fix44::parser::parse_double(begin, end)); return true;
	case field_id::LegCreditRating: setLegCreditRating({begin, len}); return true;
	case field_id::LegInstrRegistry: setLegInstrRegistry({begin, len}); return true;
	case field_id::LegCountryOfIssue: setLegCountryOfIssue({begin, len}); return true;
	case field_id::LegStateOrProvinceOfIssue: setLegStateOrProvinceOfIssue({begin, len}); return true;
	case field_id::LegLocaleOfIssue: setLegLocaleOfIssue({begin, len}); return true;
	case field_id::LegRedemptionDate: setLegRedemptionDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::LegStrikePrice: setLegStrikePrice(fix44::parser::parse_double(begin, end)); return true;
	case field_id::LegStrikeCurrency: setLegStrikeCurrency({begin, len}); return true;
	case field_id::LegOptAttribute: setLegOptAttribute(fix44::parser::parse_char(begin, end)); return true;
	case field_id::LegContractMultiplier: setLegContractMultiplier(fix44::parser::parse_double(begin, end)); return true;
	case field_id::LegCouponRate: setLegCouponRate(fix44::parser::parse_double(begin, end)); return true;
	case field_id::LegSecurityExchange: setLegSecurityExchange({begin, len}); return true;
	case field_id::LegIssuer: setLegIssuer({begin, len}); return true;
	case field_id::EncodedLegIssuerLen: setEncodedLegIssuerLen(falconfix::atoi(begin, end)); return true;
	case field_id::EncodedLegIssuer: setEncodedLegIssuer({begin, len}); return true;
	case field_id::LegSecurityDesc: setLegSecurityDesc({begin, len}); return true;
	case field_id::EncodedLegSecurityDescLen: setEncodedLegSecurityDescLen(falconfix::atoi(begin, end)); return true;
	case field_id::EncodedLegSecurityDesc: setEncodedLegSecurityDesc({begin, len}); return true;
	case field_id::LegRatioQty: setLegRatioQty(fix44::parser::parse_double(begin, end)); return true;
	case field_id::LegSide: setLegSide(fix44::parser::parse_char(begin, end)); return true;
	case field_id::LegCurrency: setLegCurrency({begin, len}); return true;
	case field_id::LegPool: setLegPool({begin, len}); return true;
	case field_id::LegDatedDate: setLegDatedDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::LegContractSettlMonth: setLegContractSettlMonth({begin, len}); return true;
	case field_id::LegInterestAccrualDate: setLegInterestAccrualDate(fix44::parser::parse_date32(begin, end)); return true;
	default: return false;
	}
}

} // namespace fix44::components
