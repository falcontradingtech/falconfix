// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 8bc69d24e2d69b3b

#include <FIX4.4/components/PositionQty.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void PositionQty::NoPositions::reset() noexcept {
	m_PosType = fields::PosType_NULL;
	m_LongQty = fields::NULL_QTY;
	m_ShortQty = fields::NULL_QTY;
	m_PosQtyStatus = fields::NULL_INT;
	m_NestedParties.reset();
	m_presence = 0;
}

bool PositionQty::NoPositions::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_NestedParties.hasAnySet() || m_NestedParties.checkRequired());
}

bool PositionQty::NoPositions::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::PosType) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoNestedPartyIDs:
			p = tag_start;
			if (!m_NestedParties.decode(p, end, field_id::PosType, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *PositionQty::NoPositions::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasPosType()) { WRITE_TAG4(fix44::tag_const::PosType); std::memcpy(p, m_PosType.data(), m_PosType.size()); p += m_PosType.size(); *p++ = fix44::serialize::SOH; }
	if (hasLongQty()) { WRITE_TAG4(fix44::tag_const::LongQty); p += falconfix::dtoa(m_LongQty, p); *p++ = fix44::serialize::SOH; }
	if (hasShortQty()) { WRITE_TAG4(fix44::tag_const::ShortQty); p += falconfix::dtoa(m_ShortQty, p); *p++ = fix44::serialize::SOH; }
	if (hasPosQtyStatus()) { WRITE_TAG4(fix44::tag_const::PosQtyStatus); p += falconfix::itoa(m_PosQtyStatus, p); *p++ = fix44::serialize::SOH; }
	if ((p = m_NestedParties.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool PositionQty::NoPositions::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::PosType: setPosType({begin, len}); return true;
	case field_id::LongQty: setLongQty(fix44::parser::parse_double(begin, end)); return true;
	case field_id::ShortQty: setShortQty(fix44::parser::parse_double(begin, end)); return true;
	case field_id::PosQtyStatus: setPosQtyStatus(falconfix::atoi(begin, end)); return true;
	default: return false;
	}
}

void PositionQty::reset() noexcept {
	m_NoPositionsGroup.clear();
	m_presence = 0;
}

bool PositionQty::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoPositionsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool PositionQty::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool PositionQty::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoPositions: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoPositionsGroup.clear();
			m_NoPositionsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoPositionsGroup.emplace_back();
				if (!m_NoPositionsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *PositionQty::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoPositionsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoPositions);
		p += falconfix::itoa(static_cast<int64_t>(m_NoPositionsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoPositionsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool PositionQty::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
