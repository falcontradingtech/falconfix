// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: c3f5269e04384be1

#include <FIX4.4/components/SecTypesGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void SecTypesGrp::NoSecurityTypes::reset() noexcept {
	m_SecurityType = fields::SecurityType_NULL;
	m_SecuritySubType = fields::SecuritySubType_NULL;
	m_Product = fields::NULL_INT;
	m_CFICode = fields::CFICode_NULL;
	m_presence = 0;
}

bool SecTypesGrp::NoSecurityTypes::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool SecTypesGrp::NoSecurityTypes::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::SecurityType) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *SecTypesGrp::NoSecurityTypes::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasSecurityType()) { WRITE_TAG4(fix44::tag_const::SecurityType); std::memcpy(p, m_SecurityType.data(), m_SecurityType.size()); p += m_SecurityType.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecuritySubType()) { WRITE_TAG4(fix44::tag_const::SecuritySubType); std::memcpy(p, m_SecuritySubType.data(), m_SecuritySubType.size()); p += m_SecuritySubType.size(); *p++ = fix44::serialize::SOH; }
	if (hasProduct()) { WRITE_TAG4(fix44::tag_const::Product); p += falconfix::itoa(m_Product, p); *p++ = fix44::serialize::SOH; }
	if (hasCFICode()) { WRITE_TAG4(fix44::tag_const::CFICode); std::memcpy(p, m_CFICode.data(), m_CFICode.size()); p += m_CFICode.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool SecTypesGrp::NoSecurityTypes::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::SecurityType: setSecurityType({begin, len}); return true;
	case field_id::SecuritySubType: setSecuritySubType({begin, len}); return true;
	case field_id::Product: setProduct(falconfix::atoi(begin, end)); return true;
	case field_id::CFICode: setCFICode({begin, len}); return true;
	default: return false;
	}
}

void SecTypesGrp::reset() noexcept {
	m_NoSecurityTypesGroup.clear();
	m_presence = 0;
}

bool SecTypesGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoSecurityTypesGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool SecTypesGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool SecTypesGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoSecurityTypes: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoSecurityTypesGroup.clear();
			m_NoSecurityTypesGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoSecurityTypesGroup.emplace_back();
				if (!m_NoSecurityTypesGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *SecTypesGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoSecurityTypesGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoSecurityTypes);
		p += falconfix::itoa(static_cast<int64_t>(m_NoSecurityTypesGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoSecurityTypesGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool SecTypesGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
