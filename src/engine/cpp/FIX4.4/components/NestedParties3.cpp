// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: cdd3cf0d9dadb9db

#include <FIX4.4/components/NestedParties3.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void NestedParties3::NoNested3PartyIDs::reset() noexcept {
	m_Nested3PartyID = fields::Nested3PartyID_NULL;
	m_Nested3PartyIDSource = fields::NULL_CHAR;
	m_Nested3PartyRole = fields::NULL_INT;
	m_NstdPtys3SubGrp.reset();
	m_presence = 0;
}

bool NestedParties3::NoNested3PartyIDs::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_NstdPtys3SubGrp.hasAnySet() || m_NstdPtys3SubGrp.checkRequired());
}

bool NestedParties3::NoNested3PartyIDs::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::Nested3PartyID) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoNested3PartySubIDs:
			p = tag_start;
			if (!m_NstdPtys3SubGrp.decode(p, end, field_id::Nested3PartyID, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *NestedParties3::NoNested3PartyIDs::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasNested3PartyID()) { WRITE_TAG4(fix44::tag_const::Nested3PartyID); std::memcpy(p, m_Nested3PartyID.data(), m_Nested3PartyID.size()); p += m_Nested3PartyID.size(); *p++ = fix44::serialize::SOH; }
	if (hasNested3PartyIDSource()) { WRITE_TAG4(fix44::tag_const::Nested3PartyIDSource); *p++ = m_Nested3PartyIDSource; *p++ = fix44::serialize::SOH; }
	if (hasNested3PartyRole()) { WRITE_TAG4(fix44::tag_const::Nested3PartyRole); p += falconfix::itoa(m_Nested3PartyRole, p); *p++ = fix44::serialize::SOH; }
	if ((p = m_NstdPtys3SubGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool NestedParties3::NoNested3PartyIDs::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::Nested3PartyID: setNested3PartyID({begin, len}); return true;
	case field_id::Nested3PartyIDSource: setNested3PartyIDSource(fix44::parser::parse_char(begin, end)); return true;
	case field_id::Nested3PartyRole: setNested3PartyRole(falconfix::atoi(begin, end)); return true;
	default: return false;
	}
}

void NestedParties3::reset() noexcept {
	m_NoNested3PartyIDsGroup.clear();
	m_presence = 0;
}

bool NestedParties3::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoNested3PartyIDsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool NestedParties3::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool NestedParties3::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoNested3PartyIDs: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoNested3PartyIDsGroup.clear();
			m_NoNested3PartyIDsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoNested3PartyIDsGroup.emplace_back();
				if (!m_NoNested3PartyIDsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *NestedParties3::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoNested3PartyIDsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoNested3PartyIDs);
		p += falconfix::itoa(static_cast<int64_t>(m_NoNested3PartyIDsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoNested3PartyIDsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool NestedParties3::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
