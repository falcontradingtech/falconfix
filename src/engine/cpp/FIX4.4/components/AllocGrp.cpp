// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 7b80dab9d9ec2883

#include <FIX4.4/components/AllocGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void AllocGrp::NoAllocs::reset() noexcept {
	m_AllocAccount = fields::AllocAccount_NULL;
	m_AllocAcctIDSource = fields::NULL_INT;
	m_MatchStatus = fields::NULL_CHAR;
	m_AllocPrice = fields::NULL_PRICE;
	m_AllocQty = fields::NULL_QTY;
	m_IndividualAllocID = fields::IndividualAllocID_NULL;
	m_ProcessCode = fields::NULL_CHAR;
	m_NestedParties.reset();
	m_NotifyBrokerOfCredit = fields::NULL_BOOLEAN;
	m_AllocHandlInst = fields::NULL_INT;
	m_AllocText = fields::AllocText_NULL;
	m_EncodedAllocTextLen = fields::NULL_LENGTH;
	m_EncodedAllocText = fields::EncodedAllocText_NULL;
	m_CommissionData.reset();
	m_AllocAvgPx = fields::NULL_PRICE;
	m_AllocNetMoney = fields::NULL_AMT;
	m_SettlCurrAmt = fields::NULL_AMT;
	m_AllocSettlCurrAmt = fields::NULL_AMT;
	m_SettlCurrency = fields::SettlCurrency_NULL;
	m_AllocSettlCurrency = fields::AllocSettlCurrency_NULL;
	m_SettlCurrFxRate = fields::NULL_FLOAT;
	m_SettlCurrFxRateCalc = fields::NULL_CHAR;
	m_AllocAccruedInterestAmt = fields::NULL_AMT;
	m_AllocInterestAtMaturity = fields::NULL_AMT;
	m_MiscFeesGrp.reset();
	m_ClrInstGrp.reset();
	m_AllocSettlInstType = fields::NULL_INT;
	m_SettlInstructionsData.reset();
	m_presence = 0;
}

bool AllocGrp::NoAllocs::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_NestedParties.hasAnySet() || m_NestedParties.checkRequired()) &&
			(!m_CommissionData.hasAnySet() || m_CommissionData.checkRequired()) &&
			(!m_MiscFeesGrp.hasAnySet() || m_MiscFeesGrp.checkRequired()) &&
			(!m_ClrInstGrp.hasAnySet() || m_ClrInstGrp.checkRequired()) &&
			(!m_SettlInstructionsData.hasAnySet() || m_SettlInstructionsData.checkRequired());
}

bool AllocGrp::NoAllocs::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::AllocAccount) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoNestedPartyIDs:
			p = tag_start;
			if (!m_NestedParties.decode(p, end, field_id::AllocAccount, true)) { return false; }
			consumed = true;
			continue;
		case field_id::Commission:
			p = tag_start;
			if (!m_CommissionData.decode(p, end, field_id::AllocAccount, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoMiscFees:
			p = tag_start;
			if (!m_MiscFeesGrp.decode(p, end, field_id::AllocAccount, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoClearingInstructions:
			p = tag_start;
			if (!m_ClrInstGrp.decode(p, end, field_id::AllocAccount, true)) { return false; }
			consumed = true;
			continue;
		case field_id::SettlDeliveryType:
			p = tag_start;
			if (!m_SettlInstructionsData.decode(p, end, field_id::AllocAccount, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *AllocGrp::NoAllocs::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasAllocAccount()) { WRITE_TAG3(fix44::tag_const::AllocAccount); std::memcpy(p, m_AllocAccount.data(), m_AllocAccount.size()); p += m_AllocAccount.size(); *p++ = fix44::serialize::SOH; }
	if (hasAllocAcctIDSource()) { WRITE_TAG4(fix44::tag_const::AllocAcctIDSource); p += falconfix::itoa(m_AllocAcctIDSource, p); *p++ = fix44::serialize::SOH; }
	if (hasMatchStatus()) { WRITE_TAG4(fix44::tag_const::MatchStatus); *p++ = m_MatchStatus; *p++ = fix44::serialize::SOH; }
	if (hasAllocPrice()) { WRITE_TAG4(fix44::tag_const::AllocPrice); p += falconfix::dtoa(m_AllocPrice, p); *p++ = fix44::serialize::SOH; }
	if (hasAllocQty()) { WRITE_TAG3(fix44::tag_const::AllocQty); p += falconfix::dtoa(m_AllocQty, p); *p++ = fix44::serialize::SOH; }
	if (hasIndividualAllocID()) { WRITE_TAG4(fix44::tag_const::IndividualAllocID); std::memcpy(p, m_IndividualAllocID.data(), m_IndividualAllocID.size()); p += m_IndividualAllocID.size(); *p++ = fix44::serialize::SOH; }
	if (hasProcessCode()) { WRITE_TAG3(fix44::tag_const::ProcessCode); *p++ = m_ProcessCode; *p++ = fix44::serialize::SOH; }
	if ((p = m_NestedParties.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasNotifyBrokerOfCredit()) { WRITE_TAG4(fix44::tag_const::NotifyBrokerOfCredit); *p++ = m_NotifyBrokerOfCredit ? 'Y' : 'N'; *p++ = fix44::serialize::SOH; }
	if (hasAllocHandlInst()) { WRITE_TAG4(fix44::tag_const::AllocHandlInst); p += falconfix::itoa(m_AllocHandlInst, p); *p++ = fix44::serialize::SOH; }
	if (hasAllocText()) { WRITE_TAG4(fix44::tag_const::AllocText); std::memcpy(p, m_AllocText.data(), m_AllocText.size()); p += m_AllocText.size(); *p++ = fix44::serialize::SOH; }
	if (hasEncodedAllocTextLen()) { WRITE_TAG4(fix44::tag_const::EncodedAllocTextLen); p += falconfix::itoa(m_EncodedAllocTextLen, p); *p++ = fix44::serialize::SOH; }
	if (hasEncodedAllocText()) { WRITE_TAG4(fix44::tag_const::EncodedAllocText); std::memcpy(p, m_EncodedAllocText.data(), m_EncodedAllocText.size()); p += m_EncodedAllocText.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_CommissionData.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasAllocAvgPx()) { WRITE_TAG4(fix44::tag_const::AllocAvgPx); p += falconfix::dtoa(m_AllocAvgPx, p); *p++ = fix44::serialize::SOH; }
	if (hasAllocNetMoney()) { WRITE_TAG4(fix44::tag_const::AllocNetMoney); p += falconfix::dtoa(m_AllocNetMoney, p); *p++ = fix44::serialize::SOH; }
	if (hasSettlCurrAmt()) { WRITE_TAG4(fix44::tag_const::SettlCurrAmt); p += falconfix::dtoa(m_SettlCurrAmt, p); *p++ = fix44::serialize::SOH; }
	if (hasAllocSettlCurrAmt()) { WRITE_TAG4(fix44::tag_const::AllocSettlCurrAmt); p += falconfix::dtoa(m_AllocSettlCurrAmt, p); *p++ = fix44::serialize::SOH; }
	if (hasSettlCurrency()) { WRITE_TAG4(fix44::tag_const::SettlCurrency); std::memcpy(p, m_SettlCurrency.data(), m_SettlCurrency.size()); p += m_SettlCurrency.size(); *p++ = fix44::serialize::SOH; }
	if (hasAllocSettlCurrency()) { WRITE_TAG4(fix44::tag_const::AllocSettlCurrency); std::memcpy(p, m_AllocSettlCurrency.data(), m_AllocSettlCurrency.size()); p += m_AllocSettlCurrency.size(); *p++ = fix44::serialize::SOH; }
	if (hasSettlCurrFxRate()) { WRITE_TAG4(fix44::tag_const::SettlCurrFxRate); p += falconfix::dtoa(m_SettlCurrFxRate, p); *p++ = fix44::serialize::SOH; }
	if (hasSettlCurrFxRateCalc()) { WRITE_TAG4(fix44::tag_const::SettlCurrFxRateCalc); *p++ = m_SettlCurrFxRateCalc; *p++ = fix44::serialize::SOH; }
	if (hasAllocAccruedInterestAmt()) { WRITE_TAG4(fix44::tag_const::AllocAccruedInterestAmt); p += falconfix::dtoa(m_AllocAccruedInterestAmt, p); *p++ = fix44::serialize::SOH; }
	if (hasAllocInterestAtMaturity()) { WRITE_TAG4(fix44::tag_const::AllocInterestAtMaturity); p += falconfix::dtoa(m_AllocInterestAtMaturity, p); *p++ = fix44::serialize::SOH; }
	if ((p = m_MiscFeesGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if ((p = m_ClrInstGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasAllocSettlInstType()) { WRITE_TAG4(fix44::tag_const::AllocSettlInstType); p += falconfix::itoa(m_AllocSettlInstType, p); *p++ = fix44::serialize::SOH; }
	if ((p = m_SettlInstructionsData.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool AllocGrp::NoAllocs::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::AllocAccount: setAllocAccount({begin, len}); return true;
	case field_id::AllocAcctIDSource: setAllocAcctIDSource(falconfix::atoi(begin, end)); return true;
	case field_id::MatchStatus: setMatchStatus(fix44::parser::parse_char(begin, end)); return true;
	case field_id::AllocPrice: setAllocPrice(fix44::parser::parse_double(begin, end)); return true;
	case field_id::AllocQty: setAllocQty(fix44::parser::parse_double(begin, end)); return true;
	case field_id::IndividualAllocID: setIndividualAllocID({begin, len}); return true;
	case field_id::ProcessCode: setProcessCode(fix44::parser::parse_char(begin, end)); return true;
	case field_id::NotifyBrokerOfCredit: setNotifyBrokerOfCredit(fix44::parser::parse_bool(begin, end)); return true;
	case field_id::AllocHandlInst: setAllocHandlInst(falconfix::atoi(begin, end)); return true;
	case field_id::AllocText: setAllocText({begin, len}); return true;
	case field_id::EncodedAllocTextLen: setEncodedAllocTextLen(falconfix::atoi(begin, end)); return true;
	case field_id::EncodedAllocText: setEncodedAllocText({begin, len}); return true;
	case field_id::AllocAvgPx: setAllocAvgPx(fix44::parser::parse_double(begin, end)); return true;
	case field_id::AllocNetMoney: setAllocNetMoney(fix44::parser::parse_double(begin, end)); return true;
	case field_id::SettlCurrAmt: setSettlCurrAmt(fix44::parser::parse_double(begin, end)); return true;
	case field_id::AllocSettlCurrAmt: setAllocSettlCurrAmt(fix44::parser::parse_double(begin, end)); return true;
	case field_id::SettlCurrency: setSettlCurrency({begin, len}); return true;
	case field_id::AllocSettlCurrency: setAllocSettlCurrency({begin, len}); return true;
	case field_id::SettlCurrFxRate: setSettlCurrFxRate(fix44::parser::parse_double(begin, end)); return true;
	case field_id::SettlCurrFxRateCalc: setSettlCurrFxRateCalc(fix44::parser::parse_char(begin, end)); return true;
	case field_id::AllocAccruedInterestAmt: setAllocAccruedInterestAmt(fix44::parser::parse_double(begin, end)); return true;
	case field_id::AllocInterestAtMaturity: setAllocInterestAtMaturity(fix44::parser::parse_double(begin, end)); return true;
	case field_id::AllocSettlInstType: setAllocSettlInstType(falconfix::atoi(begin, end)); return true;
	default: return false;
	}
}

void AllocGrp::reset() noexcept {
	m_NoAllocsGroup.clear();
	m_presence = 0;
}

bool AllocGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoAllocsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool AllocGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool AllocGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoAllocs: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoAllocsGroup.clear();
			m_NoAllocsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoAllocsGroup.emplace_back();
				if (!m_NoAllocsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *AllocGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoAllocsGroup.empty()) {
		WRITE_TAG3(fix44::tag_const::NoAllocs);
		p += falconfix::itoa(static_cast<int64_t>(m_NoAllocsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoAllocsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool AllocGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
