// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 00e4980a81e3924a

#include <FIX4.4/components/AffectedOrdGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void AffectedOrdGrp::NoAffectedOrders::reset() noexcept {
	m_OrigClOrdID = fields::OrigClOrdID_NULL;
	m_AffectedOrderID = fields::AffectedOrderID_NULL;
	m_AffectedSecondaryOrderID = fields::AffectedSecondaryOrderID_NULL;
	m_presence = 0;
}

bool AffectedOrdGrp::NoAffectedOrders::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool AffectedOrdGrp::NoAffectedOrders::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::OrigClOrdID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *AffectedOrdGrp::NoAffectedOrders::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasOrigClOrdID()) { WRITE_TAG3(fix44::tag_const::OrigClOrdID); std::memcpy(p, m_OrigClOrdID.data(), m_OrigClOrdID.size()); p += m_OrigClOrdID.size(); *p++ = fix44::serialize::SOH; }
	if (hasAffectedOrderID()) { WRITE_TAG4(fix44::tag_const::AffectedOrderID); std::memcpy(p, m_AffectedOrderID.data(), m_AffectedOrderID.size()); p += m_AffectedOrderID.size(); *p++ = fix44::serialize::SOH; }
	if (hasAffectedSecondaryOrderID()) { WRITE_TAG4(fix44::tag_const::AffectedSecondaryOrderID); std::memcpy(p, m_AffectedSecondaryOrderID.data(), m_AffectedSecondaryOrderID.size()); p += m_AffectedSecondaryOrderID.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool AffectedOrdGrp::NoAffectedOrders::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::OrigClOrdID: setOrigClOrdID({begin, len}); return true;
	case field_id::AffectedOrderID: setAffectedOrderID({begin, len}); return true;
	case field_id::AffectedSecondaryOrderID: setAffectedSecondaryOrderID({begin, len}); return true;
	default: return false;
	}
}

void AffectedOrdGrp::reset() noexcept {
	m_NoAffectedOrdersGroup.clear();
	m_presence = 0;
}

bool AffectedOrdGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoAffectedOrdersGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool AffectedOrdGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool AffectedOrdGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoAffectedOrders: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoAffectedOrdersGroup.clear();
			m_NoAffectedOrdersGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoAffectedOrdersGroup.emplace_back();
				if (!m_NoAffectedOrdersGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *AffectedOrdGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoAffectedOrdersGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoAffectedOrders);
		p += falconfix::itoa(static_cast<int64_t>(m_NoAffectedOrdersGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoAffectedOrdersGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool AffectedOrdGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
