// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 3b2c5176a5179304

#include <FIX4.4/components/RoutingGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void RoutingGrp::NoRoutingIDs::reset() noexcept {
	m_RoutingType = fields::NULL_INT;
	m_RoutingID = fields::RoutingID_NULL;
	m_presence = 0;
}

bool RoutingGrp::NoRoutingIDs::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool RoutingGrp::NoRoutingIDs::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::RoutingType) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *RoutingGrp::NoRoutingIDs::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasRoutingType()) { WRITE_TAG4(fix44::tag_const::RoutingType); p += falconfix::itoa(m_RoutingType, p); *p++ = fix44::serialize::SOH; }
	if (hasRoutingID()) { WRITE_TAG4(fix44::tag_const::RoutingID); std::memcpy(p, m_RoutingID.data(), m_RoutingID.size()); p += m_RoutingID.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool RoutingGrp::NoRoutingIDs::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::RoutingType: setRoutingType(falconfix::atoi(begin, end)); return true;
	case field_id::RoutingID: setRoutingID({begin, len}); return true;
	default: return false;
	}
}

void RoutingGrp::reset() noexcept {
	m_NoRoutingIDsGroup.clear();
	m_presence = 0;
}

bool RoutingGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoRoutingIDsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool RoutingGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool RoutingGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoRoutingIDs: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoRoutingIDsGroup.clear();
			m_NoRoutingIDsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoRoutingIDsGroup.emplace_back();
				if (!m_NoRoutingIDsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *RoutingGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoRoutingIDsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoRoutingIDs);
		p += falconfix::itoa(static_cast<int64_t>(m_NoRoutingIDsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoRoutingIDsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool RoutingGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
