// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: ecb24a8d67c2a46d

#include <FIX4.4/components/CompIDReqGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void CompIDReqGrp::NoCompIDs::reset() noexcept {
	m_RefCompID = fields::RefCompID_NULL;
	m_RefSubID = fields::RefSubID_NULL;
	m_LocationID = fields::LocationID_NULL;
	m_DeskID = fields::DeskID_NULL;
	m_presence = 0;
}

bool CompIDReqGrp::NoCompIDs::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool CompIDReqGrp::NoCompIDs::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::RefCompID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *CompIDReqGrp::NoCompIDs::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasRefCompID()) { WRITE_TAG4(fix44::tag_const::RefCompID); std::memcpy(p, m_RefCompID.data(), m_RefCompID.size()); p += m_RefCompID.size(); *p++ = fix44::serialize::SOH; }
	if (hasRefSubID()) { WRITE_TAG4(fix44::tag_const::RefSubID); std::memcpy(p, m_RefSubID.data(), m_RefSubID.size()); p += m_RefSubID.size(); *p++ = fix44::serialize::SOH; }
	if (hasLocationID()) { WRITE_TAG4(fix44::tag_const::LocationID); std::memcpy(p, m_LocationID.data(), m_LocationID.size()); p += m_LocationID.size(); *p++ = fix44::serialize::SOH; }
	if (hasDeskID()) { WRITE_TAG4(fix44::tag_const::DeskID); std::memcpy(p, m_DeskID.data(), m_DeskID.size()); p += m_DeskID.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool CompIDReqGrp::NoCompIDs::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::RefCompID: setRefCompID({begin, len}); return true;
	case field_id::RefSubID: setRefSubID({begin, len}); return true;
	case field_id::LocationID: setLocationID({begin, len}); return true;
	case field_id::DeskID: setDeskID({begin, len}); return true;
	default: return false;
	}
}

void CompIDReqGrp::reset() noexcept {
	m_NoCompIDsGroup.clear();
	m_presence = 0;
}

bool CompIDReqGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoCompIDsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool CompIDReqGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool CompIDReqGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoCompIDs: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoCompIDsGroup.clear();
			m_NoCompIDsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoCompIDsGroup.emplace_back();
				if (!m_NoCompIDsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *CompIDReqGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoCompIDsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoCompIDs);
		p += falconfix::itoa(static_cast<int64_t>(m_NoCompIDsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoCompIDsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool CompIDReqGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
