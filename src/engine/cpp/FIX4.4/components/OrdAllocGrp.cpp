// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 8bd5b8264d8e4ed7

#include <FIX4.4/components/OrdAllocGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void OrdAllocGrp::NoOrders::reset() noexcept {
	m_ClOrdID = fields::ClOrdID_NULL;
	m_OrderID = fields::OrderID_NULL;
	m_SecondaryOrderID = fields::SecondaryOrderID_NULL;
	m_SecondaryClOrdID = fields::SecondaryClOrdID_NULL;
	m_ListID = fields::ListID_NULL;
	m_NestedParties2.reset();
	m_OrderQty = fields::NULL_QTY;
	m_OrderAvgPx = fields::NULL_PRICE;
	m_OrderBookingQty = fields::NULL_QTY;
	m_presence = 0;
}

bool OrdAllocGrp::NoOrders::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_NestedParties2.hasAnySet() || m_NestedParties2.checkRequired());
}

bool OrdAllocGrp::NoOrders::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::ClOrdID) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoNested2PartyIDs:
			p = tag_start;
			if (!m_NestedParties2.decode(p, end, field_id::ClOrdID, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *OrdAllocGrp::NoOrders::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasClOrdID()) { WRITE_TAG3(fix44::tag_const::ClOrdID); std::memcpy(p, m_ClOrdID.data(), m_ClOrdID.size()); p += m_ClOrdID.size(); *p++ = fix44::serialize::SOH; }
	if (hasOrderID()) { WRITE_TAG3(fix44::tag_const::OrderID); std::memcpy(p, m_OrderID.data(), m_OrderID.size()); p += m_OrderID.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecondaryOrderID()) { WRITE_TAG4(fix44::tag_const::SecondaryOrderID); std::memcpy(p, m_SecondaryOrderID.data(), m_SecondaryOrderID.size()); p += m_SecondaryOrderID.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecondaryClOrdID()) { WRITE_TAG4(fix44::tag_const::SecondaryClOrdID); std::memcpy(p, m_SecondaryClOrdID.data(), m_SecondaryClOrdID.size()); p += m_SecondaryClOrdID.size(); *p++ = fix44::serialize::SOH; }
	if (hasListID()) { WRITE_TAG3(fix44::tag_const::ListID); std::memcpy(p, m_ListID.data(), m_ListID.size()); p += m_ListID.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_NestedParties2.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasOrderQty()) { WRITE_TAG3(fix44::tag_const::OrderQty); p += falconfix::dtoa(m_OrderQty, p); *p++ = fix44::serialize::SOH; }
	if (hasOrderAvgPx()) { WRITE_TAG4(fix44::tag_const::OrderAvgPx); p += falconfix::dtoa(m_OrderAvgPx, p); *p++ = fix44::serialize::SOH; }
	if (hasOrderBookingQty()) { WRITE_TAG4(fix44::tag_const::OrderBookingQty); p += falconfix::dtoa(m_OrderBookingQty, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool OrdAllocGrp::NoOrders::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::ClOrdID: setClOrdID({begin, len}); return true;
	case field_id::OrderID: setOrderID({begin, len}); return true;
	case field_id::SecondaryOrderID: setSecondaryOrderID({begin, len}); return true;
	case field_id::SecondaryClOrdID: setSecondaryClOrdID({begin, len}); return true;
	case field_id::ListID: setListID({begin, len}); return true;
	case field_id::OrderQty: setOrderQty(fix44::parser::parse_double(begin, end)); return true;
	case field_id::OrderAvgPx: setOrderAvgPx(fix44::parser::parse_double(begin, end)); return true;
	case field_id::OrderBookingQty: setOrderBookingQty(fix44::parser::parse_double(begin, end)); return true;
	default: return false;
	}
}

void OrdAllocGrp::reset() noexcept {
	m_NoOrdersGroup.clear();
	m_presence = 0;
}

bool OrdAllocGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoOrdersGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool OrdAllocGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool OrdAllocGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoOrders: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoOrdersGroup.clear();
			m_NoOrdersGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoOrdersGroup.emplace_back();
				if (!m_NoOrdersGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *OrdAllocGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoOrdersGroup.empty()) {
		WRITE_TAG3(fix44::tag_const::NoOrders);
		p += falconfix::itoa(static_cast<int64_t>(m_NoOrdersGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoOrdersGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool OrdAllocGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
