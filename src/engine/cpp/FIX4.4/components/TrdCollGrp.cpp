// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 7b552eb2b5d14969

#include <FIX4.4/components/TrdCollGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void TrdCollGrp::NoTrades::reset() noexcept {
	m_TradeReportID = fields::TradeReportID_NULL;
	m_SecondaryTradeReportID = fields::SecondaryTradeReportID_NULL;
	m_presence = 0;
}

bool TrdCollGrp::NoTrades::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool TrdCollGrp::NoTrades::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::TradeReportID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *TrdCollGrp::NoTrades::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasTradeReportID()) { WRITE_TAG4(fix44::tag_const::TradeReportID); std::memcpy(p, m_TradeReportID.data(), m_TradeReportID.size()); p += m_TradeReportID.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecondaryTradeReportID()) { WRITE_TAG4(fix44::tag_const::SecondaryTradeReportID); std::memcpy(p, m_SecondaryTradeReportID.data(), m_SecondaryTradeReportID.size()); p += m_SecondaryTradeReportID.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool TrdCollGrp::NoTrades::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::TradeReportID: setTradeReportID({begin, len}); return true;
	case field_id::SecondaryTradeReportID: setSecondaryTradeReportID({begin, len}); return true;
	default: return false;
	}
}

void TrdCollGrp::reset() noexcept {
	m_NoTradesGroup.clear();
	m_presence = 0;
}

bool TrdCollGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoTradesGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool TrdCollGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool TrdCollGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoTrades: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoTradesGroup.clear();
			m_NoTradesGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoTradesGroup.emplace_back();
				if (!m_NoTradesGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *TrdCollGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoTradesGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoTrades);
		p += falconfix::itoa(static_cast<int64_t>(m_NoTradesGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoTradesGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool TrdCollGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
