// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: c1373fa3d92d2fab

#include <FIX4.4/components/SecAltIDGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void SecAltIDGrp::NoSecurityAltID::reset() noexcept {
	m_SecurityAltID = fields::SecurityAltID_NULL;
	m_SecurityAltIDSource = fields::SecurityAltIDSource_NULL;
	m_presence = 0;
}

bool SecAltIDGrp::NoSecurityAltID::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool SecAltIDGrp::NoSecurityAltID::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::SecurityAltID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *SecAltIDGrp::NoSecurityAltID::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasSecurityAltID()) { WRITE_TAG4(fix44::tag_const::SecurityAltID); std::memcpy(p, m_SecurityAltID.data(), m_SecurityAltID.size()); p += m_SecurityAltID.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecurityAltIDSource()) { WRITE_TAG4(fix44::tag_const::SecurityAltIDSource); std::memcpy(p, m_SecurityAltIDSource.data(), m_SecurityAltIDSource.size()); p += m_SecurityAltIDSource.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool SecAltIDGrp::NoSecurityAltID::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::SecurityAltID: setSecurityAltID({begin, len}); return true;
	case field_id::SecurityAltIDSource: setSecurityAltIDSource({begin, len}); return true;
	default: return false;
	}
}

void SecAltIDGrp::reset() noexcept {
	m_NoSecurityAltIDGroup.clear();
	m_presence = 0;
}

bool SecAltIDGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoSecurityAltIDGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool SecAltIDGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool SecAltIDGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoSecurityAltID: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoSecurityAltIDGroup.clear();
			m_NoSecurityAltIDGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoSecurityAltIDGroup.emplace_back();
				if (!m_NoSecurityAltIDGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *SecAltIDGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoSecurityAltIDGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoSecurityAltID);
		p += falconfix::itoa(static_cast<int64_t>(m_NoSecurityAltIDGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoSecurityAltIDGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool SecAltIDGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
