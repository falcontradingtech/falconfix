// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: f3ca817ce7ce19b5

#include <FIX4.4/components/CpctyConfGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void CpctyConfGrp::NoCapacities::reset() noexcept {
	m_OrderCapacity = fields::NULL_CHAR;
	m_OrderRestrictions = fields::OrderRestrictions_NULL;
	m_OrderCapacityQty = fields::NULL_QTY;
	m_presence = 0;
}

bool CpctyConfGrp::NoCapacities::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool CpctyConfGrp::NoCapacities::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::OrderCapacity) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *CpctyConfGrp::NoCapacities::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasOrderCapacity()) { WRITE_TAG4(fix44::tag_const::OrderCapacity); *p++ = m_OrderCapacity; *p++ = fix44::serialize::SOH; }
	if (hasOrderRestrictions()) { WRITE_TAG4(fix44::tag_const::OrderRestrictions); std::memcpy(p, m_OrderRestrictions.data(), m_OrderRestrictions.size()); p += m_OrderRestrictions.size(); *p++ = fix44::serialize::SOH; }
	if (hasOrderCapacityQty()) { WRITE_TAG4(fix44::tag_const::OrderCapacityQty); p += falconfix::dtoa(m_OrderCapacityQty, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool CpctyConfGrp::NoCapacities::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::OrderCapacity: setOrderCapacity(fix44::parser::parse_char(begin, end)); return true;
	case field_id::OrderRestrictions: setOrderRestrictions({begin, len}); return true;
	case field_id::OrderCapacityQty: setOrderCapacityQty(fix44::parser::parse_double(begin, end)); return true;
	default: return false;
	}
}

void CpctyConfGrp::reset() noexcept {
	m_NoCapacitiesGroup.clear();
	m_presence = 0;
}

bool CpctyConfGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoCapacitiesGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool CpctyConfGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool CpctyConfGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoCapacities: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoCapacitiesGroup.clear();
			m_NoCapacitiesGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoCapacitiesGroup.emplace_back();
				if (!m_NoCapacitiesGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *CpctyConfGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoCapacitiesGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoCapacities);
		p += falconfix::itoa(static_cast<int64_t>(m_NoCapacitiesGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoCapacitiesGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool CpctyConfGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
