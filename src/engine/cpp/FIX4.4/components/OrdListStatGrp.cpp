// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: d8ca87a52ee3f746

#include <FIX4.4/components/OrdListStatGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void OrdListStatGrp::NoOrders::reset() noexcept {
	m_ClOrdID = fields::ClOrdID_NULL;
	m_SecondaryClOrdID = fields::SecondaryClOrdID_NULL;
	m_CumQty = fields::NULL_QTY;
	m_OrdStatus = fields::NULL_CHAR;
	m_WorkingIndicator = fields::NULL_BOOLEAN;
	m_LeavesQty = fields::NULL_QTY;
	m_CxlQty = fields::NULL_QTY;
	m_AvgPx = fields::NULL_PRICE;
	m_OrdRejReason = fields::NULL_INT;
	m_Text = fields::Text_NULL;
	m_EncodedTextLen = fields::NULL_LENGTH;
	m_EncodedText = fields::EncodedText_NULL;
	m_presence = 0;
}

bool OrdListStatGrp::NoOrders::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool OrdListStatGrp::NoOrders::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::ClOrdID) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *OrdListStatGrp::NoOrders::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasClOrdID()) { WRITE_TAG3(fix44::tag_const::ClOrdID); std::memcpy(p, m_ClOrdID.data(), m_ClOrdID.size()); p += m_ClOrdID.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecondaryClOrdID()) { WRITE_TAG4(fix44::tag_const::SecondaryClOrdID); std::memcpy(p, m_SecondaryClOrdID.data(), m_SecondaryClOrdID.size()); p += m_SecondaryClOrdID.size(); *p++ = fix44::serialize::SOH; }
	if (hasCumQty()) { WRITE_TAG3(fix44::tag_const::CumQty); p += falconfix::dtoa(m_CumQty, p); *p++ = fix44::serialize::SOH; }
	if (hasOrdStatus()) { WRITE_TAG3(fix44::tag_const::OrdStatus); *p++ = m_OrdStatus; *p++ = fix44::serialize::SOH; }
	if (hasWorkingIndicator()) { WRITE_TAG4(fix44::tag_const::WorkingIndicator); *p++ = m_WorkingIndicator ? 'Y' : 'N'; *p++ = fix44::serialize::SOH; }
	if (hasLeavesQty()) { WRITE_TAG4(fix44::tag_const::LeavesQty); p += falconfix::dtoa(m_LeavesQty, p); *p++ = fix44::serialize::SOH; }
	if (hasCxlQty()) { WRITE_TAG3(fix44::tag_const::CxlQty); p += falconfix::dtoa(m_CxlQty, p); *p++ = fix44::serialize::SOH; }
	if (hasAvgPx()) { WRITE_TAG2(fix44::tag_const::AvgPx); p += falconfix::dtoa(m_AvgPx, p); *p++ = fix44::serialize::SOH; }
	if (hasOrdRejReason()) { WRITE_TAG4(fix44::tag_const::OrdRejReason); p += falconfix::itoa(m_OrdRejReason, p); *p++ = fix44::serialize::SOH; }
	if (hasText()) { WRITE_TAG3(fix44::tag_const::Text); std::memcpy(p, m_Text.data(), m_Text.size()); p += m_Text.size(); *p++ = fix44::serialize::SOH; }
	if (hasEncodedTextLen()) { WRITE_TAG4(fix44::tag_const::EncodedTextLen); p += falconfix::itoa(m_EncodedTextLen, p); *p++ = fix44::serialize::SOH; }
	if (hasEncodedText()) { WRITE_TAG4(fix44::tag_const::EncodedText); std::memcpy(p, m_EncodedText.data(), m_EncodedText.size()); p += m_EncodedText.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool OrdListStatGrp::NoOrders::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::ClOrdID: setClOrdID({begin, len}); return true;
	case field_id::SecondaryClOrdID: setSecondaryClOrdID({begin, len}); return true;
	case field_id::CumQty: setCumQty(fix44::parser::parse_double(begin, end)); return true;
	case field_id::OrdStatus: setOrdStatus(fix44::parser::parse_char(begin, end)); return true;
	case field_id::WorkingIndicator: setWorkingIndicator(fix44::parser::parse_bool(begin, end)); return true;
	case field_id::LeavesQty: setLeavesQty(fix44::parser::parse_double(begin, end)); return true;
	case field_id::CxlQty: setCxlQty(fix44::parser::parse_double(begin, end)); return true;
	case field_id::AvgPx: setAvgPx(fix44::parser::parse_double(begin, end)); return true;
	case field_id::OrdRejReason: setOrdRejReason(falconfix::atoi(begin, end)); return true;
	case field_id::Text: setText({begin, len}); return true;
	case field_id::EncodedTextLen: setEncodedTextLen(falconfix::atoi(begin, end)); return true;
	case field_id::EncodedText: setEncodedText({begin, len}); return true;
	default: return false;
	}
}

void OrdListStatGrp::reset() noexcept {
	m_NoOrdersGroup.clear();
	m_presence = 0;
}

bool OrdListStatGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoOrdersGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool OrdListStatGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool OrdListStatGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoOrders: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoOrdersGroup.clear();
			m_NoOrdersGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoOrdersGroup.emplace_back();
				if (!m_NoOrdersGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *OrdListStatGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoOrdersGroup.empty()) {
		WRITE_TAG3(fix44::tag_const::NoOrders);
		p += falconfix::itoa(static_cast<int64_t>(m_NoOrdersGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoOrdersGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool OrdListStatGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
