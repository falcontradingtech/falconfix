// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: b6fa9e82f6427448

#include <FIX4.4/components/LegBenchmarkCurveData.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void LegBenchmarkCurveData::reset() noexcept {
	m_LegBenchmarkCurveCurrency = fields::LegBenchmarkCurveCurrency_NULL;
	m_LegBenchmarkCurveName = fields::LegBenchmarkCurveName_NULL;
	m_LegBenchmarkCurvePoint = fields::LegBenchmarkCurvePoint_NULL;
	m_LegBenchmarkPrice = fields::NULL_PRICE;
	m_LegBenchmarkPriceType = fields::NULL_INT;
	m_presence = 0;
}

bool LegBenchmarkCurveData::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool LegBenchmarkCurveData::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool LegBenchmarkCurveData::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return true;
}

char *LegBenchmarkCurveData::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasLegBenchmarkCurveCurrency()) { WRITE_TAG4(fix44::tag_const::LegBenchmarkCurveCurrency); std::memcpy(p, m_LegBenchmarkCurveCurrency.data(), m_LegBenchmarkCurveCurrency.size()); p += m_LegBenchmarkCurveCurrency.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegBenchmarkCurveName()) { WRITE_TAG4(fix44::tag_const::LegBenchmarkCurveName); std::memcpy(p, m_LegBenchmarkCurveName.data(), m_LegBenchmarkCurveName.size()); p += m_LegBenchmarkCurveName.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegBenchmarkCurvePoint()) { WRITE_TAG4(fix44::tag_const::LegBenchmarkCurvePoint); std::memcpy(p, m_LegBenchmarkCurvePoint.data(), m_LegBenchmarkCurvePoint.size()); p += m_LegBenchmarkCurvePoint.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegBenchmarkPrice()) { WRITE_TAG4(fix44::tag_const::LegBenchmarkPrice); p += falconfix::dtoa(m_LegBenchmarkPrice, p); *p++ = fix44::serialize::SOH; }
	if (hasLegBenchmarkPriceType()) { WRITE_TAG4(fix44::tag_const::LegBenchmarkPriceType); p += falconfix::itoa(m_LegBenchmarkPriceType, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool LegBenchmarkCurveData::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::LegBenchmarkCurveCurrency: setLegBenchmarkCurveCurrency({begin, len}); return true;
	case field_id::LegBenchmarkCurveName: setLegBenchmarkCurveName({begin, len}); return true;
	case field_id::LegBenchmarkCurvePoint: setLegBenchmarkCurvePoint({begin, len}); return true;
	case field_id::LegBenchmarkPrice: setLegBenchmarkPrice(fix44::parser::parse_double(begin, end)); return true;
	case field_id::LegBenchmarkPriceType: setLegBenchmarkPriceType(falconfix::atoi(begin, end)); return true;
	default: return false;
	}
}

} // namespace fix44::components
