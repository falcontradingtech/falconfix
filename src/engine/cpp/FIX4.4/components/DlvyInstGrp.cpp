// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: d9366d9026da576c

#include <FIX4.4/components/DlvyInstGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void DlvyInstGrp::NoDlvyInst::reset() noexcept {
	m_SettlInstSource = fields::NULL_CHAR;
	m_DlvyInstType = fields::NULL_CHAR;
	m_SettlParties.reset();
	m_presence = 0;
}

bool DlvyInstGrp::NoDlvyInst::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_SettlParties.hasAnySet() || m_SettlParties.checkRequired());
}

bool DlvyInstGrp::NoDlvyInst::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::SettlInstSource) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoSettlPartyIDs:
			p = tag_start;
			if (!m_SettlParties.decode(p, end, field_id::SettlInstSource, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *DlvyInstGrp::NoDlvyInst::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasSettlInstSource()) { WRITE_TAG4(fix44::tag_const::SettlInstSource); *p++ = m_SettlInstSource; *p++ = fix44::serialize::SOH; }
	if (hasDlvyInstType()) { WRITE_TAG4(fix44::tag_const::DlvyInstType); *p++ = m_DlvyInstType; *p++ = fix44::serialize::SOH; }
	if ((p = m_SettlParties.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool DlvyInstGrp::NoDlvyInst::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::SettlInstSource: setSettlInstSource(fix44::parser::parse_char(begin, end)); return true;
	case field_id::DlvyInstType: setDlvyInstType(fix44::parser::parse_char(begin, end)); return true;
	default: return false;
	}
}

void DlvyInstGrp::reset() noexcept {
	m_NoDlvyInstGroup.clear();
	m_presence = 0;
}

bool DlvyInstGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoDlvyInstGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool DlvyInstGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool DlvyInstGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoDlvyInst: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoDlvyInstGroup.clear();
			m_NoDlvyInstGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoDlvyInstGroup.emplace_back();
				if (!m_NoDlvyInstGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *DlvyInstGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoDlvyInstGroup.empty()) {
		WRITE_TAG3(fix44::tag_const::NoDlvyInst);
		p += falconfix::itoa(static_cast<int64_t>(m_NoDlvyInstGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoDlvyInstGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool DlvyInstGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
