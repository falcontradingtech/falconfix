// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 60761a5451f30c27

#include <FIX4.4/components/InstrmtStrkPxGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void InstrmtStrkPxGrp::NoStrikes::reset() noexcept {
	m_Instrument.reset();
	m_presence = 0;
}

bool InstrmtStrkPxGrp::NoStrikes::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_Instrument.hasAnySet() || m_Instrument.checkRequired());
}

bool InstrmtStrkPxGrp::NoStrikes::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::Symbol) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::Symbol:
			p = tag_start;
			if (!m_Instrument.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *InstrmtStrkPxGrp::NoStrikes::encode(char *p, bool includeMilliseconds) const noexcept {
	if ((p = m_Instrument.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool InstrmtStrkPxGrp::NoStrikes::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	default: return false;
	}
}

void InstrmtStrkPxGrp::reset() noexcept {
	m_NoStrikesGroup.clear();
	m_presence = 0;
}

bool InstrmtStrkPxGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoStrikesGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool InstrmtStrkPxGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool InstrmtStrkPxGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoStrikes: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoStrikesGroup.clear();
			m_NoStrikesGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoStrikesGroup.emplace_back();
				if (!m_NoStrikesGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *InstrmtStrkPxGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoStrikesGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoStrikes);
		p += falconfix::itoa(static_cast<int64_t>(m_NoStrikesGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoStrikesGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool InstrmtStrkPxGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
