// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 1c2e43072068c825

#include <FIX4.4/components/QuotSetGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void QuotSetGrp::NoQuoteSets::reset() noexcept {
	m_QuoteSetID = fields::QuoteSetID_NULL;
	m_UnderlyingInstrument.reset();
	m_QuoteSetValidUntilTime = fields::NULL_UTCTIMESTAMP;
	m_TotNoQuoteEntries = fields::NULL_INT;
	m_LastFragment = fields::NULL_BOOLEAN;
	m_QuotEntryGrp.reset();
	m_presence = 0;
}

bool QuotSetGrp::NoQuoteSets::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_UnderlyingInstrument.hasAnySet() || m_UnderlyingInstrument.checkRequired()) &&
			(!m_QuotEntryGrp.hasAnySet() || m_QuotEntryGrp.checkRequired());
}

bool QuotSetGrp::NoQuoteSets::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::QuoteSetID) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::UnderlyingSymbol:
			p = tag_start;
			if (!m_UnderlyingInstrument.decode(p, end, field_id::QuoteSetID, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoQuoteEntries:
			p = tag_start;
			if (!m_QuotEntryGrp.decode(p, end, field_id::QuoteSetID, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *QuotSetGrp::NoQuoteSets::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasQuoteSetID()) { WRITE_TAG4(fix44::tag_const::QuoteSetID); std::memcpy(p, m_QuoteSetID.data(), m_QuoteSetID.size()); p += m_QuoteSetID.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_UnderlyingInstrument.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasQuoteSetValidUntilTime()) { WRITE_TAG4(fix44::tag_const::QuoteSetValidUntilTime); p += fix44::datetime::format_UTCTimestamp_compact(m_QuoteSetValidUntilTime, p, includeMilliseconds); *p++ = fix44::serialize::SOH; }
	if (hasTotNoQuoteEntries()) { WRITE_TAG4(fix44::tag_const::TotNoQuoteEntries); p += falconfix::itoa(m_TotNoQuoteEntries, p); *p++ = fix44::serialize::SOH; }
	if (hasLastFragment()) { WRITE_TAG4(fix44::tag_const::LastFragment); *p++ = m_LastFragment ? 'Y' : 'N'; *p++ = fix44::serialize::SOH; }
	if ((p = m_QuotEntryGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool QuotSetGrp::NoQuoteSets::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::QuoteSetID: setQuoteSetID({begin, len}); return true;
	case field_id::QuoteSetValidUntilTime: setQuoteSetValidUntilTime(falconfix::atoi(begin, end)); return true;
	case field_id::TotNoQuoteEntries: setTotNoQuoteEntries(falconfix::atoi(begin, end)); return true;
	case field_id::LastFragment: setLastFragment(fix44::parser::parse_bool(begin, end)); return true;
	default: return false;
	}
}

void QuotSetGrp::reset() noexcept {
	m_NoQuoteSetsGroup.clear();
	m_presence = 0;
}

bool QuotSetGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoQuoteSetsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool QuotSetGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool QuotSetGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoQuoteSets: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoQuoteSetsGroup.clear();
			m_NoQuoteSetsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoQuoteSetsGroup.emplace_back();
				if (!m_NoQuoteSetsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *QuotSetGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoQuoteSetsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoQuoteSets);
		p += falconfix::itoa(static_cast<int64_t>(m_NoQuoteSetsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoQuoteSetsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool QuotSetGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
