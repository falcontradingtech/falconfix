// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: a29636fa37a2aef7

#include <FIX4.4/components/NestedParties2.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void NestedParties2::NoNested2PartyIDs::reset() noexcept {
	m_Nested2PartyID = fields::Nested2PartyID_NULL;
	m_Nested2PartyIDSource = fields::NULL_CHAR;
	m_Nested2PartyRole = fields::NULL_INT;
	m_NstdPtys2SubGrp.reset();
	m_presence = 0;
}

bool NestedParties2::NoNested2PartyIDs::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_NstdPtys2SubGrp.hasAnySet() || m_NstdPtys2SubGrp.checkRequired());
}

bool NestedParties2::NoNested2PartyIDs::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::Nested2PartyID) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoNested2PartySubIDs:
			p = tag_start;
			if (!m_NstdPtys2SubGrp.decode(p, end, field_id::Nested2PartyID, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *NestedParties2::NoNested2PartyIDs::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasNested2PartyID()) { WRITE_TAG4(fix44::tag_const::Nested2PartyID); std::memcpy(p, m_Nested2PartyID.data(), m_Nested2PartyID.size()); p += m_Nested2PartyID.size(); *p++ = fix44::serialize::SOH; }
	if (hasNested2PartyIDSource()) { WRITE_TAG4(fix44::tag_const::Nested2PartyIDSource); *p++ = m_Nested2PartyIDSource; *p++ = fix44::serialize::SOH; }
	if (hasNested2PartyRole()) { WRITE_TAG4(fix44::tag_const::Nested2PartyRole); p += falconfix::itoa(m_Nested2PartyRole, p); *p++ = fix44::serialize::SOH; }
	if ((p = m_NstdPtys2SubGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool NestedParties2::NoNested2PartyIDs::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::Nested2PartyID: setNested2PartyID({begin, len}); return true;
	case field_id::Nested2PartyIDSource: setNested2PartyIDSource(fix44::parser::parse_char(begin, end)); return true;
	case field_id::Nested2PartyRole: setNested2PartyRole(falconfix::atoi(begin, end)); return true;
	default: return false;
	}
}

void NestedParties2::reset() noexcept {
	m_NoNested2PartyIDsGroup.clear();
	m_presence = 0;
}

bool NestedParties2::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoNested2PartyIDsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool NestedParties2::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool NestedParties2::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoNested2PartyIDs: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoNested2PartyIDsGroup.clear();
			m_NoNested2PartyIDsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoNested2PartyIDsGroup.emplace_back();
				if (!m_NoNested2PartyIDsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *NestedParties2::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoNested2PartyIDsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoNested2PartyIDs);
		p += falconfix::itoa(static_cast<int64_t>(m_NoNested2PartyIDsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoNested2PartyIDsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool NestedParties2::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
