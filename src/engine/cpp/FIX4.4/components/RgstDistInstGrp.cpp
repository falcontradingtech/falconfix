// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 5a1f9ec8f67d581d

#include <FIX4.4/components/RgstDistInstGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void RgstDistInstGrp::NoDistribInsts::reset() noexcept {
	m_DistribPaymentMethod = fields::NULL_INT;
	m_DistribPercentage = fields::NULL_PERCENTAGE;
	m_CashDistribCurr = fields::CashDistribCurr_NULL;
	m_CashDistribAgentName = fields::CashDistribAgentName_NULL;
	m_CashDistribAgentCode = fields::CashDistribAgentCode_NULL;
	m_CashDistribAgentAcctNumber = fields::CashDistribAgentAcctNumber_NULL;
	m_CashDistribPayRef = fields::CashDistribPayRef_NULL;
	m_CashDistribAgentAcctName = fields::CashDistribAgentAcctName_NULL;
	m_presence = 0;
}

bool RgstDistInstGrp::NoDistribInsts::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool RgstDistInstGrp::NoDistribInsts::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::DistribPaymentMethod) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *RgstDistInstGrp::NoDistribInsts::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasDistribPaymentMethod()) { WRITE_TAG4(fix44::tag_const::DistribPaymentMethod); p += falconfix::itoa(m_DistribPaymentMethod, p); *p++ = fix44::serialize::SOH; }
	if (hasDistribPercentage()) { WRITE_TAG4(fix44::tag_const::DistribPercentage); p += falconfix::dtoa(m_DistribPercentage, p); *p++ = fix44::serialize::SOH; }
	if (hasCashDistribCurr()) { WRITE_TAG4(fix44::tag_const::CashDistribCurr); std::memcpy(p, m_CashDistribCurr.data(), m_CashDistribCurr.size()); p += m_CashDistribCurr.size(); *p++ = fix44::serialize::SOH; }
	if (hasCashDistribAgentName()) { WRITE_TAG4(fix44::tag_const::CashDistribAgentName); std::memcpy(p, m_CashDistribAgentName.data(), m_CashDistribAgentName.size()); p += m_CashDistribAgentName.size(); *p++ = fix44::serialize::SOH; }
	if (hasCashDistribAgentCode()) { WRITE_TAG4(fix44::tag_const::CashDistribAgentCode); std::memcpy(p, m_CashDistribAgentCode.data(), m_CashDistribAgentCode.size()); p += m_CashDistribAgentCode.size(); *p++ = fix44::serialize::SOH; }
	if (hasCashDistribAgentAcctNumber()) { WRITE_TAG4(fix44::tag_const::CashDistribAgentAcctNumber); std::memcpy(p, m_CashDistribAgentAcctNumber.data(), m_CashDistribAgentAcctNumber.size()); p += m_CashDistribAgentAcctNumber.size(); *p++ = fix44::serialize::SOH; }
	if (hasCashDistribPayRef()) { WRITE_TAG4(fix44::tag_const::CashDistribPayRef); std::memcpy(p, m_CashDistribPayRef.data(), m_CashDistribPayRef.size()); p += m_CashDistribPayRef.size(); *p++ = fix44::serialize::SOH; }
	if (hasCashDistribAgentAcctName()) { WRITE_TAG4(fix44::tag_const::CashDistribAgentAcctName); std::memcpy(p, m_CashDistribAgentAcctName.data(), m_CashDistribAgentAcctName.size()); p += m_CashDistribAgentAcctName.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool RgstDistInstGrp::NoDistribInsts::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::DistribPaymentMethod: setDistribPaymentMethod(falconfix::atoi(begin, end)); return true;
	case field_id::DistribPercentage: setDistribPercentage(fix44::parser::parse_double(begin, end)); return true;
	case field_id::CashDistribCurr: setCashDistribCurr({begin, len}); return true;
	case field_id::CashDistribAgentName: setCashDistribAgentName({begin, len}); return true;
	case field_id::CashDistribAgentCode: setCashDistribAgentCode({begin, len}); return true;
	case field_id::CashDistribAgentAcctNumber: setCashDistribAgentAcctNumber({begin, len}); return true;
	case field_id::CashDistribPayRef: setCashDistribPayRef({begin, len}); return true;
	case field_id::CashDistribAgentAcctName: setCashDistribAgentAcctName({begin, len}); return true;
	default: return false;
	}
}

void RgstDistInstGrp::reset() noexcept {
	m_NoDistribInstsGroup.clear();
	m_presence = 0;
}

bool RgstDistInstGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoDistribInstsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool RgstDistInstGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool RgstDistInstGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoDistribInsts: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoDistribInstsGroup.clear();
			m_NoDistribInstsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoDistribInstsGroup.emplace_back();
				if (!m_NoDistribInstsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *RgstDistInstGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoDistribInstsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoDistribInsts);
		p += falconfix::itoa(static_cast<int64_t>(m_NoDistribInstsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoDistribInstsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool RgstDistInstGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
