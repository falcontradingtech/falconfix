// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 7b01013fc30fe419

#include <FIX4.4/components/EvntGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void EvntGrp::NoEvents::reset() noexcept {
	m_EventType = fields::NULL_INT;
	m_EventDate = fields::NULL_LOCALMKTDATE;
	m_EventPx = fields::NULL_PRICE;
	m_EventText = fields::EventText_NULL;
	m_presence = 0;
}

bool EvntGrp::NoEvents::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool EvntGrp::NoEvents::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::EventType) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *EvntGrp::NoEvents::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasEventType()) { WRITE_TAG4(fix44::tag_const::EventType); p += falconfix::itoa(m_EventType, p); *p++ = fix44::serialize::SOH; }
	if (hasEventDate()) { WRITE_TAG4(fix44::tag_const::EventDate); p += fix44::datetime::format_UTCDateOnly(m_EventDate, p); *p++ = fix44::serialize::SOH; }
	if (hasEventPx()) { WRITE_TAG4(fix44::tag_const::EventPx); p += falconfix::dtoa(m_EventPx, p); *p++ = fix44::serialize::SOH; }
	if (hasEventText()) { WRITE_TAG4(fix44::tag_const::EventText); std::memcpy(p, m_EventText.data(), m_EventText.size()); p += m_EventText.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool EvntGrp::NoEvents::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::EventType: setEventType(falconfix::atoi(begin, end)); return true;
	case field_id::EventDate: setEventDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::EventPx: setEventPx(fix44::parser::parse_double(begin, end)); return true;
	case field_id::EventText: setEventText({begin, len}); return true;
	default: return false;
	}
}

void EvntGrp::reset() noexcept {
	m_NoEventsGroup.clear();
	m_presence = 0;
}

bool EvntGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoEventsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool EvntGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool EvntGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoEvents: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoEventsGroup.clear();
			m_NoEventsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoEventsGroup.emplace_back();
				if (!m_NoEventsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *EvntGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoEventsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoEvents);
		p += falconfix::itoa(static_cast<int64_t>(m_NoEventsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoEventsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool EvntGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
