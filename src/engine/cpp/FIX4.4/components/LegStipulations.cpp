// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 128eb7ff81e7b4ec

#include <FIX4.4/components/LegStipulations.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void LegStipulations::NoLegStipulations::reset() noexcept {
	m_LegStipulationType = fields::LegStipulationType_NULL;
	m_LegStipulationValue = fields::LegStipulationValue_NULL;
	m_presence = 0;
}

bool LegStipulations::NoLegStipulations::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool LegStipulations::NoLegStipulations::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::LegStipulationType) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *LegStipulations::NoLegStipulations::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasLegStipulationType()) { WRITE_TAG4(fix44::tag_const::LegStipulationType); std::memcpy(p, m_LegStipulationType.data(), m_LegStipulationType.size()); p += m_LegStipulationType.size(); *p++ = fix44::serialize::SOH; }
	if (hasLegStipulationValue()) { WRITE_TAG4(fix44::tag_const::LegStipulationValue); std::memcpy(p, m_LegStipulationValue.data(), m_LegStipulationValue.size()); p += m_LegStipulationValue.size(); *p++ = fix44::serialize::SOH; }
	return p;
}

bool LegStipulations::NoLegStipulations::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::LegStipulationType: setLegStipulationType({begin, len}); return true;
	case field_id::LegStipulationValue: setLegStipulationValue({begin, len}); return true;
	default: return false;
	}
}

void LegStipulations::reset() noexcept {
	m_NoLegStipulationsGroup.clear();
	m_presence = 0;
}

bool LegStipulations::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoLegStipulationsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool LegStipulations::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool LegStipulations::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoLegStipulations: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoLegStipulationsGroup.clear();
			m_NoLegStipulationsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoLegStipulationsGroup.emplace_back();
				if (!m_NoLegStipulationsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *LegStipulations::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoLegStipulationsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoLegStipulations);
		p += falconfix::itoa(static_cast<int64_t>(m_NoLegStipulationsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoLegStipulationsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool LegStipulations::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
