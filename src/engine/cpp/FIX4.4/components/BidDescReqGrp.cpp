// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 9f6ee65247a7c3bc

#include <FIX4.4/components/BidDescReqGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void BidDescReqGrp::NoBidDescriptors::reset() noexcept {
	m_BidDescriptorType = fields::NULL_INT;
	m_BidDescriptor = fields::BidDescriptor_NULL;
	m_SideValueInd = fields::NULL_INT;
	m_LiquidityValue = fields::NULL_AMT;
	m_LiquidityNumSecurities = fields::NULL_INT;
	m_LiquidityPctLow = fields::NULL_PERCENTAGE;
	m_LiquidityPctHigh = fields::NULL_PERCENTAGE;
	m_EFPTrackingError = fields::NULL_PERCENTAGE;
	m_FairValue = fields::NULL_AMT;
	m_OutsideIndexPct = fields::NULL_PERCENTAGE;
	m_ValueOfFutures = fields::NULL_AMT;
	m_presence = 0;
}

bool BidDescReqGrp::NoBidDescriptors::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool BidDescReqGrp::NoBidDescriptors::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::BidDescriptorType) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *BidDescReqGrp::NoBidDescriptors::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasBidDescriptorType()) { WRITE_TAG4(fix44::tag_const::BidDescriptorType); p += falconfix::itoa(m_BidDescriptorType, p); *p++ = fix44::serialize::SOH; }
	if (hasBidDescriptor()) { WRITE_TAG4(fix44::tag_const::BidDescriptor); std::memcpy(p, m_BidDescriptor.data(), m_BidDescriptor.size()); p += m_BidDescriptor.size(); *p++ = fix44::serialize::SOH; }
	if (hasSideValueInd()) { WRITE_TAG4(fix44::tag_const::SideValueInd); p += falconfix::itoa(m_SideValueInd, p); *p++ = fix44::serialize::SOH; }
	if (hasLiquidityValue()) { WRITE_TAG4(fix44::tag_const::LiquidityValue); p += falconfix::dtoa(m_LiquidityValue, p); *p++ = fix44::serialize::SOH; }
	if (hasLiquidityNumSecurities()) { WRITE_TAG4(fix44::tag_const::LiquidityNumSecurities); p += falconfix::itoa(m_LiquidityNumSecurities, p); *p++ = fix44::serialize::SOH; }
	if (hasLiquidityPctLow()) { WRITE_TAG4(fix44::tag_const::LiquidityPctLow); p += falconfix::dtoa(m_LiquidityPctLow, p); *p++ = fix44::serialize::SOH; }
	if (hasLiquidityPctHigh()) { WRITE_TAG4(fix44::tag_const::LiquidityPctHigh); p += falconfix::dtoa(m_LiquidityPctHigh, p); *p++ = fix44::serialize::SOH; }
	if (hasEFPTrackingError()) { WRITE_TAG4(fix44::tag_const::EFPTrackingError); p += falconfix::dtoa(m_EFPTrackingError, p); *p++ = fix44::serialize::SOH; }
	if (hasFairValue()) { WRITE_TAG4(fix44::tag_const::FairValue); p += falconfix::dtoa(m_FairValue, p); *p++ = fix44::serialize::SOH; }
	if (hasOutsideIndexPct()) { WRITE_TAG4(fix44::tag_const::OutsideIndexPct); p += falconfix::dtoa(m_OutsideIndexPct, p); *p++ = fix44::serialize::SOH; }
	if (hasValueOfFutures()) { WRITE_TAG4(fix44::tag_const::ValueOfFutures); p += falconfix::dtoa(m_ValueOfFutures, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool BidDescReqGrp::NoBidDescriptors::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::BidDescriptorType: setBidDescriptorType(falconfix::atoi(begin, end)); return true;
	case field_id::BidDescriptor: setBidDescriptor({begin, len}); return true;
	case field_id::SideValueInd: setSideValueInd(falconfix::atoi(begin, end)); return true;
	case field_id::LiquidityValue: setLiquidityValue(fix44::parser::parse_double(begin, end)); return true;
	case field_id::LiquidityNumSecurities: setLiquidityNumSecurities(falconfix::atoi(begin, end)); return true;
	case field_id::LiquidityPctLow: setLiquidityPctLow(fix44::parser::parse_double(begin, end)); return true;
	case field_id::LiquidityPctHigh: setLiquidityPctHigh(fix44::parser::parse_double(begin, end)); return true;
	case field_id::EFPTrackingError: setEFPTrackingError(fix44::parser::parse_double(begin, end)); return true;
	case field_id::FairValue: setFairValue(fix44::parser::parse_double(begin, end)); return true;
	case field_id::OutsideIndexPct: setOutsideIndexPct(fix44::parser::parse_double(begin, end)); return true;
	case field_id::ValueOfFutures: setValueOfFutures(fix44::parser::parse_double(begin, end)); return true;
	default: return false;
	}
}

void BidDescReqGrp::reset() noexcept {
	m_NoBidDescriptorsGroup.clear();
	m_presence = 0;
}

bool BidDescReqGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoBidDescriptorsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool BidDescReqGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool BidDescReqGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoBidDescriptors: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoBidDescriptorsGroup.clear();
			m_NoBidDescriptorsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoBidDescriptorsGroup.emplace_back();
				if (!m_NoBidDescriptorsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *BidDescReqGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoBidDescriptorsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoBidDescriptors);
		p += falconfix::itoa(static_cast<int64_t>(m_NoBidDescriptorsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoBidDescriptorsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool BidDescReqGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
