// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: da6f46dffee7a59f

#include <FIX4.4/components/MiscFeesGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void MiscFeesGrp::NoMiscFees::reset() noexcept {
	m_MiscFeeAmt = fields::NULL_AMT;
	m_MiscFeeCurr = fields::MiscFeeCurr_NULL;
	m_MiscFeeType = fields::NULL_CHAR;
	m_MiscFeeBasis = fields::NULL_INT;
	m_presence = 0;
}

bool MiscFeesGrp::NoMiscFees::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	return true;
}

bool MiscFeesGrp::NoMiscFees::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::MiscFeeAmt) {
			p = tag_start;
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *MiscFeesGrp::NoMiscFees::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasMiscFeeAmt()) { WRITE_TAG4(fix44::tag_const::MiscFeeAmt); p += falconfix::dtoa(m_MiscFeeAmt, p); *p++ = fix44::serialize::SOH; }
	if (hasMiscFeeCurr()) { WRITE_TAG4(fix44::tag_const::MiscFeeCurr); std::memcpy(p, m_MiscFeeCurr.data(), m_MiscFeeCurr.size()); p += m_MiscFeeCurr.size(); *p++ = fix44::serialize::SOH; }
	if (hasMiscFeeType()) { WRITE_TAG4(fix44::tag_const::MiscFeeType); *p++ = m_MiscFeeType; *p++ = fix44::serialize::SOH; }
	if (hasMiscFeeBasis()) { WRITE_TAG4(fix44::tag_const::MiscFeeBasis); p += falconfix::itoa(m_MiscFeeBasis, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool MiscFeesGrp::NoMiscFees::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::MiscFeeAmt: setMiscFeeAmt(fix44::parser::parse_double(begin, end)); return true;
	case field_id::MiscFeeCurr: setMiscFeeCurr({begin, len}); return true;
	case field_id::MiscFeeType: setMiscFeeType(fix44::parser::parse_char(begin, end)); return true;
	case field_id::MiscFeeBasis: setMiscFeeBasis(falconfix::atoi(begin, end)); return true;
	default: return false;
	}
}

void MiscFeesGrp::reset() noexcept {
	m_NoMiscFeesGroup.clear();
	m_presence = 0;
}

bool MiscFeesGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoMiscFeesGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool MiscFeesGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool MiscFeesGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoMiscFees: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoMiscFeesGroup.clear();
			m_NoMiscFeesGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoMiscFeesGroup.emplace_back();
				if (!m_NoMiscFeesGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *MiscFeesGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoMiscFeesGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoMiscFees);
		p += falconfix::itoa(static_cast<int64_t>(m_NoMiscFeesGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoMiscFeesGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool MiscFeesGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
