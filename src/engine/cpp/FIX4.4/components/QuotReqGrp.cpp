// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: d76c8ec07b719c32

#include <FIX4.4/components/QuotReqGrp.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void QuotReqGrp::NoRelatedSym::reset() noexcept {
	m_Instrument.reset();
	m_FinancingDetails.reset();
	m_UndInstrmtGrp.reset();
	m_PrevClosePx = fields::NULL_PRICE;
	m_QuoteRequestType = fields::NULL_INT;
	m_QuoteType = fields::NULL_INT;
	m_TradingSessionID = fields::TradingSessionID_NULL;
	m_TradingSessionSubID = fields::TradingSessionSubID_NULL;
	m_TradeOriginationDate = fields::NULL_LOCALMKTDATE;
	m_Side = fields::NULL_CHAR;
	m_QtyType = fields::NULL_INT;
	m_OrderQtyData.reset();
	m_SettlType = fields::NULL_CHAR;
	m_SettlDate = fields::NULL_LOCALMKTDATE;
	m_SettlDate2 = fields::NULL_LOCALMKTDATE;
	m_OrderQty2 = fields::NULL_QTY;
	m_Currency = fields::Currency_NULL;
	m_Stipulations.reset();
	m_Account = fields::Account_NULL;
	m_AcctIDSource = fields::NULL_INT;
	m_AccountType = fields::NULL_INT;
	m_QuotReqLegsGrp.reset();
	m_QuotQualGrp.reset();
	m_QuotePriceType = fields::NULL_INT;
	m_OrdType = fields::NULL_CHAR;
	m_ValidUntilTime = fields::NULL_UTCTIMESTAMP;
	m_ExpireTime = fields::NULL_UTCTIMESTAMP;
	m_TransactTime = fields::NULL_UTCTIMESTAMP;
	m_SpreadOrBenchmarkCurveData.reset();
	m_PriceType = fields::NULL_INT;
	m_Price = fields::NULL_PRICE;
	m_Price2 = fields::NULL_PRICE;
	m_YieldData.reset();
	m_Parties.reset();
	m_presence = 0;
}

bool QuotReqGrp::NoRelatedSym::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_Instrument.hasAnySet() || m_Instrument.checkRequired()) &&
			(!m_FinancingDetails.hasAnySet() || m_FinancingDetails.checkRequired()) &&
			(!m_UndInstrmtGrp.hasAnySet() || m_UndInstrmtGrp.checkRequired()) &&
			(!m_OrderQtyData.hasAnySet() || m_OrderQtyData.checkRequired()) &&
			(!m_Stipulations.hasAnySet() || m_Stipulations.checkRequired()) &&
			(!m_QuotReqLegsGrp.hasAnySet() || m_QuotReqLegsGrp.checkRequired()) &&
			(!m_QuotQualGrp.hasAnySet() || m_QuotQualGrp.checkRequired()) &&
			(!m_SpreadOrBenchmarkCurveData.hasAnySet() || m_SpreadOrBenchmarkCurveData.checkRequired()) &&
			(!m_YieldData.hasAnySet() || m_YieldData.checkRequired()) &&
			(!m_Parties.hasAnySet() || m_Parties.checkRequired());
}

bool QuotReqGrp::NoRelatedSym::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::Symbol) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::Symbol:
			p = tag_start;
			if (!m_Instrument.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::AgreementDesc:
			p = tag_start;
			if (!m_FinancingDetails.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoUnderlyings:
			p = tag_start;
			if (!m_UndInstrmtGrp.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::OrderQty:
			p = tag_start;
			if (!m_OrderQtyData.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoStipulations:
			p = tag_start;
			if (!m_Stipulations.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoLegs:
			p = tag_start;
			if (!m_QuotReqLegsGrp.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoQuoteQualifiers:
			p = tag_start;
			if (!m_QuotQualGrp.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::Spread:
			p = tag_start;
			if (!m_SpreadOrBenchmarkCurveData.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::YieldType:
			p = tag_start;
			if (!m_YieldData.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		case field_id::NoPartyIDs:
			p = tag_start;
			if (!m_Parties.decode(p, end, field_id::Symbol, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *QuotReqGrp::NoRelatedSym::encode(char *p, bool includeMilliseconds) const noexcept {
	if ((p = m_Instrument.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if ((p = m_FinancingDetails.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if ((p = m_UndInstrmtGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasPrevClosePx()) { WRITE_TAG4(fix44::tag_const::PrevClosePx); p += falconfix::dtoa(m_PrevClosePx, p); *p++ = fix44::serialize::SOH; }
	if (hasQuoteRequestType()) { WRITE_TAG4(fix44::tag_const::QuoteRequestType); p += falconfix::itoa(m_QuoteRequestType, p); *p++ = fix44::serialize::SOH; }
	if (hasQuoteType()) { WRITE_TAG4(fix44::tag_const::QuoteType); p += falconfix::itoa(m_QuoteType, p); *p++ = fix44::serialize::SOH; }
	if (hasTradingSessionID()) { WRITE_TAG4(fix44::tag_const::TradingSessionID); std::memcpy(p, m_TradingSessionID.data(), m_TradingSessionID.size()); p += m_TradingSessionID.size(); *p++ = fix44::serialize::SOH; }
	if (hasTradingSessionSubID()) { WRITE_TAG4(fix44::tag_const::TradingSessionSubID); std::memcpy(p, m_TradingSessionSubID.data(), m_TradingSessionSubID.size()); p += m_TradingSessionSubID.size(); *p++ = fix44::serialize::SOH; }
	if (hasTradeOriginationDate()) { WRITE_TAG4(fix44::tag_const::TradeOriginationDate); p += fix44::datetime::format_UTCDateOnly(m_TradeOriginationDate, p); *p++ = fix44::serialize::SOH; }
	if (hasSide()) { WRITE_TAG3(fix44::tag_const::Side); *p++ = m_Side; *p++ = fix44::serialize::SOH; }
	if (hasQtyType()) { WRITE_TAG4(fix44::tag_const::QtyType); p += falconfix::itoa(m_QtyType, p); *p++ = fix44::serialize::SOH; }
	if ((p = m_OrderQtyData.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasSettlType()) { WRITE_TAG3(fix44::tag_const::SettlType); *p++ = m_SettlType; *p++ = fix44::serialize::SOH; }
	if (hasSettlDate()) { WRITE_TAG3(fix44::tag_const::SettlDate); p += fix44::datetime::format_UTCDateOnly(m_SettlDate, p); *p++ = fix44::serialize::SOH; }
	if (hasSettlDate2()) { WRITE_TAG4(fix44::tag_const::SettlDate2); p += fix44::datetime::format_UTCDateOnly(m_SettlDate2, p); *p++ = fix44::serialize::SOH; }
	if (hasOrderQty2()) { WRITE_TAG4(fix44::tag_const::OrderQty2); p += falconfix::dtoa(m_OrderQty2, p); *p++ = fix44::serialize::SOH; }
	if (hasCurrency()) { WRITE_TAG3(fix44::tag_const::Currency); std::memcpy(p, m_Currency.data(), m_Currency.size()); p += m_Currency.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_Stipulations.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasAccount()) { WRITE_TAG2(fix44::tag_const::Account); std::memcpy(p, m_Account.data(), m_Account.size()); p += m_Account.size(); *p++ = fix44::serialize::SOH; }
	if (hasAcctIDSource()) { WRITE_TAG4(fix44::tag_const::AcctIDSource); p += falconfix::itoa(m_AcctIDSource, p); *p++ = fix44::serialize::SOH; }
	if (hasAccountType()) { WRITE_TAG4(fix44::tag_const::AccountType); p += falconfix::itoa(m_AccountType, p); *p++ = fix44::serialize::SOH; }
	if ((p = m_QuotReqLegsGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if ((p = m_QuotQualGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasQuotePriceType()) { WRITE_TAG4(fix44::tag_const::QuotePriceType); p += falconfix::itoa(m_QuotePriceType, p); *p++ = fix44::serialize::SOH; }
	if (hasOrdType()) { WRITE_TAG3(fix44::tag_const::OrdType); *p++ = m_OrdType; *p++ = fix44::serialize::SOH; }
	if (hasValidUntilTime()) { WRITE_TAG3(fix44::tag_const::ValidUntilTime); p += fix44::datetime::format_UTCTimestamp_compact(m_ValidUntilTime, p, includeMilliseconds); *p++ = fix44::serialize::SOH; }
	if (hasExpireTime()) { WRITE_TAG4(fix44::tag_const::ExpireTime); p += fix44::datetime::format_UTCTimestamp_compact(m_ExpireTime, p, includeMilliseconds); *p++ = fix44::serialize::SOH; }
	if (hasTransactTime()) { WRITE_TAG3(fix44::tag_const::TransactTime); p += fix44::datetime::format_UTCTimestamp_compact(m_TransactTime, p, includeMilliseconds); *p++ = fix44::serialize::SOH; }
	if ((p = m_SpreadOrBenchmarkCurveData.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasPriceType()) { WRITE_TAG4(fix44::tag_const::PriceType); p += falconfix::itoa(m_PriceType, p); *p++ = fix44::serialize::SOH; }
	if (hasPrice()) { WRITE_TAG3(fix44::tag_const::Price); p += falconfix::dtoa(m_Price, p); *p++ = fix44::serialize::SOH; }
	if (hasPrice2()) { WRITE_TAG4(fix44::tag_const::Price2); p += falconfix::dtoa(m_Price2, p); *p++ = fix44::serialize::SOH; }
	if ((p = m_YieldData.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if ((p = m_Parties.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool QuotReqGrp::NoRelatedSym::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::PrevClosePx: setPrevClosePx(fix44::parser::parse_double(begin, end)); return true;
	case field_id::QuoteRequestType: setQuoteRequestType(falconfix::atoi(begin, end)); return true;
	case field_id::QuoteType: setQuoteType(falconfix::atoi(begin, end)); return true;
	case field_id::TradingSessionID: setTradingSessionID({begin, len}); return true;
	case field_id::TradingSessionSubID: setTradingSessionSubID({begin, len}); return true;
	case field_id::TradeOriginationDate: setTradeOriginationDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::Side: setSide(fix44::parser::parse_char(begin, end)); return true;
	case field_id::QtyType: setQtyType(falconfix::atoi(begin, end)); return true;
	case field_id::SettlType: setSettlType(fix44::parser::parse_char(begin, end)); return true;
	case field_id::SettlDate: setSettlDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::SettlDate2: setSettlDate2(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::OrderQty2: setOrderQty2(fix44::parser::parse_double(begin, end)); return true;
	case field_id::Currency: setCurrency({begin, len}); return true;
	case field_id::Account: setAccount({begin, len}); return true;
	case field_id::AcctIDSource: setAcctIDSource(falconfix::atoi(begin, end)); return true;
	case field_id::AccountType: setAccountType(falconfix::atoi(begin, end)); return true;
	case field_id::QuotePriceType: setQuotePriceType(falconfix::atoi(begin, end)); return true;
	case field_id::OrdType: setOrdType(fix44::parser::parse_char(begin, end)); return true;
	case field_id::ValidUntilTime: setValidUntilTime(falconfix::atoi(begin, end)); return true;
	case field_id::ExpireTime: setExpireTime(falconfix::atoi(begin, end)); return true;
	case field_id::TransactTime: setTransactTime(falconfix::atoi(begin, end)); return true;
	case field_id::PriceType: setPriceType(falconfix::atoi(begin, end)); return true;
	case field_id::Price: setPrice(fix44::parser::parse_double(begin, end)); return true;
	case field_id::Price2: setPrice2(fix44::parser::parse_double(begin, end)); return true;
	default: return false;
	}
}

void QuotReqGrp::reset() noexcept {
	m_NoRelatedSymGroup.clear();
	m_presence = 0;
}

bool QuotReqGrp::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoRelatedSymGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool QuotReqGrp::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool QuotReqGrp::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoRelatedSym: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoRelatedSymGroup.clear();
			m_NoRelatedSymGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoRelatedSymGroup.emplace_back();
				if (!m_NoRelatedSymGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *QuotReqGrp::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoRelatedSymGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoRelatedSym);
		p += falconfix::itoa(static_cast<int64_t>(m_NoRelatedSymGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoRelatedSymGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool QuotReqGrp::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
