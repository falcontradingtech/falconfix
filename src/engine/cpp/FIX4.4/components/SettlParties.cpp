// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: 9b6c41f81c0da10d

#include <FIX4.4/components/SettlParties.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void SettlParties::NoSettlPartyIDs::reset() noexcept {
	m_SettlPartyID = fields::SettlPartyID_NULL;
	m_SettlPartyIDSource = fields::NULL_CHAR;
	m_SettlPartyRole = fields::NULL_INT;
	m_SettlPtysSubGrp.reset();
	m_presence = 0;
}

bool SettlParties::NoSettlPartyIDs::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_SettlPtysSubGrp.hasAnySet() || m_SettlPtysSubGrp.checkRequired());
}

bool SettlParties::NoSettlPartyIDs::decode(const char *&p, const char *end) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (consumed && tag == field_id::SettlPartyID) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoSettlPartySubIDs:
			p = tag_start;
			if (!m_SettlPtysSubGrp.decode(p, end, field_id::SettlPartyID, true)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return consumed;
}

char *SettlParties::NoSettlPartyIDs::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasSettlPartyID()) { WRITE_TAG4(fix44::tag_const::SettlPartyID); std::memcpy(p, m_SettlPartyID.data(), m_SettlPartyID.size()); p += m_SettlPartyID.size(); *p++ = fix44::serialize::SOH; }
	if (hasSettlPartyIDSource()) { WRITE_TAG4(fix44::tag_const::SettlPartyIDSource); *p++ = m_SettlPartyIDSource; *p++ = fix44::serialize::SOH; }
	if (hasSettlPartyRole()) { WRITE_TAG4(fix44::tag_const::SettlPartyRole); p += falconfix::itoa(m_SettlPartyRole, p); *p++ = fix44::serialize::SOH; }
	if ((p = m_SettlPtysSubGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	return p;
}

bool SettlParties::NoSettlPartyIDs::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::SettlPartyID: setSettlPartyID({begin, len}); return true;
	case field_id::SettlPartyIDSource: setSettlPartyIDSource(fix44::parser::parse_char(begin, end)); return true;
	case field_id::SettlPartyRole: setSettlPartyRole(falconfix::atoi(begin, end)); return true;
	default: return false;
	}
}

void SettlParties::reset() noexcept {
	m_NoSettlPartyIDsGroup.clear();
	m_presence = 0;
}

bool SettlParties::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}
	for (const auto &entry : m_NoSettlPartyIDsGroup) {
		if (!entry.checkRequired()) {
			return false;
		}
	}
	return true;
}

bool SettlParties::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool SettlParties::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoSettlPartyIDs: {
			++p;
			const char *valueBegin = p;
			while (p < end && *p != fix44::serialize::SOH) { ++p; }
			if (p >= end) { return false; }
			const char *valueEnd = p;
			++p;
			const int64_t count_i64 = falconfix::atoi(valueBegin, valueEnd);
			if (count_i64 < 0) { return false; }
			const std::size_t count = static_cast<std::size_t>(count_i64);
			m_NoSettlPartyIDsGroup.clear();
			m_NoSettlPartyIDsGroup.reserve(count);
			for (std::size_t i = 0; i < count; ++i) {
				m_NoSettlPartyIDsGroup.emplace_back();
				if (!m_NoSettlPartyIDsGroup.back().decode(p, end)) { return false; }
			}
			consumed = true;
			continue;
		}
		default:
			p = tag_start;
			return consumed;
		}
	}
	return consumed;
}

char *SettlParties::encode(char *p, bool includeMilliseconds) const noexcept {
	if (!m_NoSettlPartyIDsGroup.empty()) {
		WRITE_TAG4(fix44::tag_const::NoSettlPartyIDs);
		p += falconfix::itoa(static_cast<int64_t>(m_NoSettlPartyIDsGroup.size()), p);
		*p++ = fix44::serialize::SOH;
		for (const auto &entry : m_NoSettlPartyIDsGroup) {
			if ((p = entry.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
		}
	}
	return p;
}

bool SettlParties::setField(int32_t, const char *, const char *) noexcept {
	return false;
}

} // namespace fix44::components
