// SPDX-License-Identifier: MIT
// Copyright (c) 2026 Michel Tonetti, Herik Lima, and Fabio Galuppo
// Generated by FalconFIX Generator
// ContentHash: bdf949742a1d843c

#include <FIX4.4/components/Instrument.h>

#include <cstring>

#include <FIX4.4/utils/macros.h>
#include <FIX4.4/utils/parser.h>
#include <FIX4.4/utils/serializer.h>
#include <FIX4.4/utils/tag_constants.h>
#include <FIX4.4/utils/datetime.h>
#include <utils/string_utils.h>

namespace fix44::components {

void Instrument::reset() noexcept {
	m_Symbol = fields::Symbol_NULL;
	m_SymbolSfx = fields::SymbolSfx_NULL;
	m_SecurityID = fields::SecurityID_NULL;
	m_SecurityIDSource = fields::SecurityIDSource_NULL;
	m_SecAltIDGrp.reset();
	m_Product = fields::NULL_INT;
	m_CFICode = fields::CFICode_NULL;
	m_SecurityType = fields::SecurityType_NULL;
	m_SecuritySubType = fields::SecuritySubType_NULL;
	m_MaturityMonthYear = fields::MaturityMonthYear_NULL;
	m_MaturityDate = fields::NULL_LOCALMKTDATE;
	m_PutOrCall = fields::NULL_INT;
	m_CouponPaymentDate = fields::NULL_LOCALMKTDATE;
	m_IssueDate = fields::NULL_LOCALMKTDATE;
	m_RepoCollateralSecurityType = fields::RepoCollateralSecurityType_NULL;
	m_RepurchaseTerm = fields::NULL_INT;
	m_RepurchaseRate = fields::NULL_PERCENTAGE;
	m_Factor = fields::NULL_FLOAT;
	m_CreditRating = fields::CreditRating_NULL;
	m_InstrRegistry = fields::InstrRegistry_NULL;
	m_CountryOfIssue = fields::CountryOfIssue_NULL;
	m_StateOrProvinceOfIssue = fields::StateOrProvinceOfIssue_NULL;
	m_LocaleOfIssue = fields::LocaleOfIssue_NULL;
	m_RedemptionDate = fields::NULL_LOCALMKTDATE;
	m_StrikePrice = fields::NULL_PRICE;
	m_StrikeCurrency = fields::StrikeCurrency_NULL;
	m_OptAttribute = fields::NULL_CHAR;
	m_ContractMultiplier = fields::NULL_FLOAT;
	m_CouponRate = fields::NULL_PERCENTAGE;
	m_SecurityExchange = fields::SecurityExchange_NULL;
	m_Issuer = fields::Issuer_NULL;
	m_EncodedIssuerLen = fields::NULL_LENGTH;
	m_EncodedIssuer = fields::EncodedIssuer_NULL;
	m_SecurityDesc = fields::SecurityDesc_NULL;
	m_EncodedSecurityDescLen = fields::NULL_LENGTH;
	m_EncodedSecurityDesc = fields::EncodedSecurityDesc_NULL;
	m_Pool = fields::Pool_NULL;
	m_ContractSettlMonth = fields::ContractSettlMonth_NULL;
	m_CPProgram = fields::NULL_INT;
	m_CPRegType = fields::CPRegType_NULL;
	m_EvntGrp.reset();
	m_DatedDate = fields::NULL_LOCALMKTDATE;
	m_InterestAccrualDate = fields::NULL_LOCALMKTDATE;
	m_presence = 0;
}

bool Instrument::checkRequired() const noexcept {
	if ((m_presence & REQUIRED) != REQUIRED) {
		return false;
	}

	return (!m_SecAltIDGrp.hasAnySet() || m_SecAltIDGrp.checkRequired()) &&
			(!m_EvntGrp.hasAnySet() || m_EvntGrp.checkRequired());
}

bool Instrument::decode(const char *&p, const char *end) noexcept {
	return decode(p, end, 0, false);
}

bool Instrument::decode(const char *&p, const char *end, int32_t stop_tag, bool stop_on_repetition) noexcept {
	bool consumed = false;
	while (p < end) {
		const char *tag_start = p;
		int32_t tag = 0;
		while (p < end && *p != '=') {
			const unsigned d = static_cast<unsigned>(*p - '0');
			if (d > 9) { return false; }
			tag = tag * 10 + static_cast<int32_t>(d);
			++p;
		}
		if (p >= end) { return false; }
		if (stop_on_repetition && consumed && tag == stop_tag) {
			p = tag_start;
			break;
		}
		switch (tag) {
		case field_id::NoSecurityAltID:
			p = tag_start;
			if (!m_SecAltIDGrp.decode(p, end, stop_tag, stop_on_repetition)) { return false; }
			consumed = true;
			continue;
		case field_id::NoEvents:
			p = tag_start;
			if (!m_EvntGrp.decode(p, end, stop_tag, stop_on_repetition)) { return false; }
			consumed = true;
			continue;
		default:
			break;
		}
		++p;
		const char *valueBegin = p;
		while (p < end && *p != fix44::serialize::SOH) { ++p; }
		if (p >= end) { return false; }
		const char *valueEnd = p;
		++p;
		if (!setField(tag, valueBegin, valueEnd)) {
			p = tag_start;
			break;
		}
		consumed = true;
	}
	return true;
}

char *Instrument::encode(char *p, bool includeMilliseconds) const noexcept {
	if (hasSymbol()) { WRITE_TAG3(fix44::tag_const::Symbol); std::memcpy(p, m_Symbol.data(), m_Symbol.size()); p += m_Symbol.size(); *p++ = fix44::serialize::SOH; }
	if (hasSymbolSfx()) { WRITE_TAG3(fix44::tag_const::SymbolSfx); std::memcpy(p, m_SymbolSfx.data(), m_SymbolSfx.size()); p += m_SymbolSfx.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecurityID()) { WRITE_TAG3(fix44::tag_const::SecurityID); std::memcpy(p, m_SecurityID.data(), m_SecurityID.size()); p += m_SecurityID.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecurityIDSource()) { WRITE_TAG3(fix44::tag_const::SecurityIDSource); std::memcpy(p, m_SecurityIDSource.data(), m_SecurityIDSource.size()); p += m_SecurityIDSource.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_SecAltIDGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasProduct()) { WRITE_TAG4(fix44::tag_const::Product); p += falconfix::itoa(m_Product, p); *p++ = fix44::serialize::SOH; }
	if (hasCFICode()) { WRITE_TAG4(fix44::tag_const::CFICode); std::memcpy(p, m_CFICode.data(), m_CFICode.size()); p += m_CFICode.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecurityType()) { WRITE_TAG4(fix44::tag_const::SecurityType); std::memcpy(p, m_SecurityType.data(), m_SecurityType.size()); p += m_SecurityType.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecuritySubType()) { WRITE_TAG4(fix44::tag_const::SecuritySubType); std::memcpy(p, m_SecuritySubType.data(), m_SecuritySubType.size()); p += m_SecuritySubType.size(); *p++ = fix44::serialize::SOH; }
	if (hasMaturityMonthYear()) { WRITE_TAG4(fix44::tag_const::MaturityMonthYear); std::memcpy(p, m_MaturityMonthYear.data(), m_MaturityMonthYear.size()); p += m_MaturityMonthYear.size(); *p++ = fix44::serialize::SOH; }
	if (hasMaturityDate()) { WRITE_TAG4(fix44::tag_const::MaturityDate); p += fix44::datetime::format_UTCDateOnly(m_MaturityDate, p); *p++ = fix44::serialize::SOH; }
	if (hasPutOrCall()) { WRITE_TAG4(fix44::tag_const::PutOrCall); p += falconfix::itoa(m_PutOrCall, p); *p++ = fix44::serialize::SOH; }
	if (hasCouponPaymentDate()) { WRITE_TAG4(fix44::tag_const::CouponPaymentDate); p += fix44::datetime::format_UTCDateOnly(m_CouponPaymentDate, p); *p++ = fix44::serialize::SOH; }
	if (hasIssueDate()) { WRITE_TAG4(fix44::tag_const::IssueDate); p += fix44::datetime::format_UTCDateOnly(m_IssueDate, p); *p++ = fix44::serialize::SOH; }
	if (hasRepoCollateralSecurityType()) { WRITE_TAG4(fix44::tag_const::RepoCollateralSecurityType); std::memcpy(p, m_RepoCollateralSecurityType.data(), m_RepoCollateralSecurityType.size()); p += m_RepoCollateralSecurityType.size(); *p++ = fix44::serialize::SOH; }
	if (hasRepurchaseTerm()) { WRITE_TAG4(fix44::tag_const::RepurchaseTerm); p += falconfix::itoa(m_RepurchaseTerm, p); *p++ = fix44::serialize::SOH; }
	if (hasRepurchaseRate()) { WRITE_TAG4(fix44::tag_const::RepurchaseRate); p += falconfix::dtoa(m_RepurchaseRate, p); *p++ = fix44::serialize::SOH; }
	if (hasFactor()) { WRITE_TAG4(fix44::tag_const::Factor); p += falconfix::dtoa(m_Factor, p); *p++ = fix44::serialize::SOH; }
	if (hasCreditRating()) { WRITE_TAG4(fix44::tag_const::CreditRating); std::memcpy(p, m_CreditRating.data(), m_CreditRating.size()); p += m_CreditRating.size(); *p++ = fix44::serialize::SOH; }
	if (hasInstrRegistry()) { WRITE_TAG4(fix44::tag_const::InstrRegistry); std::memcpy(p, m_InstrRegistry.data(), m_InstrRegistry.size()); p += m_InstrRegistry.size(); *p++ = fix44::serialize::SOH; }
	if (hasCountryOfIssue()) { WRITE_TAG4(fix44::tag_const::CountryOfIssue); std::memcpy(p, m_CountryOfIssue.data(), m_CountryOfIssue.size()); p += m_CountryOfIssue.size(); *p++ = fix44::serialize::SOH; }
	if (hasStateOrProvinceOfIssue()) { WRITE_TAG4(fix44::tag_const::StateOrProvinceOfIssue); std::memcpy(p, m_StateOrProvinceOfIssue.data(), m_StateOrProvinceOfIssue.size()); p += m_StateOrProvinceOfIssue.size(); *p++ = fix44::serialize::SOH; }
	if (hasLocaleOfIssue()) { WRITE_TAG4(fix44::tag_const::LocaleOfIssue); std::memcpy(p, m_LocaleOfIssue.data(), m_LocaleOfIssue.size()); p += m_LocaleOfIssue.size(); *p++ = fix44::serialize::SOH; }
	if (hasRedemptionDate()) { WRITE_TAG4(fix44::tag_const::RedemptionDate); p += fix44::datetime::format_UTCDateOnly(m_RedemptionDate, p); *p++ = fix44::serialize::SOH; }
	if (hasStrikePrice()) { WRITE_TAG4(fix44::tag_const::StrikePrice); p += falconfix::dtoa(m_StrikePrice, p); *p++ = fix44::serialize::SOH; }
	if (hasStrikeCurrency()) { WRITE_TAG4(fix44::tag_const::StrikeCurrency); std::memcpy(p, m_StrikeCurrency.data(), m_StrikeCurrency.size()); p += m_StrikeCurrency.size(); *p++ = fix44::serialize::SOH; }
	if (hasOptAttribute()) { WRITE_TAG4(fix44::tag_const::OptAttribute); *p++ = m_OptAttribute; *p++ = fix44::serialize::SOH; }
	if (hasContractMultiplier()) { WRITE_TAG4(fix44::tag_const::ContractMultiplier); p += falconfix::dtoa(m_ContractMultiplier, p); *p++ = fix44::serialize::SOH; }
	if (hasCouponRate()) { WRITE_TAG4(fix44::tag_const::CouponRate); p += falconfix::dtoa(m_CouponRate, p); *p++ = fix44::serialize::SOH; }
	if (hasSecurityExchange()) { WRITE_TAG4(fix44::tag_const::SecurityExchange); std::memcpy(p, m_SecurityExchange.data(), m_SecurityExchange.size()); p += m_SecurityExchange.size(); *p++ = fix44::serialize::SOH; }
	if (hasIssuer()) { WRITE_TAG4(fix44::tag_const::Issuer); std::memcpy(p, m_Issuer.data(), m_Issuer.size()); p += m_Issuer.size(); *p++ = fix44::serialize::SOH; }
	if (hasEncodedIssuerLen()) { WRITE_TAG4(fix44::tag_const::EncodedIssuerLen); p += falconfix::itoa(m_EncodedIssuerLen, p); *p++ = fix44::serialize::SOH; }
	if (hasEncodedIssuer()) { WRITE_TAG4(fix44::tag_const::EncodedIssuer); std::memcpy(p, m_EncodedIssuer.data(), m_EncodedIssuer.size()); p += m_EncodedIssuer.size(); *p++ = fix44::serialize::SOH; }
	if (hasSecurityDesc()) { WRITE_TAG4(fix44::tag_const::SecurityDesc); std::memcpy(p, m_SecurityDesc.data(), m_SecurityDesc.size()); p += m_SecurityDesc.size(); *p++ = fix44::serialize::SOH; }
	if (hasEncodedSecurityDescLen()) { WRITE_TAG4(fix44::tag_const::EncodedSecurityDescLen); p += falconfix::itoa(m_EncodedSecurityDescLen, p); *p++ = fix44::serialize::SOH; }
	if (hasEncodedSecurityDesc()) { WRITE_TAG4(fix44::tag_const::EncodedSecurityDesc); std::memcpy(p, m_EncodedSecurityDesc.data(), m_EncodedSecurityDesc.size()); p += m_EncodedSecurityDesc.size(); *p++ = fix44::serialize::SOH; }
	if (hasPool()) { WRITE_TAG4(fix44::tag_const::Pool); std::memcpy(p, m_Pool.data(), m_Pool.size()); p += m_Pool.size(); *p++ = fix44::serialize::SOH; }
	if (hasContractSettlMonth()) { WRITE_TAG4(fix44::tag_const::ContractSettlMonth); std::memcpy(p, m_ContractSettlMonth.data(), m_ContractSettlMonth.size()); p += m_ContractSettlMonth.size(); *p++ = fix44::serialize::SOH; }
	if (hasCPProgram()) { WRITE_TAG4(fix44::tag_const::CPProgram); p += falconfix::itoa(m_CPProgram, p); *p++ = fix44::serialize::SOH; }
	if (hasCPRegType()) { WRITE_TAG4(fix44::tag_const::CPRegType); std::memcpy(p, m_CPRegType.data(), m_CPRegType.size()); p += m_CPRegType.size(); *p++ = fix44::serialize::SOH; }
	if ((p = m_EvntGrp.encode(p, includeMilliseconds)) == nullptr) { return nullptr; }
	if (hasDatedDate()) { WRITE_TAG4(fix44::tag_const::DatedDate); p += fix44::datetime::format_UTCDateOnly(m_DatedDate, p); *p++ = fix44::serialize::SOH; }
	if (hasInterestAccrualDate()) { WRITE_TAG4(fix44::tag_const::InterestAccrualDate); p += fix44::datetime::format_UTCDateOnly(m_InterestAccrualDate, p); *p++ = fix44::serialize::SOH; }
	return p;
}

bool Instrument::setField(int32_t tag, const char *begin, const char *end) noexcept {
	const std::size_t len = static_cast<std::size_t>(end - begin);

	switch (tag) {
	case field_id::Symbol: setSymbol({begin, len}); return true;
	case field_id::SymbolSfx: setSymbolSfx({begin, len}); return true;
	case field_id::SecurityID: setSecurityID({begin, len}); return true;
	case field_id::SecurityIDSource: setSecurityIDSource({begin, len}); return true;
	case field_id::Product: setProduct(falconfix::atoi(begin, end)); return true;
	case field_id::CFICode: setCFICode({begin, len}); return true;
	case field_id::SecurityType: setSecurityType({begin, len}); return true;
	case field_id::SecuritySubType: setSecuritySubType({begin, len}); return true;
	case field_id::MaturityMonthYear: setMaturityMonthYear({begin, len}); return true;
	case field_id::MaturityDate: setMaturityDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::PutOrCall: setPutOrCall(falconfix::atoi(begin, end)); return true;
	case field_id::CouponPaymentDate: setCouponPaymentDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::IssueDate: setIssueDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::RepoCollateralSecurityType: setRepoCollateralSecurityType({begin, len}); return true;
	case field_id::RepurchaseTerm: setRepurchaseTerm(falconfix::atoi(begin, end)); return true;
	case field_id::RepurchaseRate: setRepurchaseRate(fix44::parser::parse_double(begin, end)); return true;
	case field_id::Factor: setFactor(fix44::parser::parse_double(begin, end)); return true;
	case field_id::CreditRating: setCreditRating({begin, len}); return true;
	case field_id::InstrRegistry: setInstrRegistry({begin, len}); return true;
	case field_id::CountryOfIssue: setCountryOfIssue({begin, len}); return true;
	case field_id::StateOrProvinceOfIssue: setStateOrProvinceOfIssue({begin, len}); return true;
	case field_id::LocaleOfIssue: setLocaleOfIssue({begin, len}); return true;
	case field_id::RedemptionDate: setRedemptionDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::StrikePrice: setStrikePrice(fix44::parser::parse_double(begin, end)); return true;
	case field_id::StrikeCurrency: setStrikeCurrency({begin, len}); return true;
	case field_id::OptAttribute: setOptAttribute(fix44::parser::parse_char(begin, end)); return true;
	case field_id::ContractMultiplier: setContractMultiplier(fix44::parser::parse_double(begin, end)); return true;
	case field_id::CouponRate: setCouponRate(fix44::parser::parse_double(begin, end)); return true;
	case field_id::SecurityExchange: setSecurityExchange({begin, len}); return true;
	case field_id::Issuer: setIssuer({begin, len}); return true;
	case field_id::EncodedIssuerLen: setEncodedIssuerLen(falconfix::atoi(begin, end)); return true;
	case field_id::EncodedIssuer: setEncodedIssuer({begin, len}); return true;
	case field_id::SecurityDesc: setSecurityDesc({begin, len}); return true;
	case field_id::EncodedSecurityDescLen: setEncodedSecurityDescLen(falconfix::atoi(begin, end)); return true;
	case field_id::EncodedSecurityDesc: setEncodedSecurityDesc({begin, len}); return true;
	case field_id::Pool: setPool({begin, len}); return true;
	case field_id::ContractSettlMonth: setContractSettlMonth({begin, len}); return true;
	case field_id::CPProgram: setCPProgram(falconfix::atoi(begin, end)); return true;
	case field_id::CPRegType: setCPRegType({begin, len}); return true;
	case field_id::DatedDate: setDatedDate(fix44::parser::parse_date32(begin, end)); return true;
	case field_id::InterestAccrualDate: setInterestAccrualDate(fix44::parser::parse_date32(begin, end)); return true;
	default: return false;
	}
}

} // namespace fix44::components
